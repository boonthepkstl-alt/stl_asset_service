# RAISE — Open Findings

**Purpose:** every known gap, unresolved question, or piece of debt that
affects this project, collected in one place so nothing is only findable by
reading every document in the chain. Nothing here is new — each item is
already flagged somewhere in `docs/`; this file just indexes them. For the
authoritative detail on any PRD-level question, follow the link back to
`RAISE-PRD.md` §16 — this file summarizes, it doesn't supersede.

**Maintenance rule:** add a finding when one surfaces during any checkpoint
(per [`PROJECT-CHECKPOINTS.md`](PROJECT-CHECKPOINTS.md)'s process). Mark
**Resolved** with the resolving PR/commit when closed — never delete a row,
so the history of what was once open stays visible.

---

## Blocking (gates an MVP requirement)

| ID | Area | Description | Source | Status |
|---|---|---|---|---|
| F-03 | Executive Dashboard | NBV/Risk KPI formulas and thresholds | PRD §16 Q3–Q4 | **Open — but substantially narrowed 2026-09-05 (R-28).** Three of the four things this finding covered are now settled, and what remains is a single missing input rather than an undefined problem. **(1) Utilization was never actually blocked on a decision** — PRD §16 Resolved Questions 27 and 29 had fixed its definition and mechanics on 2026-08-21; it was missing only because nobody had built it. Built and live 2026-09-05 (PR #102, `321265f`). **(2) NBV's formula is confirmed** (§16 **Resolved Question 46**): straight-line from the `purchaseDate`/`purchaseCost` fields that already exist, useful life configurable per Asset Category in Settings, salvage zero, clamped at 0. **(3) Risk is confirmed OUT of MVP scope** (§16 **Resolved Question 47**), which *confirms rather than changes* what the PRD already said — `RAISE-AI-RISK-001` was already Priority "Pilot (not confirmed MVP)", so §16 Q4 was never a blocker on this P0 requirement. **What is left:** the default per-Asset-Category useful-life values, which business was asked for directly and answered "I will specify these myself" — tracked as new PRD §16 **Open Question 3a**. NBV is therefore **specified-but-not-yet-buildable**, a materially different state from the "formulas and thresholds undefined" this finding was opened for. Do not read the narrowing as closure. **Groundwork built 2026-09-07, still open on the same one input (R-36).** `frontend/src/lib/nbv.ts` now implements RQ46's formula in full — straight-line from `purchaseDate`/`purchaseCost`, salvage zero, clamped at 0 — with the per-category useful life taken as an **injected lookup**, exactly as `lib/alerts.ts` takes `warrantyThresholdFor` rather than hardcoding RQ41's 90 days. **No default is defined anywhere in the module or its tests**, so nothing here invents the numbers business has not supplied. 15 unit tests, each passing its own values in. **What this changes about the finding:** it was described as fully blocked; it never was. The formula half was buildable the moment RQ46 landed, and the tile still cannot render until real defaults exist. **What remains, stated precisely rather than as "the chain sync" — restated again 2026-09-08, because the 2026-09-07 wording went stale twice over in one day:** **(1)** the Dashboard/Executive tile specification pass **was performed** (Design v0.17 → Matrix v2.7), so it is no longer "required first" — **F-52**'s specification half is CLOSED. **(2)** the values are **no longer five per Asset Category**. PRD §16 **Resolved Question 52** (2026-09-08) confirmed the useful life is configured **per Asset Type**, amending RQ46, because business could not give a single fixed figure for **IT Hardware** (*"it depends on the equipment purchased"*) while confirming the other four categories can each carry one. **What is actually needed now:** one value **per Asset Type present in the data** — as seeded, Laptop/Monitor/Headphones, Smartphone/Tablet, Printer/Projector, Router/Server, Camera — and **not a fixed enumeration**, since `type` is a free-text `varchar(100)` that grows exactly as `category` does. Then the Settings section, the tile, and execution. **A second chain sync is also owed**, because the synced chain still keys the configuration by Category — raised as **F-53**. |
| F-04 | Oracle FA Integration | Integration method, mapping, sync, security | PRD §16 Q6–Q10 | Open |
| F-05 | Alerts | Trigger rules and channels | PRD §16 (Alerts section) | **RESOLVED 2026-09-04 (R-23)** — see the Resolved table. Five MVP trigger conditions and a fixed 3-level severity confirmed by business Q&A and propagated through all seven chain documents ([PR #95](https://github.com/boonthepkstl-alt/stl_asset_service/pull/95)). **Note the finding's own premise was partly wrong:** the *channels* half was never actually open — `RAISE-FR-ALERT-001`'s Scope line already read "MVP (single-channel; multi-channel is Roadmap)", so only stale TBD wording was removed, not a new decision taken. **This closes the specification only.** Four of the five conditions are still unbuilt — tracked as **Gap 16** in the traceability matrix, deliberately left open |
| F-06 | Natural Language Search | Citation precision/format | PRD §16 Q18 | Open |
| F-07 | Document Intelligence | Confidence thresholds, field lists, matching/merge rules | PRD §16 Q20a (asked, left unanswered) + Resolved Q28/30–32 (scope only) | Open |
| F-08 | Auth / RBAC | Auth mechanism and role/permission matrix content | PRD §16 Q21–Q22 | **Open — but narrowed 2026-09-04 (R-25).** Two sub-questions are now resolved (PRD v0.16 §16 Resolved Question 45): the **Alerts screen's access gate** is confirmed as *any authenticated user* (all four roles), and **role enforcement is declared in code per route** (`ProtectedRoute allowedRoles`), not data-driven. That closed `AC-ALERT-001-01`'s access-gate blocker and took **`RAISE-FR-ALERT-001` to a full `PASS`**. **Still open, and this is the larger part:** the role/permission matrix *content* for **every screen other than Alerts**, and the authentication mechanism itself (Q21) — no real user store exists (**F-11**) and `middleware.RequireRole` is wired only to the Go template's demo routes (**F-12**). **Recorded consequence:** the shipped Role Management screen presents an editable, persisted 15×6 permission matrix that **enforces nothing** — no code reads it. It is presentational only, and must not be read as working access control |
| F-53 | Executive Dashboard / chain consistency | The deliverable chain and the PRD **disagree about how NBV useful life is keyed**. The chain was synced on 2026-09-08 for RQ50/RQ51 and specifies **`NBVSettings: Record<AssetCategory, usefulLifeYears>`** — keyed by **Category** — across Design v0.17, Prototype v0.18, AC v0.17, Test Plan v0.17, Test Cases v0.27 and Matrix v2.7. **Later the same day, PRD §16 Resolved Question 52 superseded that key with per Asset Type.** The most concrete instance: `TC-WARRANTY-001-07` reads *"P-018 Settings NBV section shows **all 5 categories** with editable useful-life inputs"*, which is now the wrong shape. **This is a deliberate deferral, not a discovered defect** — it was created knowingly, recorded inside RQ52 itself at the moment it arose, and deferred so the second sync can be done **once**, together with the numeric values, instead of twice. It is raised here so the deferral carries a tracking ID and cannot quietly become a stale specification of the kind **F-52** already was. | Created and recorded 2026-09-08 when RQ52 was written; the six chain versions it contradicts had been committed hours earlier the same day (`4a442be`) | **RESOLVED 2026-09-08 (R-39) — raised and closed the same day.** The second chain sync was performed: Design v0.18, Prototype v0.19, AC v0.18, Test Plan **v0.19**, Test Cases v0.28, Matrix v2.8. `TC-WARRANTY-001-07` — the sharpest instance — now reads *"one row per Asset Type currently present in the data"*. **Matrix v2.8 reached the same verdict independently**, opening and closing **Gap 22** to record it, while noting the closure does **not** cover the missing values or the unbuilt tile (those stay under **Gap 21** and **F-03**). **The deferral was reversed on evidence, and the reasoning is the record worth keeping:** it had been deferred so the six subagents would run once, on the premise that the values were imminent. They had been asked for **three times over two days**, and the last answer changed the *model* instead of supplying numbers — so the premise was unsafe. Leaving PRD v0.20 and the chain contradicting each other would have made this **the fifth** finding in one session about a document still asserting something that had stopped being true (after F-47, F-50, F-51, F-52) — and the first one created deliberately. *History, superseded:* while open, closing it early was judged to cost a duplicate six-subagent run. **Nothing reads the wrong key today** — `NBVSettings` does not exist in `frontend/src`, `lib/nbv.ts` has zero consumers, and every affected test case is **BLOCKED**, so no verdict rests on it. **What must not happen:** the chain being read as authoritative on the key. PRD v0.20 is the authority; the chain is stale on this one point and says so in RQ52. Tracked alongside **F-03**, and expected to close in the same pass that records the per-Type values. |
| F-55 | IT Requisition & Maintenance / Create Requisition (real-API mode) | **The Create IT Requisition page cannot submit at all when the API flags are on, because its default requester is a mock fixture id.** `frontend/src/pages/CreateRequisition/index.tsx:60` reads `const requesterId = params.get('requesterId') \|\| 'e1';`. **`'e1'` is a `mockData.ts` fixture id** (`mockData.ts:345`), not an Employee that exists in Postgres — server-side ids are UUIDs (e.g. `04ac3239-eed4-4630-81cd-e94beba7a006`). With `VITE_EMPLOYEE_API_ENABLED=true`, `ticket-service.ts:30`'s `employeeService.getEmployee(input.requesterId)` resolves over HTTP, gets a **404**, returns `null`, and **line 31 throws `Employee e1 not found` before any `POST /tickets` is attempted**. `CreateRequisition/index.tsx:130`'s bare `catch` collapses it to the generic *"Unable to submit the IT requisition. Please try again."*, so the page reports a retryable failure for a condition that **can never succeed on retry**. **Affected flow:** entering the page **without** a `requesterId` param — i.e. from the nav, from the Maintenance list's "New IT Requisition" button, and from Asset Detail (whose button passes `assetId`/`title`/`location` but **not** `requesterId`, per this file's own comment block at lines 55-57). Only the **Employee Detail** entry point passes a real `requesterId`. **Why it works in mock mode and fails in real-API mode:** `MockEmployeeRepository` is seeded from the same `mockData.ts` that defines `'e1'`, so the lookup resolves; `HttpEmployeeRepository` asks Postgres, which has never heard of `'e1'`. **The failure is therefore invisible to the entire existing test suite** (54 files / 286 tests, all mock-backed) and to `tsc` — `'e1'` is a valid `string` either way. **Impact:** `RAISE-FR-MAINT-001`'s primary create flow is unusable in the one configuration that talks to the real backend — which is **`docker-compose.yml`'s default** (`VITE_TICKET_API_ENABLED` and `VITE_EMPLOYEE_API_ENABLED` both default `true` there). **Nothing user-facing is broken today**, because the flags default to `false` outside that stack (`featureFlags.ts:20-30`, `.env.example:28`) — this is stated as scope, not as mitigation: the defect is real and will surface the moment the flags flip. **No workaround belongs in the product;** the one used to prove Gap 27 is a **test-harness route, not a fix**: navigating to `/maintenance/create?requesterId=<real employee UUID>&assetId=seed-a1&priority=<P>` uses the page's own already-supported query-param entry point (the same one Employee Detail uses) with an id fetched from `GET /api/employees`. | Found 2026-09-09 during the **Gap 27** frontend real-API execution, from the browser's own network log — `GET http://localhost:8080/api/employees/e1` → **404 Not Found**, with **no** `POST /api/tickets` following it and the generic error text on screen. Not found by any test, and not visible to static inspection alone. | **OPEN — BLOCKED on a business decision, and deliberately not fixed.** The code change is small; **deciding what it should do is not, and is not this project's call to make.** **DECISION REQUIRED: how is the requester resolved in the default Create Requisition flow?** Three shapes were identified while documenting this, and **none is chosen here** — (a) **the logged-in user**, (b) **an explicit employee/delegated-requester selection on the form itself**, or (c) **another rule the stakeholder specifies**. **A fact that constrains (a), verified rather than assumed:** `AuthContext`'s `User` is `{id, username, fullName, role}` (`types/auth.ts:4-9`) and carries **no employee id** — the demo user's `id` is the literal `"admin"`, and auth is demo-only with one hardcoded user and no user store (see `CLAUDE.md`, and **F-08**/PRD §16 Q21—Q22, still open). So (a) is **not implementable today** without first establishing an auth-user → Employee link that does not exist, which makes this finding partly downstream of **F-08**. (b) is implementable now but **adds a field to a confirmed screen**, which is a spec change requiring the same authority. **Explicitly does not change Gap 27's verdict.** Gap 27 asked whether `ticket-service.ts`'s real-API branch had ever been executed; it now has been, end to end, all four priorities (`ITR-2026-005`…`008`, `slaTargetHours` **2/8/24/48** confirmed in the `POST` response the frontend received, in an independent frontend `GET` read-back, and in the Postgres row). **That is a coverage question and this is a defect question** — the execution reached the code path precisely *because* the defect was routed around through a supported entry point, and conflating the two would either falsify a real PASS or bury a real defect. **Not to be marked resolved by any test, document or workaround** — only by a stakeholder decision plus the change that implements it. `RAISE-FR-MAINT-001` is untouched by this row pending that decision; **no SLA value, business rule or API contract is affected.** |
| F-52 | Executive Dashboard / Main Dashboard | The NBV tile has **no forward specification anywhere in the chain** — every layer specifies its **absence**, so a built tile would contradict the criteria meant to accept it. Verified line by line on `a92aeb5`: **`AC-DASH-03b`** — *"when a user inspects it for an NBV tile, **then no such tile is present**"*; **`AC-EXEC-001-03b`** — *"**then no such tile is present**"*; **`TC-DASH-03b`** expected result — *"No NBV tile is present in the shipped grid — **this documents today's accurate absence, not a target for NBV to be displayed**"*; **`RAISE-PROTOTYPE.md`:783** — *"**No NBV tile exists on this dashboard today, and none can be built**"*. **Two concrete consequences, not stylistic ones:** (1) `TC-DASH-03b` and `TC-EXEC-001-03b` are **not** cases a built tile would pass — building it makes them **fail**; (2) `AC-DASH-01`/`AC-EXEC-001-01` assert a **nine-tile** grid in fixed order and `TC-DASH-01`/`TC-EXEC-001-01` are recorded **PASS** against it, so a tenth tile invalidates those records too. **The Settings half is unaffected and is genuinely specified as presence** — Prototype §23A, `AC-WARRANTY-001-07` and `TC-WARRANTY-001-07` (*"shows all 5 categories with editable useful-life inputs"*), which needs only executing. **This is not a defect in those documents:** each correctly described a state in which the tile could not be built (F-03). It becomes a gap only at the moment F-03 unblocks, which is why it is raised now rather than discovered mid-implementation. | Found 2026-09-07 during the F-03 Business Input Gate review, by checking `NEXT-STEP.md`'s own claim that the chain was "specified end-to-end" against the chain documents. **That claim was this AI's, written the same day as a correction to an earlier claim of its own** — the second overshoot in the same area. Corrected in `NEXT-STEP.md` at `eeefad7` | **Open — but the specification half is CLOSED as of 2026-09-08.** DoR-3 was answered (**PRD §16 Resolved Question 50**: NBV is a **tenth tile**, Monthly Depreciation kept unchanged) and the chain was synced end to end: Design v0.17, Prototype v0.18, AC v0.17, Test Plan v0.17, Test Cases v0.27, Matrix v2.7. `AC-DASH-03b`/`AC-EXEC-001-03b` and their cases are now **presence** specifications, and the nine-tile assertion is now a **ten**-tile assertion. **What remains open is the half this finding also named:** the tile is not built and cannot be, because F-03's five useful-life values are still missing, so `TC-DASH-01`/`TC-EXEC-001-01` still need **re-execution** against a real ten-tile grid — tracked as **Gap 21** in the matrix, which the matrix opened rather than declaring the sync complete. **The matrix judged this finding in two halves rather than closing it wholesale**, and that judgement is adopted here. *History, superseded:* while the specification half was open it was gated on F-03's DoR-3 — the decision about how a real NBV figure is presented beside the existing **static, illustrative "Monthly Depreciation"** tile (`Dashboard/index.tsx:78` → `mockData.ts:755`, `monthlyDepreciation: 42800`) — because that answer determines what the criteria say. **Writing the criteria first would mean inventing the presentation decision**, which is exactly what this project forbids. **Consequence worth stating plainly: even if F-03's five useful-life values arrive, no Dashboard code can begin until DoR-3 is answered.** The Settings section could proceed on the values alone. **That tile must not be changed, removed or reinterpreted without a requirement or business decision behind it.** Tracked alongside **F-03**; `RAISE-FR-EXEC-001` stays **`PASS (partial)`** meanwhile. |

## Unresolved (scope question, not yet blocking a build)

| ID | Area | Description | Source | Status |
|---|---|---|---|---|
| F-09 | Asset field list | Full asset master field list | PRD §16 Q1 | Open **Reclassified 2026-09-07 (F-48):** moved out of "Blocking (gates an MVP requirement)". It gates nothing — it appears **zero** times in `RAISE-TRACEABILITY-MATRIX.md`, and no requirement's verdict anywhere in the chain depends on it. Genuinely open, but as a scope question, not a blocker. |
| F-35 | Asset code generation scheme | RAISE currently auto-generates `AST-####`; a real company handover form (Singer Thailand "ใบดำเนินการเกี่ยวกับคอมพิวเตอร์และอุปกรณ์" Version 2024) shows the company's actual convention is `<Company><YY><MM><Dept><TypeCode><4-digit running number>` (e.g. `STL2203ITNB0066`, `STL2310ITOT0042`) | User-supplied reference file, 2026-09-02 (`Retail9972 ไทวัสดุ ภูเก็ต.xlsx`) | Open — **partially confirmed 2026-09-02**: the running number is a single continuous company-wide counter (not reset per month/year, not per type). **Still blocked**: the 2-letter type-code table (only `NB`=Notebook and `OT`=? are attested; Monitor/Keyboard/Mouse/UPS/Scanner/Printer/Bag codes unknown) and the department-code table (only `IT` attested) — user explicitly declined to guess these, waiting on the actual list from the company's IT department before implementation **Reclassified 2026-09-07 (F-48):** moved out of "Blocking (gates an MVP requirement)". The information really is outstanding, but `RAISE-FR-ASSET-001` is a full `PASS` on the current `AST-####` scheme, so this gates a **future enhancement**, not an MVP verdict (zero mentions in the traceability matrix or the Compliance Review). |
| F-36 | Employee ID convention | RAISE previously auto-generated `EMP-<8 hex chars>` (backend) / `EMP-####` (seed fixtures) with no reference to any real convention. **Business confirmed 2026-09-03** (direct Q&A) that the company's actual Employee ID is **exactly 8 digits, where the first 2 are the Gregorian join year** (e.g. `26725898` = joined 2026), and the **remaining 6 digits are issued by HR/payroll — RAISE cannot derive or generate them**. The direct sibling of **F-35** (asset code generation scheme): same shape of question, opposite answer — for assets the company's format is *constructible*, so the open part is the missing code tables; for employees the format is *not* constructible by RAISE at all, so generation is out of scope by nature, not by lack of information | User Q&A, 2026-09-03; implemented in [PR #81](https://github.com/boonthepkstl-alt/stl_asset_service/pull/81) (`CHECKPOINT-2026-09-03-006`) | Open — **not blocked**: the confirmed scope (validate the format on input only, deliberately *not* auto-generate) is **already shipped** — Create Employee rejects anything that isn't exactly 8 digits, on blur and on submit, with format checked before uniqueness. **What remains open** is a genuine follow-up: **seed fixtures and the backend auto-generation fallback still emit the legacy `EMP-####`/`EMP-<hex>` form, which the app's own new format check rejects** — so RAISE currently enforces a convention its own generated data does not follow. Switching them would require a rule for the HR-issued 6-digit portion, which HR owns and RAISE cannot invent; the alternative is removing auto-generation entirely on the grounds that RAISE has no legitimate way to mint an HR-issued number. Not decided either way — do not pick one without asking |
| F-37 | Employee login provisioning (username/password) | Raised during the 2026-09-03 Create Employee work: should creating an Employee also provision a login (username/password)? **Deferred, not built.** Four independent reasons, none of which this session had authority to overturn: (1) `User` (auth) and `Employee` are **deliberately unlinked** in the data model — no `employeeId` exists on the auth type (verified by grep), and `ESAPS-UI-FOUNDATION-BASELINE.md` records an explicit **DEFER** on `UserManagement.tsx`/`Auth.tsx`/`Profile.tsx` precisely because no role/permission model exists to build invite/suspend flows against; (2) the PRD marks the authentication mechanism itself **TBD** (§16 Q21–Q22, tracked as **F-08**); (3) the backend has **no user table at all** — auth is a single hardcoded demo user (**F-11**); (4) the existing User Management page uses an **invite-based** flow, not admin-typed passwords, which is also the safer pattern and the one any future implementation should follow rather than adding a password field to an employee form | User Q&A, 2026-09-03; `frontend/src/pages/UserManagement/index.tsx`; `docs/project-foundation-baseline/ESAPS-UI-FOUNDATION-BASELINE.md` | Open — deferred by design, blocked behind **F-08**/**F-11**, not an oversight. Nothing was built; the app's behavior is unchanged |
| F-39 | "Modify Specs" button has no specification fields to modify | Employee Detail's **IT Workstation & Environment Profile** card (Workstation Archetype, primary operating systems, etc.) carries a "Modify Specs" action. It routes to the Edit Employee page — which contains **no workstation, OS, or spec field at all**, only Job Title, Phone, Status, Department, Location Campus, Physical Desk and Reporting Manager. A user who clicks it expecting to change the values shown on that card lands somewhere that cannot change any of them. The mislabel **predates** [PR #87](https://github.com/boonthepkstl-alt/stl_asset_service/pull/87) — the old modal was wired to the same handler and was equally unable to edit specs — but PR #87 made it more costly: a modal opened instantly over the same context, whereas this is now a full route change and a Back trip to recover. Found by the code review on PR #87 and **deliberately not fixed there**: both available fixes change product scope, and neither is RAISE's call to make unilaterally | Code review of [PR #87](https://github.com/boonthepkstl-alt/stl_asset_service/pull/87), 2026-09-04 (`CHECKPOINT-2026-09-04-001`); `frontend/src/pages/EmployeeDetail/index.tsx:286` | Open — **product decision required, not an engineering task.** Exactly two options, both scope changes: **(a)** make the workstation/OS spec fields editable on the Edit Employee page, or **(b)** remove the button and leave the card read-only. **Do not pick one without asking.** Note there is no governing requirement to appeal to — the PRD has **no `RAISE-FR-EMP-*` requirement at all** (the same gap already recorded against PRs #78–#82), so neither option can be derived from the chain |
| F-10 | Custody vs. Check-in/out | `RAISE-FR-ASSET-003` (Custody History) and `RAISE-FR-OPS-002` (Check-in/Check-out) cover adjacent ground; overlap flagged twice in the PRD's own Pre-Finalization Quality Pass without resolution | PRD Pre-Finalization Quality Pass | Open |
| F-54 | Maintenance / Ticket SLA | **The product ships concrete SLA commitments that no business decision authorises, and shows them to users.** `frontend/src/services/ticket-service.ts:14` defines `SLA_HOURS = { Critical: 2, High: 8, Medium: 24, Low: 48 }`; `go-template-main/service/ticketService.go:19-26` carries the same map with a comment saying it *"mirrors frontend/src/services/ticket-service.ts's SLA_HOURS map exactly"*; every ticket created gets `slaTargetHours` stamped from it; and the priority selector labels the choices **"Critical (2h SLA)"**, **"High (8h SLA)"**, **"Medium (24h SLA)"**, **"Low (48h SLA)"**, so a requester reads a time commitment at the moment of filing. **The chain says the opposite, in its own words.** `RAISE-PRD.md` states *"Still TBD: SLA per stage"* (§8 `RAISE-FR-MAINT-001` Open Question row, and again in the §16 narrative); **no §16 Resolved Question covers SLA at all**; `RAISE-PROTOTYPE.md` §15 P-009 lists **`Priority (conceptual — TBD)`**; and `RAISE-ACCEPTANCE-CRITERIA.md` §12 says *"SLA per stage… remain **TBD** and are marked **NOT TESTABLE YET**"*. **No Prototype, AC or Test Case anywhere names a numeric SLA value** — checked by grep across all three. **Origin, traced rather than assumed:** the values are inherited from the ESAPS reference (`src/data/requisitionData.ts:231,328` seed `slaTargetHours: 8` / `24`), so **RAISE did not invent them** — but inheriting an unconfirmed number is not the same as having it confirmed, and the ESAPS app is **REFERENCE ONLY** per `CLAUDE.md`. **Why this matters more than its size suggests:** it is **F-03 inverted**. F-03 is five business numbers correctly *withheld* from the product — this project refused to invent them across four separate requests, and PRD §16 Q3a says in terms *"Do not invent or use an illustrative number as if confirmed."* SLA is four business numbers **already invented and shipped**, in both tiers, displayed as a promise. **The same rule was applied with great care to the numbers nobody uses and never applied to the numbers in production.** | Found 2026-09-08 while assessing what to do next, by tracing the `priority` field that PR #122 carried onto the new full-page form. **It was visible during PR #122 and waved through as "pre-existing"** — recorded that way because the miss is part of the finding | **RESOLVED 2026-09-08 (R-40) — raised and closed the same day.** Business chose **option (a)** of the three exits below: **confirm the four values as they stand** — Critical **2h**, High **8h**, Medium **24h**, Low **48h**. Recorded as **PRD §16 Resolved Question 53** and propagated through the chain (Design v0.19, Prototype v0.20, AC v0.19, Test Plan v0.20, Test Cases v0.29, Matrix v2.9). **Matrix v2.9 reached the same verdict independently**, opening and closing **Gap 24** to record it. **Because business confirmed the shipped values, no code changed** — the outcome is that behaviour already in production now has authority behind it. **Two things the closure explicitly does NOT cover**, both named by the matrix rather than glossed: **"SLA per stage" is still open** (a different question — per-workflow-stage budgets, never supplied, never implemented), and the new `TC-MAINT-001-10` is **testable but unexecuted**, tracked as **Gap 23**. *History, superseded:* while open this read "business input required, and deliberately no code change proposed". **What this does NOT do: it does not invalidate `RAISE-FR-MAINT-001`'s full `PASS`.** That verdict rests on the confirmed 4-stage transitions (PRD §16 Resolved Question 33), and the AC deliberately excluded SLA as NOT TESTABLE YET — so **no executed test asserts an SLA value and no verdict rests on one.** The accurate classification is **unrequested scope carrying unauthorised business values**, not a failed requirement. **Three ways to close it, all business decisions and none of them mine to make:** (a) confirm the four values as they stand, recording them as a Resolved Question — the cheapest outcome, and the shipped behaviour is then correct as-is; (b) supply different values — a one-line change in each tier plus the labels; (c) decide MVP makes no SLA promise — the labels and `slaTargetHours` come out, which is the only option that touches behaviour users see. **Deliberately not done:** removing the labels or altering the numbers on my own initiative. Either would be inventing the business decision in the opposite direction, which is the same error the numbers already represent. Tracked alongside **F-03**, since both are the same question — *who is allowed to choose a number* — pointed at different halves of the codebase. |

## Known Limitations (by design, not a defect)

| ID | Area | Description | Source | Status |
|---|---|---|---|---|
| F-11 | Auth | Login is a single hardcoded demo credential; no real user store exists | `go-template-main/service/authService.go` | Accepted — real user store confirmed Roadmap, PRD §16 Resolved Q38 |
| F-12 | RBAC enforcement | `middleware.RequireRole` is only wired to the template's demo `/samples` routes, not any real RAISE domain | PRD §16 Resolved Q38 | Accepted — confirmed Roadmap, not MVP |

## Infrastructure / Process (not addressed anywhere in the PRD)

| ID | Area | Description | Source | Status |
|---|---|---|---|---|
| F-13 | Hosting | No deployment target or infrastructure decision exists | `RAISE-HIGH-LEVEL-ARCHITECTURE.md` §6 | Open — not a PRD-scope item. **Partial progress 2026-09-01**: `docker-compose.yml` + `frontend`/`go-template-main` Dockerfiles now make the app runnable as 3 local containers (`DOCKER.md`) — this makes local dev/demo reproducible, it does **not** decide a production hosting target (cloud provider, orchestration, IaC), which remains genuinely undecided |
| F-14 | CI/CD | No pipeline configured for either `frontend/` or `go-template-main/` | `RAISE-HIGH-LEVEL-ARCHITECTURE.md` §6 | **RESOLVED 2026-09-04 (R-20)** — see the Resolved table. `.github/workflows/ci.yml` now runs both stacks on every PR and every push to `main`. Verified on `main` itself, not just on the PR branch (run `33843149477`, event `push`, sha `103e069`, both jobs green, zero annotations). **Still open and deliberately unchanged: image build/push.** CI validates the source; it does not build or publish the Docker images, so the 2026-09-01 Docker work remains a manual local command and **F-13 (hosting) is untouched** |
| F-15 | API versioning | **Description corrected 2026-09-07 (F-48).** It previously read "`/api` vs `/api/v1` undecided", which implies the codebase has not picked one. **It has, consistently on both sides:** the backend serves `app.Group("/api")` (`router/sampleRouter.go`), the frontend defaults to `http://localhost:8080/api` (`services/api-client.ts`), and `RAISE-API-DB-SPEC.md` already records the base URL as **"unversioned"**. There is no client/server disagreement and no current risk. What is genuinely undecided is only whether to **introduce** versioning before production — a forward-looking decision, not an inconsistency | `COMPANY-FOUNDATION-BASELINE.md` §5.1 | Open |
| F-16 | DB migration tooling | `sql/pg/V*__*.sql` files are applied manually; no migration tool wired up | `RAISE-HIGH-LEVEL-ARCHITECTURE.md` §6 | Open |
| F-17 | NFR backlog | Performance, availability, scalability, backup/recovery, encryption, monitoring, logging targets all undefined | PRD §10 | Open — acknowledged at every layer of the chain, not silently omitted |

## Confirmed via Test Execution (not blocked on any PRD question)

Found running `TC-OPS-001-*`/`TC-AUDIT-001-*`/`TC-EXEC-001-*`/`TC-ASSET-001-*`/
`TC-ASSET-001-D-*`/`TC-ASSET-002-*`/`TC-ASSET-003-*` against the real app
on 2026-08-26, `TC-OPS-002-*`/`TC-MAINT-001-*` on 2026-08-28,
`TC-DASH-*`/`TC-ORACLE-001-*`/`TC-ALERT-001-*`/`TC-AI-SEARCH-001-*`/
`TC-AI-STATES-*` on 2026-08-29, and `TC-OPS-002-04` through `TC-OPS-002-09`
(the new IT Hardware Assignment Approval Workflow, backend/API-level) on
2026-09-02 against the real running Docker stack (see
`RAISE-TRACEABILITY-MATRIX.md` §3/§6 Gap 15 for the full evidence). Unlike
the "Blocking"/"Unresolved" sections above, these are not waiting on a
business decision — the AC/prototype already says what's required; the
implementation simply doesn't do it yet.

| ID | Area | Description | Source | Status |
|---|---|---|---|---|
| F-31 | Oracle FA / Financial View (P-011) not built — placeholder stub only | Prototype P-011 ("Financial View") and `AC-ORACLE-001-01..04` specify a screen with Asset Number/Acquisition Information/NBV/Depreciation/Oracle Source/Synchronization Status fields, plus 4 distinct states (available/unavailable/error/conflict). The route the app maps to `RAISE-FR-ORACLE-001` (`/reconciliation`, "Oracle FA Reconcile" in navigation) instead renders `frontend/src/pages/_shared/ModulePage.tsx` — a generic "foundation placeholder" `EmptyState` with no fields and no state logic at all ("Migrates from src/pages/Reconciliation.tsx once Oracle FA is connected in Phase 6."). The closest analog, Asset Detail's own "Financial" section (added for F-24), shows only Purchase Cost/Current Value/Purchase Date — no Oracle-specific field. This is distinct from **F-04** (the integration-mechanism question, PRD §16 Q6–Q10, still genuinely open) — even presence-only testing of the four UI states fails, independent of how the real integration would eventually work, the same pattern already established by F-22 for the Dashboard. **Business decision 2026-09-01: explicitly deferred** — do not build even a scoped placeholder-vs-real Financial View now; wait until the real Oracle FA integration lands (F-04 resolved) before touching this screen. Unlike F-22/F-27/F-30, there is no existing field/data to reuse for a "match reality" or "reuse existing field" fix — NBV/Depreciation/Oracle Source/Sync Status all depend on a real external integration that doesn't exist yet, so building anything now would either fabricate data or pre-empt the still-open F-04 formula question. | `frontend/src/pages/_shared/ModulePage.tsx`; `frontend/src/config/navigation.ts`; `docs/03-prototype/RAISE-PROTOTYPE.md` P-011; confirmed by browser test execution 2026-08-29 (TC-ORACLE-001-01..04) | Open — deferred by explicit business decision, not a "buildable now" item |
| F-33 | AI Assistant (P-015) doesn't answer natural-language questions or exhibit any response state — two non-matching surfaces exist instead | Prototype P-015 ("AI Assistant") and `AC-AI-SEARCH-001-01..03`/`AC-AI-STATES-01..05` specify a natural-language Q&A surface with 5 distinct response states (Success/No-data/Unable-to-answer/Source-unavailable/Data-conflict): submit a question, receive an answer with a "Sources / Data Used" section, and (for the PRD's illustrative "Which notebooks expire within 90 days?" example) a table of Asset/Warranty/Age/Maintenance/Status columns plus an affected-asset count. Two distinct "AI" surfaces exist in the built app, neither matching this: (1) the header "AI Assistant" drawer (`frontend/src/components/AppShell.tsx`) accepts no input at all — a static placeholder message only ("AI Assistant will connect to POST /api/v1/ai/chat once the AI module migration phase lands."); (2) the Assets page's "Ask AI" box (`frontend/src/pages/Assets/index.tsx`'s `handleAISearch`) is a hardcoded keyword-to-filter matcher ported from legacy ESAPS (predating this PRD/prototype) — submitting the exact PRD example question only interpreted it as `Type = Laptop` (keyword match on "notebook") and narrowed the existing Asset list; it never returns a natural-language answer, a Sources/Data Used section, or the required Asset/Warranty/Age/Maintenance/Status table. None of the 5 required `AC-AI-STATES` literal messages exist anywhere in `frontend/src` (confirmed by source grep) — submitting a deliberately nonsense query produced "AI interpreted: No specific filters detected — showing all assets," i.e. the keyword filter falls back to showing everything rather than exhibiting a "no match" state, since it has no concept of "no data found," "unable to answer," "source unavailable," or "conflicting sources." This is distinct from **F-06** (the citation-precision/format question, PRD §16 Q18, still genuinely open) — even presence-only testing of "an answer is returned" fails, the same pattern already established by F-22/F-31/F-32. Both `TS-AI-SEARCH-001` and `TS-AI-STATES` trace to the exact same two built surfaces and the same root cause (no real Q&A engine exists at all), so `TS-AI-STATES`'s result **broadens this finding** rather than opening a new one — the same precedent already established by F-22 (Dashboard, two Prototype screens/one page). **Business decision 2026-09-01: explicitly deferred** — do not build even a scoped canned-answer engine for the PRD's illustrative example question ("Which notebooks expire within 90 days?") now; wait until a real AI backend integration lands. Considered and rejected: a canned-answer implementation matching only that one illustrative query using existing Asset/Warranty/Type data was offered as an option, but the user chose to defer rather than build a narrow keyword-matched simulation of natural-language Q&A — a larger and more speculative undertaking than F-27/F-30/F-32's fixes (8 test cases across 5 response states, not a single reused field). | `frontend/src/components/AppShell.tsx`; `frontend/src/pages/Assets/index.tsx`; `docs/03-prototype/RAISE-PROTOTYPE.md` P-015; confirmed by browser test execution 2026-08-29 (TC-AI-SEARCH-001-01..03, TC-AI-STATES-01..05) | Open — deferred by explicit business decision, not a "buildable now" item |

## Minor / Tech Debt

| ID | Area | Description | Source | Status |
|---|---|---|---|---|
| F-56 | Asset Detail / employee link | **The assignee link on Asset Detail falls back to the same hardcoded fixture id `'e1'` as F-55, in a second location F-55's own investigation did not cover.** `frontend/src/pages/AssetDetail/index.tsx:313` (found while reviewing this session's work, code unmodified this session until this fix): the assignee button's `onClick` navigates to `/employees/` plus `assignedEmployeeId`, falling back to the literal `'e1'` when `assignedEmployeeId` is missing — gated only on `assignedTo` being truthy, **not** on `assignedEmployeeId`. **Reachable today with real seeded data, not merely hypothetical:** fixture `a9` (`AST-0009`, Epson PowerLite Projector) has `assignedTo: 'Conference Room A'` and `assignedEmployeeId: null` (`mockData.ts:271`) — a room-style assignee with a display name but no employee record behind it. Clicking that asset's assignee link falls back to `'e1'` and navigates to `/employees/e1` — **Sarah Chen's real profile**, not Conference Room A's. **Wrong output today, not merely a real-API-mode risk** (unlike F-55): the mock repository, which this whole test suite runs against, already has a real employee at id `'e1'`, so the fallback silently substitutes a plausible-looking but wrong person rather than erroring. In real-API mode the same fallback would 404 or resolve to whatever unrelated record Postgres happens to have at that id. **Unlike F-55, this needs no business decision:** there is no "who is the requester" question to answer — the fix is only "do not guess an id," so `assignedTo`'s own business meaning (a display label, not a promise of a resolvable employee record) is unchanged. | Found 2026-09-09 during a code-review pass over this session's work (`/code-review`, medium effort), by an angle searching the frontend for other instances of F-55's hardcoded-fixture-id fallback pattern; verified against `mockData.ts`'s fixture data rather than assumed. Not found by any test — no existing test in `AssetDetail/index.test.tsx` exercises the assignee link for an asset with `assignedTo` set and `assignedEmployeeId` null. | **RESOLVED 2026-09-10 (`c3bac79`).** PR #125 squash-merged to `main` (https://github.com/boonthepkstl-alt/stl_asset_service/pull/125), CI green on both jobs before and after merge. Fix: the assignee control now renders as a clickable link only when `assignedEmployeeId` is present; when `assignedTo` is set but `assignedEmployeeId` is not, it renders the same label as inert text instead of a link — `assignedTo` is still shown, nothing about its display or business meaning changes, only the guessed navigation target is removed. New test in `AssetDetail/index.test.tsx` covers asset `a9` and asserts no link/button targets `/employees/e1` while the label text is still present, and a companion case confirms an asset with a real `assignedEmployeeId` still links correctly. Verified on `main` after the merge, not assumed from CI alone: `tsc` 0, ESLint 0, **54 files / 288 tests** (was 286→288 across this and the companion PR). |
| F-18 | Frontend bundle size | Production bundle over the 500 kB chunk-size warning threshold, as a **single** chunk containing all 27 routes | `npm run build` output | **RESOLVED 2026-09-04 (R-21)** — see the Resolved table. Route-level code splitting via `React.lazy`; entry chunk **694 KiB → 305 KiB (−56%)**, 1 chunk → 80, Vite's warning gone. **Performance targets themselves remain undefined (F-17)**, so this closes a self-identified build warning, not a stated NFR |
| F-42 | `TC-ALERT-001-09` specifies a procedure the product cannot perform | The test case's step 2 reads *"Edit that Asset's `warrantyExpiry` to a future date"*, but **RAISE has no asset-edit capability at all**. Verified in source on `main` `c2e6b76`: `frontend/src/services/asset-repository.ts` exposes only `create`, `assign` and `checkIn` — there is no `updateAsset` method, no update endpoint consumed, and no edit-asset UI anywhere under `frontend/src/pages/`. An asset's `warrantyExpiry` can be set at creation and never changed afterwards. **Classification matters here: this is a TEST-CASE defect, not an implementation defect and not a specification defect.** `AC-ALERT-001-09` (an alert disappears once its condition no longer holds) is sound, and the implementation satisfies it — demonstrated during the same execution by lowering the IT Hardware "Expiring" threshold 90 → 3 through the real Settings UI, which removed the Warranty Expiring row and took the alert total from **19 → 18**, restoring to 19 on reset. That evidence was recorded as *supporting* and explicitly **not** substituted for the specified procedure, so the case was left **BLOCKED rather than marked PASS**. | Formal execution of `TC-ALERT-001-03..10`, 2026-09-04, against merged `main` `c2e6b76`; `CHECKPOINT-2026-09-04-006`; tracked as **Gap 18** in `RAISE-TRACEABILITY-MATRIX.md` v2.0 | **RESOLVED 2026-09-04 (R-24)** — see the Resolved table. The procedure was corrected first (Test Cases v0.19) and left deliberately **unexecuted**, then executed (v0.20) and **PASSED**; that ordering is the evidence it was not tailored to pass. **Gap 16 and Gap 18 are both CLOSED** in matrix v2.1. Correcting it needed no business decision — only a deliberate edit to `RAISE-TEST-CASES.md` re-pointing step 2 at a state change the product actually supports (completing a maintenance ticket to `DONE`, which clears that ticket's Overdue/On Hold rows). **History, superseded and kept for the record:** while open, this was the single item holding Gap 16, since that gap's discipline is that no gap closes while any case in its scope is unexecuted; it was deliberately not rewritten during the execution pass itself, because editing a test's steps while executing it would destroy the evidence that the procedure as written was unrunnable. **This cell's own wording was a defect (F-50)** — it opened with `Open` and disclosed the resolution 656 characters later. |
| F-41 | Some **4xx** response bodies also carry raw driver errors, not just sentinel text | Discovered live while validating F-19's fix on 2026-09-04, and it **partly contradicts the assumption F-19's scoping rested on**. F-19 was scoped to 5xx on the stated basis that 4xx bodies carry *sentinel* errors from the service layer whose text is meaningful validation feedback (`ErrAssetNotITHardware`, `ErrHandoverWrongStage`, ...). That holds for many sites — but not all. Confirmed against the running stack: `GET /api/assets/does-not-exist` returns **`{"error":"sql: no rows in result set","message":"Asset not found"}`** with HTTP 404 — a raw `database/sql` error, not a sentinel. The `message` field already says everything the caller needs; the `error` field adds only internal detail. | Live verification against the Docker stack during PR #93's validation, 2026-09-04; `controller/assetController.go` and the other 4xx sites listed by the F-19 audit | **Resolved 2026-09-05 (R-26)** — audited all 28 sites, then fixed 7 of them. The audit found the finding's own two-way split (sentinels vs raw driver errors) was missing a third, largest group: **17 request-parse sites** whose `err.Error()` is Go's json/schema decoder text, neither a sentinel nor a driver error. Those are **deliberately left in place** and tracked as **F-43** — sweeping them is exactly the blanket removal this finding warned against. Also found: the leak was **worse than this finding recorded**. `GetPGReadDB` returns a *connection* error when the pool cannot be built, which the not-found handlers passed through, so a database outage answered **404** with the driver's dial text, host and port included — the same class of detail F-19 removed from 5xx, reached through a status F-19's guard did not watch. The "lower severity than F-19" wording here was therefore only true of the one case that had been observed. |
| F-40 | Flaky navigate-away assertions in page tests | Tests that submit a form, wait for the success toast, then assert **synchronously** that the form has unmounted. The toast and the route change commit independently, so on a differently-scheduled machine the toast renders before the unmount flushes and the assertion fails on correct code. **Found three times, in three separate ways, which is the point of recording it as a pattern rather than three incidents:** first by PR #87's code review in `EditEmployee` (fixed there), then by CI on [PR #89](https://github.com/boonthepkstl-alt/stl_asset_service/pull/89)'s fourth run in `CreateEmployee/index.test.tsx:70` (`expect(element).not.toBeInTheDocument()` — `found <input id="name" value="Test Employee" />`), and finally in `CreateAsset/index.test.tsx:81` by grepping for the same shape after CI caught the second. All three passed every local run; the CI one had also passed the three CI runs before it. | CI run on [PR #89](https://github.com/boonthepkstl-alt/stl_asset_service/pull/89), 2026-09-04; `CHECKPOINT-2026-09-04-002` | **Resolved for all three known sites** (`EditEmployee` in PR #87, `CreateEmployee` + `CreateAsset` in PR #89 — each now wraps the removal assertion in `waitFor`). Kept open as a **pattern to watch, not an outstanding defect**: nothing prevents the shape being reintroduced, and there is no lint rule for it. Any new test that asserts an unmount off the back of a toast should wrap it in `waitFor`. Grep for `not.toBeInTheDocument()` not preceded by `await waitFor` when adding page tests |
| F-19 | Backend error responses | Several `500` paths include the raw underlying Go error string in the JSON response | `RAISE-DETAILED-DESIGN.md` §7 | **RESOLVED 2026-09-04 (R-22)** — see the Resolved table. All **18** 5xx sites across 7 controllers now return the human-readable `message` only; **0 remain** (verified on `main`). Guarded against reintroduction by a source-level invariant test. **The 4xx half is a separate, still-open issue — see F-41**, which this fix's own validation surfaced |
| F-43 | Request-parse 4xx bodies echo Go decoder text, and one 401 reports a server-side failure | The 21 `err.Error()` sites remaining in 4xx/401 bodies after **F-41**'s fix split two ways. **(a) 16 request-parse sites** (`c.QueryParser`/`c.BodyParser`, all 400) return text like `json: cannot unmarshal string into Go struct field CreateAssetRequest.purchaseCost of type float64` — internal Go struct and field names, but also genuinely useful client feedback, and *not* infrastructure detail. Removing them is a usability-vs-disclosure trade that F-41 did not decide and this AI should not decide alone. **(b) `controller/authController.go:60`** puts `err.Error()` in the **`message`** field of a **401**. Two of its three sources are legitimate sentinels (`"invalid credentials"`, `"username and password are required"`), but the third is a `util.GenerateToken` signing failure — a *server-side* error reported as an authentication failure. That is a status-code defect as much as a disclosure one, and fixing it changes login response text the frontend may rely on. | Per-site audit of all 28 sites during F-41's fix, 2026-09-05; `controller/*.go`, `service/authService.go:51`, `util/jwtUtils.go:50` | **Open — but the (b) half is RESOLVED 2026-09-07 (R-32); only the (a) parse-error half remains.** **(b) `controller/authController.go` is fixed.** `authService.Login` now returns three exported sentinels instead of three bare `errors.New` values, and the controller tells them apart: the two genuine authentication cases keep **401** with their exact original wording (the only part a caller legitimately reads), while a `util.GenerateToken` signing failure now answers **500** with a fixed message and the detail in the log only. That closes both halves of the defect at once — the wrong status (a server fault reported as a rejected password) and the disclosure. **(a) The 15 request-parse sites are untouched and still open**: their `err.Error()` is Go's json/schema decoder text, which is internal struct and field naming but also genuinely useful client feedback and *not* infrastructure detail. Removing it is a usability-versus-disclosure trade nobody has decided, and F-41 explicitly warned that a blanket sweep is the wrong shape of fix. |
| F-44 | Local parallel `vitest run` on Windows silently **under-collects** test files | Observed repeatedly on 2026-09-05 while validating F-41: `npx vitest run` reported **47/219**, then **49/240**, then **49/244** on consecutive runs of an unchanged tree, each ending with `Vitest caught 1 unhandled error` — `EBUSY: resource busy or locked` on a file under vitest's own temp cache. Root cause: Vitest 2's default pool is **`forks`**, and a forked child cannot share the parent's module cache, so it round-trips every transformed module through a file under `os.tmpdir()` (`cacheFs: true`, set only on the child-process channel — `vitest/dist/chunks/resolveConfig.*.js`). On Windows two workers race that write and the loser gets EBUSY, which drops whole test **files** from collection. **Correction to this finding as first written:** it claimed the run "still exits 0". That was never measured — the original run was piped through `grep`, so `$?` reported grep's status, not vitest's. Measured properly, a failing run **exits 1** and prints `Unhandled Errors`. The hazard is therefore narrower than first recorded: the run fails loudly, but a reader taking the `Tests N passed` summary line at face value can still record a short count. | Observed directly while running the suite during F-41's validation, 2026-09-05; Windows only — the Linux CI job has reported 50/257 consistently | **Resolved 2026-09-05 (R-27)** — `pool: 'threads'` set in `frontend/vitest.config.ts`. `threads` shares memory and never touches that temp path. Measured before committing: threads **50/257 on 3 of 3** runs; forks **50/257 on only 3 of 4**. Both ~12s, so this costs no speed — unlike `--no-file-parallelism`, which is deterministic but takes **160s**. |
| F-45 | Two test cases tell the tester to log in as a role that does not exist | `TC-EXEC-001-01` and `TC-EXEC-001-03a` open with *"Log in as Executive"*. **There is no Executive role in the application** — `UserRole` (`frontend/src/types/auth.ts`) has exactly four values: `ADMIN`, `IT_MANAGER`, `IT_STAFF`, `EMPLOYEE`. "Executive" is a PRD **persona** (`RAISE-FR-EXEC-001`'s User/Actor field), not a system role. | Found while executing the Gap 19 sweep, 2026-09-05; `RAISE-TEST-CASES.md` v0.23 §16 records the deviation | **Resolved 2026-09-07 (R-29).** Both step 1s now read "Log in as any authenticated user", with a note at each that the dashboard route carries no role restriction (PRD §16 Resolved Question 45), so the change narrows nothing. Fixed in `RAISE-TEST-CASES.md` v0.25 — **wording only; both cases keep their recorded PASS and their 2026-09-05 execution history untouched**, since the defect was in how the step was written, never in what was verified. |
| F-46 | `AC-ALERT-001-12` specified a numeric badge on the closed header bell that the product does not render | The criterion said the **header** displays the numeric alert total. It does not: the closed bell button renders only a **presence dot** — its own rendered text is empty, and its single child element is a class-only span with no text. The numeral lives in the button's `aria-label` ("Notifications, 19 alerts") and in the dropdown panel header badge ("19"). `TC-ALERT-001-12` inherited the error and told the tester to read a numeral "without opening the bell dropdown", which is not possible. | Found while executing the Gap 20 sweep, 2026-09-07, against merged `main` `6a6bcac`; `RAISE-ACCEPTANCE-CRITERIA.md` v0.15 §15, `RAISE-TEST-CASES.md` v0.24 §14 | **Resolved 2026-09-07 (R-30) — raised and closed in the same revision.** **This was an AI-introduced specification error, not a business decision and not a product defect**, and it is recorded as such rather than blamed on the product: the criterion was drafted during the 2026-09-05 chain sync from an imprecise description ("the bell badge shows the TOTAL alert count") that was true of the *panel* badge and was mis-read as the *header* badge. **PRD §16 Resolved Question 49 never specified a numeral on the closed button**, and the pre-existing bell already used a dot — so the product matched the business decision as built, and the specification over-reached beyond it. |
| F-47 | `RAISE-COMPLIANCE-REVIEW.md` had fallen twelve matrix revisions behind and was **overstating** one requirement | The chain's terminal deliverable — the document that answers "is this MVP compliant?" — sat at v1.0, consolidating Traceability Matrix **v1.4**, while the matrix had reached **v2.6**. Two verdicts had gone stale in opposite directions, and the dangerous one was upward: it reported `RAISE-FR-EXEC-001` as a full, unqualified `PASS` when the matrix records `PASS (partial)`. Its §5 also listed four findings as open that had been resolved (F-02, F-05, F-18, F-19), and its §7 claimed "Gaps 1–13 resolved" when the matrix records Gaps 1–20 resolved with none open. **The root cause is a process gap, not an oversight in any one PR:** v1.0 itself said "no action is required to finish this review", so nothing in the close-out protocol ever pointed back at it, and every subsequent close-out updated the matrix without updating the document that consumes it. | Found by auditing "what is actually still outstanding" on 2026-09-07, after the matrix reached zero open gaps | **Resolved 2026-09-07 (R-31) — raised and closed in the same revision.** Re-verified to **v1.1** against matrix v2.6: `RAISE-FR-EXEC-001` corrected **down** to `PASS (partial)`, `RAISE-FR-ALERT-001` upgraded to a full `PASS`, §4/§5/§7 corrected, and a §1a Revision Note added recording what moved and why. The process gap is closed too: the document's Next Action now reads "re-verify in the same pass that closes a gap or resolves a finding, not when someone happens to check", and `CLAUDE.md` — which still said the file did not exist — now lists it with that warning. |
| F-48 | The findings register misclassified two findings as blocking, and misdescribed a third | A third audit of the claim *"everything left is a business decision"* found no hidden buildable work — but did find three bookkeeping errors that distort triage. **(1) F-09** (asset master field list) sat under **"Blocking (gates an MVP requirement)"** while appearing **zero** times in `RAISE-TRACEABILITY-MATRIX.md`; the Compliance Review's `RAISE-FR-ASSET-001` row states outright that it *"does not block this row's current PASS"*. **(2) F-35** (asset code scheme) sat in the same section with zero mentions in either the matrix or the Compliance Review; the information is genuinely outstanding, but `RAISE-FR-ASSET-001` is a full `PASS` on the current `AST-####` scheme, so it gates a future enhancement. **(3) F-15** read "`/api` vs `/api/v1` undecided", implying the code had not picked one — both sides use `/api` and the API-DB spec already records it as "unversioned". | Third audit of the parked-work claim, 2026-09-07, cross-checking every "Blocking" entry against the matrix and the Compliance Review | **Resolved 2026-09-07 (R-33) — raised and closed in the same revision.** F-09 and F-35 moved to "Unresolved (scope question, not yet blocking a build)" with the reason recorded in place; F-15's description corrected. **Nothing was closed or downgraded in substance** — all three remain open, and none became buildable. The correction is to how they are filed, which is what a reader triages from. |
| F-49 | `ci.yml` and `R-20` both recorded a false reason for having no `gofmt` gate | Both said the repository commits Go sources with CRLF, so `gofmt -l` would flag every file on a Linux runner. **The repository commits LF.** Verified by extracting all 64 Go files with `git show :<path>` — the content a runner actually checks out — finding **zero** CRs, and running `gofmt -l` over that content, which flagged **nothing**. The CRLF is `core.autocrlf=true` translating on a Windows working tree, which no runner sees. **The precise root cause is worth keeping:** the underlying observation was real — `gofmt -l` genuinely does flag 42 files locally, and R-20 even says it was confirmed with `gofmt -d` — but the check ran against the local working tree and the conclusion was stated about CI. A true measurement, an untested inference. | Found while starting the `.gitattributes` task on 2026-09-07, by checking the index rather than the working tree | **Resolved 2026-09-07 (R-34) — raised and closed in the same revision.** The gate is added, `ci.yml`'s comment is replaced with the measured facts rather than deleted, R-20's parenthetical is corrected in place, and `.gitattributes` pins `*.go text eol=lf` so a contributor with `core.autocrlf=false` cannot commit CRLF and turn the new gate red for a reason unrelated to code quality. **This is the third finding in two days whose blocker was a premise written down without being checked** (after F-43(b) and F-47), and the second of those written by this AI. **Correction made while this was in review:** the working note claimed gofmt over the repository content "flagged nothing", i.e. that CI had always been clean. **The evidence did not support that.** It showed the 42 locally-flagged files and the 42 locally-CRLF files were the same 42 — which proves the *sets* match, not that CRLF was the only reason each file was flagged. **On its very first run the new gate failed on `controller/internalErrorLeak_test.go`**: a genuine missing blank line before the F-41 guard, introduced in PR #103, that the CRLF noise had been masking. Fixed in the same change, and the whole module re-verified clean over LF content (64 files, 0 flagged). **The gate justified itself on its first run**, which is a better outcome than the claim it replaced. |
| F-38 | Employee audit trail is a mock-mode fixture shim, and Employee Detail aliases it as state | Employee audit entries have no backend. `pages/EditEmployee/index.tsx` records a save's field changes by **mutating the module-level `employeeAuditLogs` fixture array in place** (`fixtureAudit.unshift(...)`), and `pages/EmployeeDetail/index.tsx:71` holds that **same array reference** as component state (`useState(fixtureAudit)`). Two consequences. (1) **Latent staleness:** `employeeAudit` is a `useMemo` keyed on `[auditLogs, employee]`, and the array's *identity* never changes when its *contents* are mutated — so the memo can never recompute from a write. This is masked today only because writes happen while the detail page is unmounted; any future code that appends while it is mounted would leave the Audit tab and its badge count silently stale until a remount. (2) **Scope:** the write is gated to `!EMPLOYEE_API_ENABLED` (added in [PR #87](https://github.com/boonthepkstl-alt/stl_asset_service/pull/87)'s review pass) precisely because with a real backend the server owns the audit trail — so this shim is demo scaffolding, not an implementation of `RAISE-FR-AUDIT-001`. Found by the code review on PR #87 and **deliberately not fixed there**: patching the aliasing would harden scaffolding that a real audit endpoint should delete outright | Code review of [PR #87](https://github.com/boonthepkstl-alt/stl_asset_service/pull/87), 2026-09-04 (`CHECKPOINT-2026-09-04-001`); `frontend/src/pages/EmployeeDetail/index.tsx:71`; `frontend/src/pages/EditEmployee/index.tsx` | **Half resolved 2026-09-07 (R-35); the backend half stays open.** The **aliasing and stale-derivation defect is fixed**: `EmployeeDetail` no longer holds the shared fixture in `useState` and no longer memoises the derived rows — it filters the live array on every render, so an append is reflected on the next render for any reason. **Two smaller fixes were tried first and both were wrong, recorded in the code so they are not retried:** keying the memo on `fixtureAudit.length` (rejected by `react-hooks/exhaustive-deps`, correctly — a module array cannot be a reactive input), and reading the live array while keeping the memo (**purely cosmetic** — the memo still caches after mount, so a later append stays invisible). **Still open:** the Employee audit trail has no backend at all, and `EditEmployee` still writes by mutating a module fixture. Not fixable within scope — **no `RAISE-FR-EMP-*` requirement exists in the PRD** to trace an Employee audit source to, so building one would be unrequirement-backed. Still mock-mode behaviour, now honest about it. |
| F-50 | The findings register's own F-42 row read as open long after it closed | The status cell **opened with** *"Open — and it is the single item keeping Gap 16 open"* and disclosed **RESOLVED 2026-09-04 (R-24)** only at character **656**, with no separator between the two. Every claim in that lead was false by then: R-24 is in the Resolved table, and `RAISE-TRACEABILITY-MATRIX.md` records **Gap 16 and Gap 18 both CLOSED** as of v2.1 (2026-09-04). **The evidence that it actually misleads is direct:** an enumeration script written during the 2026-09-07 assessment read the row's lead and classified F-42 as OPEN. A scan of all 34 `F-` rows found **this row alone** with that shape — F-03, F-08 and F-43 also open with `Open`, correctly, because they are still open. | Found during the next-step assessment on 2026-09-07, by a script this AI wrote misreading the row it was counting | **Resolved 2026-09-07 (R-37) — raised and closed in the same revision.** Cell rewritten to lead with the resolution and demote the pre-2026-09-04 reasoning to a labelled history clause; **nothing deleted**, per this file's own maintenance rule. **Blast radius verified as contained to this file** — `CURRENT-STATUS.md`, `NEXT-STEP.md` and `RAISE-COMPLIANCE-REVIEW.md` were checked and none of them describes F-42 as open. Sibling of **F-48**, the same class of defect in the same register: a wrong status, not a wrong fact about the product. |
| F-51 | Three in-code comments still state PRD §16 Q3/Q4 as open questions | `frontend/src/lib/utilization.ts`, `frontend/src/services/dashboard-repository.ts` and `go-template-main/model/dashboardModel.go` each fence themselves off from NBV and Risk on the stated grounds that *"PRD §16 Q3/Q4 remain open"*. **That reason is stale, though not because both questions closed** — a distinction the first version of this row got wrong, and which is corrected here against the PRD's own words. **Q3's formula half is resolved:** Resolved Question 46 confirmed the NBV formula, configuration shape, salvage value and clamp (implemented in `frontend/src/lib/nbv.ts`, R-36); what remains is **Q3a**, the per-category useful-life defaults, which *is* F-03. **Q4 reads `**Still open**` verbatim** in PRD §16 — what Resolved Question 47 changed is that it *no longer bears on the Executive Dashboard*, which carries no Risk KPI tile for MVP, so Q4 belongs entirely to `RAISE-AI-RISK-001` (Pilot/Roadmap). **The fencing decisions themselves stay correct** — neither module should compute NBV or Risk — so this is a stale rationale on a sound boundary, not a code defect. It matters because a reader checking whether NBV is answerable would find three separate places in the source saying it is not. | Found during the next-step assessment on 2026-09-07, grepping the source for Q3/Q4 references | **Resolved 2026-09-07 (R-38) — raised and closed in the same revision.** All three comments now cite RQ46 and RQ47 by name, state the real reason each module excludes the figure (NBV lives in `lib/nbv.ts` and is still gated on F-03's per-category defaults; Risk carries no MVP dashboard tile), and **say plainly that Q3a and Q4 do remain open** rather than implying the questions closed. **Correction made while PR #119 was in review, before merge:** the first version of this fix wrote *"neither is an open question any more"* and *"stale on both halves"* in all three comments and in this row. **Checked against `RAISE-PRD.md` §16 and both were wrong** — Q4 is marked `Still open` there and Q3a is open too. What was actually stale is the *reason*, not the questions. **No executable line changed**, so the fence each comment describes is byte-identical. |

## Correctly Fenced (not a problem — listed for completeness)

- License Management, AI Decision Center, Risk Scoring, and Lifecycle
  Prediction all remain behind feature flags with no backend built.
  Confirmed Roadmap-only; not creeping into MVP scope.

---

## Resolved

| ID | Area | Description | Resolved by |
|---|---|---|---|
| R-01 | Login contract | Backend response envelope (snake_case, cookie-only token) didn't match frontend's expected bare camelCase object with token in body | [PR #10](https://github.com/boonthepkstl-alt/stl_asset_service/pull/10) |
| R-02 | Traceability drift | Matrix claimed to be current against a PRD version it had not re-verified (Gap 6) | [PR #12](https://github.com/boonthepkstl-alt/stl_asset_service/pull/12) |
| R-03 | react-template-main build failure | Unused imports broke the build | [PR #6](https://github.com/boonthepkstl-alt/stl_asset_service/pull/6) |
| R-04 | Checkpoint coverage | PRs #19-28 had `DEVELOPMENT-LOG.md` rows but no Level 1 Task Checkpoint entries in `PROJECT-CHECKPOINTS.md` — checkpointing had lapsed after `CHECKPOINT-2026-08-24-009` (PR #18) until `CHECKPOINT-2026-08-25-001` (PR #29) | Backfilled as `CHECKPOINT-2026-08-24-010` through `-019`, reconstructed from real `gh pr view` metadata, not memory — [PR #36](https://github.com/boonthepkstl-alt/stl_asset_service/pull/36) |
| R-05 | QR/Barcode invalid-code state | `AC-OPS-001-03` requires a distinct "invalid code" state for a malformed/unreadable scan, separate from "not found" (`AC-OPS-001-02`) — the Scan QR modal showed the identical generic "No asset found for ..." message for both | Added a plausible-code-format check (`isPlausibleCodeFormat`, alphanumeric + `-`/`_`) before attempting a lookup, showing a distinct "Invalid code..." message when it fails; re-executed `TC-OPS-001-03` and confirmed the distinct message renders |
| R-06 | Asset Registry — no Category filter | `TC-ASSET-001-03` required a Category filter that narrows the asset list; the Filters panel had Status/Department/Location selects only | Added a Category `Select` (sourced from a new `categories` fixture export, same pattern as `departments`/`locations`), wired into `MockAssetRepository.list()`'s filter predicate (not forwarded to `HttpAssetRepository` — go-template-main's `GET /assets` has no category query param, so it would silently no-op against the real backend); re-executed `TC-ASSET-001-03` via browser and confirmed the list narrows (15 → 2 on "Infrastructure") and "Clear filters" resets it |
| R-07 | Asset Detail — missing Financial and Lifecycle sections | `TC-ASSET-001-D-01` required 9 named sections on Asset Detail (Basic Info, Category, Custody, Financial, Warranty, Maintenance, QR/Barcode, Lifecycle, Audit/History); "Financial" and "Lifecycle" were entirely absent | Added a "Financial" section (Purchase Cost/Current Value/Purchase Date — fields the `Asset` record already carries, same ones the Assets list already shows) and a "Lifecycle" section (a connectivity summary linking to the Custody/Warranty/Maintenance/Audit tabs, matching Prototype P-004's "one asset → connected information across its lifecycle" principle and `AC-LIFE-001-01` — no new lifecycle-stage data model was invented); re-executed `TC-ASSET-001-D-01` via browser on asset `a1` and confirmed both sections render with real data, and that clicking a Lifecycle row jumps to the corresponding tab |
| R-08 | Custody/Assignment History is not append-only | `AC-ASSET-003-02` required a custody-changing event (Check-in/Check-out) to append a new history entry while leaving prior entries unchanged; the "Assignment History" panel instead derived a single "current custody state" row, replaced on every change | Rewired the History tab to render from the same per-asset audit trail `RAISE-FR-AUDIT-001` already builds (`recordMockAuditEntry`, `audit-repository.ts`) — append-only by construction (only ever `unshift`s, never mutates/removes), and `assign`/`checkIn` already fed it. No new custody-log data model was invented. Re-verified live on asset `a1`: performed a real Check-in (appended "Asset checked in") then a real Assign (appended "Asset assigned to Sarah Chen" alongside it, not replacing it) — both entries visible, newest-first |
| R-09 | No Category & Hierarchy screen (P-005) | `RAISE-FR-ASSET-002`'s dedicated screen didn't exist anywhere in the app's routing or navigation — not just its taxonomy content | Added a scoped-down first cut: each category expands to show the real assets registered under it — the one parent/child relationship actually confirmed anywhere in the chain. Initially shipped as a standalone `/categories` page with its own sidebar nav entry ([PR #43](https://github.com/boonthepkstl-alt/stl_asset_service/pull/43)); per user request, folded into Asset Management as a "By Category" view alongside the existing asset list ([PR #44](https://github.com/boonthepkstl-alt/stl_asset_service/pull/44)) — same content and behavior, now at `frontend/src/pages/Assets/index.tsx`, no separate route or nav entry. Does **not** implement Prototype P-005's illustrative sub-category tree (tracked separately as **F-27**, still open/TBD). Verified live: "IT Hardware" expands to 6 real seeded assets; clicking one navigates to its Asset Detail |
| R-10 | Maintenance record list — missing date and cost fields | `AC-MAINT-001-01` requires maintenance records to display "date, event, status, and cost"; Asset Detail's "Maintenance & Tickets" tab showed only ticket code, priority, workflow-stage badge, and title — no created date and no cost field rendered anywhere | Added created date and a cost field per record — preferring `itExecution.actualCost` once known, falling back to `itAssignment.estimatedCost`, or an honest "—" for a ticket not yet dispatched (no fabricated value). Verified live on asset `a1`: `REQ-2026-0042` (dispatched, in progress) shows "2026-08-15 09:30 AM · Cost: $120"; `REQ-2026-0044` (not yet dispatched) shows "Cost: —" |
| R-11 | Maintenance stage-progress indicator doesn't distinguish Current from Pending | `AC-MAINT-001-09` requires the 4-stage progress indicator to show Done/Current/Pending as 3 distinct states; `GovernanceStep` (`TicketDetail/index.tsx`) only rendered 2 (done checkmark vs. a plain gray circle for everything else) | Derived the current stage from `ticket.status` (already fully determines it — no new field invented) and added a 3rd visual state to `GovernanceStep`: a brand-colored circle with a ring, a highlighted card background, and a "Current" badge. Verified live across `PENDING_DEPT_APPROVAL` (stage 2 current), `PENDING_IT_DISPATCH` (stage 3 current), and `DONE` (no stage marked current, all done) |
| R-12 | Warranty field list undefined | `RAISE-FR-WARRANTY-001`'s field list beyond `warrantyExpiry` was undefined (PRD §16 Q15) — the standing uncompleted business-decision request in this session | User confirmed 2026-08-29: for MVP, `warrantyExpiry` is the only Warranty field; a draft 8-field proposal (start date, provider/vendor, type, coverage details, cost, claim contact, document reference) was explicitly rejected, not deferred. Recorded as PRD §16 Resolved Question 40 and propagated through the full deliverable chain (Design §5.2, Prototype P-010, Acceptance Criteria AC-WARRANTY-001, Test Plan TS-WARRANTY-001, Test Cases TC-WARRANTY-001-01/-02, Traceability Matrix Gap 7) via `/update-prd` + `/run-full-chain`. Same day, per explicit user direction ("ไม่ควรสร้างหน้า แต่เพิ่มในหน้าอุปกรณ์ที่เกี่ยวข้องพอ" — don't build a page, add it to the relevant asset page instead), implemented as a new "Warranty" column on the Assets Registry list (`frontend/src/pages/Assets/index.tsx`) showing `warrantyExpiry` + an Active/Expired badge, sortable by date — no standalone P-010 screen built. Verified live: asset `a1` shows "Active"/"2027-01-15", asset `a13` shows "Expired"/"2024-03-15". `AC-WARRANTY-001-03`'s separate 90-day-window threshold (and the "Expiring" 3rd state it would gate) remains open, unaffected |
| R-13 | Executive Dashboard vs. Prototype P-014 (and Main Dashboard vs. P-002 — same page, same gap) | Prototype P-014/P-002 specified an identical, never-built tile/section spec (NBV/Risk/Utilization tiles; "Asset by Category"/"Lifecycle / Maintenance Overview"/"Recent Alerts" sections) that didn't match the actually shipped dashboard (`frontend/src/pages/Dashboard/index.tsx`) at all. Business confirmed 2026-08-31: "แก้ Prototype ให้ตรงกับของจริง" — document the shipped app rather than change the code to match the old spec. Propagated via `/run-full-chain`: `RAISE-DESIGN.md` §13 (v0.9) replaced the wireframe with "Logical Dashboard — Current MVP (As Built)"; `RAISE-PROTOTYPE.md` P-002/P-014 (v0.8) rewritten to the as-built list, cross-referencing each other as the same page; `RAISE-ACCEPTANCE-CRITERIA.md` AC-DASH/AC-EXEC-001 (v0.7) rewritten Testable, new `AC-DASH-03` documents NBV/Risk/Utilization absence as NOT TESTABLE YET (F-03); `RAISE-TEST-PLAN.md`/`RAISE-TEST-CASES.md` (v0.7) corrected to match. Re-executed 2026-08-31 against the real app: `TC-DASH-01/-02`/`TC-EXEC-001-01/-02` **PASS** (all 8 KPI tiles and all 10 sections confirmed present via real page text); `TC-DASH-03` **PASS** on the absence-check itself. `RAISE-TRACEABILITY-MATRIX.md` (v0.8) updated to PASS/PASS (partial), Gap 8 closed. NBV/Risk/Utilization remain a documented, not-yet-scheduled enhancement (F-03), not deleted and not resolved by this fix. | [PR #56](https://github.com/boonthepkstl-alt/stl_asset_service/pull/56) (chain correction) + [PR #57](https://github.com/boonthepkstl-alt/stl_asset_service/pull/57) (re-execution) |
| R-14 | Category & Hierarchy sub-taxonomy undefined | Prototype P-005's Computer/Network example tree was explicitly illustrative, not finalized business data — the actual sub-category taxonomy was undefined. Business confirmed 2026-09-01: the existing Asset `type` field IS the sub-category (no new field/data model); the hierarchy is exactly 2 levels — Category → Type → individual assets — using the real, currently-seeded breakdown (IT Hardware → Laptop/Monitor/Headphones; Mobile → Smartphone/Tablet; Office Equipment → Printer/Projector; Infrastructure → Server/Router; Media Equipment → Camera). Propagated via `/run-full-chain`: `RAISE-PROTOTYPE.md` P-005 (v0.9) rewritten with the real Category→Type tree; `RAISE-ACCEPTANCE-CRITERIA.md` AC-ASSET-002 (v0.8) resolved its NOT TESTABLE YET note, added new `AC-ASSET-002-03`; `RAISE-TEST-PLAN.md`/`RAISE-TEST-CASES.md` (v0.8/v0.9) corrected to match. Implemented same day: the "By Category" view (`frontend/src/pages/Assets/index.tsx`) extended one level deeper (Category → Type → assets), with a shared `toggleTypeExpanded` state mirroring the existing category-expand pattern. Re-executed against the real app: expanding "IT Hardware" reveals Laptop/Monitor/Headphones sub-groups; expanding "Laptop" reveals exactly its 3 assets, none of Monitor's/Headphones'. `TC-ASSET-002-01..03` all **PASS**; 3 automated tests added/updated, full suite (145 tests) passes, `tsc`/lint clean. `RAISE-TRACEABILITY-MATRIX.md` reached **v1.0** — Gap 9 closed, all gaps now resolved. | [PR #58](https://github.com/boonthepkstl-alt/stl_asset_service/pull/58) |
| R-15 | No mock fallback for Auth — TC-LOGIN-01/-02 couldn't be exercised in a dev sandbox | `auth-service.ts`'s `login()` always called the real `go-template-main` backend (`POST /auth/login`) — unlike Asset/Employee/Ticket/Audit/Dashboard, there was no `Mock*Repository`-style fallback, so valid/invalid login couldn't be tested without a real backend + Postgres running | Per confirmed business decision 2026-09-01, added `frontend/src/services/auth-repository.ts` (`AuthRepository` interface, `MockAuthRepository`, `HttpAuthRepository`) mirroring the existing Mock/Http pattern exactly, gated by a new `AUTH_API_ENABLED` flag (`config/featureFlags.ts`, default OFF). Four demo accounts, one per Role (`types/auth.ts`): `admin@raise.dev`/`manager@raise.dev`/`itstaff@raise.dev`/`employee@raise.dev`, all `demo1234`. New `auth-service.test.ts` (2 tests) passes; full suite 147/147 (was 145); `tsc`/lint clean. Live-verified through the real Login page UI (not a localStorage bypass): invalid credentials show "Invalid username or password" (genuine rejection, not a network-failure false positive); valid credentials (`admin@raise.dev`/`demo1234`) log in and land on the Executive Dashboard as "Demo Admin"/ADMIN. `RAISE-TRACEABILITY-MATRIX.md` (v1.1) updated to PASS, Gap 10 closed (infrastructure half only). **This resolves the infrastructure gap only** — the separate, still-open PRD-content question (auth mechanism / role-permission matrix content, PRD §16 Q21–Q22) remains genuinely undefined; the 4 demo accounts are a testing convenience, not a confirmed production role/permission matrix. | [PR #59](https://github.com/boonthepkstl-alt/stl_asset_service/pull/59) |
| R-16 | Alerts (P-012) not built — the sidebar route 404s, worse than a placeholder stub | Prototype P-012 ("Alerts") and `AC-ALERT-001-01..02` specify a screen listing each alert with severity, description, and associated asset, in-app only. The sidebar nav entry "Notification Center" (`id: notifications`) routed to `/notifications`, which rendered the app's generic 404 page, not even `ModulePage`'s placeholder pattern used for Oracle FA/License Mgmt/etc | Per confirmed business decision 2026-09-01, added `frontend/src/pages/Alerts/index.tsx`, registered at `ROUTES.NOTIFICATIONS` (`/notifications`) in `App.tsx`. Scoped to derive its one alert-triggering condition from the one already confirmed elsewhere in the app — an asset's `warrantyExpiry` being in the past (the same `isWarrantyExpired` check the Assets list's Warranty column already uses) — no new field or data model. Severity is rendered honestly as "Not yet defined" rather than an invented High/Medium/Low, since severity mapping and trigger rules for any other condition remain undefined (PRD §6.9 Open Question, **F-05, still genuinely open, not resolved by this fix**). `RAISE-ACCEPTANCE-CRITERIA.md` `AC-ALERT-001` was not modified — it already correctly scoped `AC-ALERT-001-01` as testable only for structural display, so no spec correction was needed first (unlike the F-22/F-27 pattern). Live-verified: `/notifications` renders 11 alert rows, matching the Dashboard's "Expired Warranty: 11" tile exactly; severity/description/asset display correctly, asset links navigate to Asset Detail; no Email/Teams/LINE delivery UI exists anywhere. 2 new automated tests pass; full suite 149/149 (was 147), `tsc`/lint clean. `RAISE-TRACEABILITY-MATRIX.md` (v1.2) updated to PASS (partial), Gap 11 opened and closed same revision (build-gap scope only) — the header bell-icon dropdown across other pages remains a separate, smaller-scope item not wired here. | [PR #61](https://github.com/boonthepkstl-alt/stl_asset_service/pull/61) |
| R-17 | Warranty "Expiring" threshold undefined (90-day figure was illustrative only) | `AC-WARRANTY-001-03`'s 90-day expiry window was the PRD's illustrative Business Example (§6.7), never confirmed as a generalizable business rule — a genuinely open item since the field-list question (Q15, R-12) was resolved 2026-08-29 | Business confirmed 2026-09-01: the Expiring threshold is **per-Asset-Category configurable**, not a single global constant, because different equipment categories carry different warranty periods. Defaults to **90 days for all 5 current categories** (IT Hardware, Mobile, Office Equipment, Infrastructure, Media Equipment), admin-adjustable via a new Settings screen (P-018). Recorded as PRD §16 Resolved Question 41 (resolving new Open Question 15b) and propagated through the full chain via `/update-prd` + individual `/sync-*` calls: `RAISE-DESIGN.md` (v0.10, §5.2 3-state model + new §5.4 Settings Domain), `RAISE-PROTOTYPE.md` (v0.10, P-010 rewritten + new §23A P-018 Settings), `RAISE-ACCEPTANCE-CRITERIA.md` (v0.9, AC-WARRANTY-001-03 rewritten + new AC-WARRANTY-001-04/-05/-06), `RAISE-TEST-PLAN.md` (v0.9, TS-WARRANTY-001 fully unblocked), `RAISE-TEST-CASES.md` (v0.11, 6 test cases). Implemented same day: `frontend/src/lib/warranty.ts`'s `getWarrantyStatus(warrantyExpiry, thresholdDays, asOf)` (3-state Active/Expiring/Expired), a new `WarrantySettings` record (`types/settings.ts`, `services/settings-service.ts`/`settings-repository.ts`, seeded at 90 per category), the Warranty badge on Assets list and Asset Detail updated to the 3-state model, and a new Settings "Warranty" section (`frontend/src/pages/Settings/index.tsx`) with one editable threshold input per category. Verified via automated tests (151/151 passing, `tsc`/lint clean, including a new `TC-WARRANTY-001-03` test and a `settings-service.test.ts` per-category-merge test) and live browser execution: Settings > Warranty renders all 5 categories at default 90; editing IT Hardware to 5000 and saving correctly flags only IT Hardware assets (MacBook Pro, Dell UltraSharp Monitor) as "Expiring" on both the Assets list and Asset Detail, while an unrelated Mobile-category expired asset (iPhone 15 Pro) still correctly shows "Expired" — confirming no cross-category leakage. `RAISE-TRACEABILITY-MATRIX.md` reached **v1.3** — Gap 12 opened and closed same revision. **Not resolved by this fix:** `AC-WARRANTY-001-06`'s non-admin-denial test case for the new P-018 screen is written but not yet executed — tracked separately as **F-34** (Gap 13, still open). | [PR #63](https://github.com/boonthepkstl-alt/stl_asset_service/pull/63) |
| R-18 | Warranty Settings (P-018) access gate — `TC-WARRANTY-001-06` unexecuted (F-34), and a real defect found on execution | `AC-WARRANTY-001-06` (non-admin access/write to the new Settings > Warranty screen is denied) was written but not yet executed | Executed 2026-09-01 — found a real defect, not just an unexercised test: `frontend/src/App.tsx`'s `ROUTES.SETTINGS` was **not actually gated to ADMIN**, sitting in the general authenticated-user route block instead of the existing `<ProtectedRoute allowedRoles={['ADMIN']}>` block that already gates Administration/User/Role Management. Fixed by moving it into that existing block — no new RBAC mechanism invented, reuses the one already confirmed elsewhere (PRD §16 Resolved Q38, UI-only/client-side MVP enforcement). Verified via 2 new tests in `frontend/src/App.rbac.test.tsx` (non-ADMIN redirected to Forbidden; ADMIN let through) — full suite 153/153 (was 151), `tsc`/lint clean. Live-verified: an EMPLOYEE-role user hitting `/settings` sees the real "403 — Access denied" page; an ADMIN-role user sees Settings render normally. `RAISE-TEST-CASES.md` (v0.12) `TC-WARRANTY-001-06` now **PASS**. `RAISE-TRACEABILITY-MATRIX.md` reached **v1.4** — Gap 13 closed (opened v1.3, resolved v1.4, once execution caught the real defect). | [PR #64](https://github.com/boonthepkstl-alt/stl_asset_service/pull/64) |
| R-19 | Check-in/Check-out workflow shape, permission gate, and Custody holder data model (PRD §16 Q11–Q13) | Exact workflow, who may assign/transfer, and the holder data model were all undefined — flagged in `RAISE-FR-OPS-002`/`RAISE-FR-ASSET-003`'s Open Question fields since the PRD's first draft | Business confirmed 2026-09-01, matching already-built, already-tested behavior exactly — no new code required: (1) Check-in/Check-out is an immediate state-change operation, no approval step or exception-handling workflow, deliberately simpler than `RAISE-FR-MAINT-001`'s 4-stage workflow; (2) the permission gate is "any authenticated user, no role restriction," matching the already-confirmed MVP UI-only RBAC enforcement level (PRD §16 Resolved Question 38) — this resolves only this one specific gate, not the general role/permission-matrix content question for other domains (**F-08, still genuinely open**); (3) the Custody holder data model is a direct 1:1 link to an Employee record (`Asset.assignedEmployeeId`/`assignedTo`), no additional organizational relationship model needed for MVP. Recorded as PRD §16 Resolved Question 42 and propagated through the full chain: `RAISE-DESIGN.md` (v0.11, §4.2), `RAISE-PROTOTYPE.md` (v0.12, P-008/P-006 — **a v0.11 draft briefly and incorrectly over-resolved a separate, unconfirmed question** — whether Check-in/Check-out exclusively writes Custody History — **caught and corrected same-session in v0.12**, restoring that question to genuinely open, tracked as **F-10**, unaffected), `RAISE-ACCEPTANCE-CRITERIA.md` (v0.10, AC-OPS-002-01/-02 rewritten), `RAISE-TEST-PLAN.md` (v0.10, TS-OPS-002 fully unblocked), `RAISE-TEST-CASES.md` (v0.13, TC-OPS-002-01..03 status notes updated). **No new test execution was performed or claimed** — `TC-OPS-002-01..03` were already formally executed and PASS (2026-08-28), and that execution already exercised the newly-confirmed scope (an authenticated user performed the operation with no role gate blocking them, since none exists). `RAISE-TRACEABILITY-MATRIX.md` reached **v1.5** — Gap 14 opened and closed same revision. **Not resolved by this fix:** Open Finding F-10 (Custody History write-path exclusivity) and Open Finding F-08 (general RBAC role/permission-matrix content for other domains) both remain genuinely open, explicitly untouched. | (branch/PR pending — see `DEVELOPMENT-LOG.md`) |

| R-20 | CI/CD pipeline (F-14) | Neither stack had any automated build/test pipeline. Every one of the 88 PRs merged before this was verified locally only, by hand, relying entirely on `SESSION-CLOSEOUT-PROTOCOL.md` step 3 being honoured each session — with multiple Claude sessions working in the same directory concurrently (`CLAUDE.md`), and no mechanism to catch a check that was skipped or a machine-specific failure | [PR #89](https://github.com/boonthepkstl-alt/stl_asset_service/pull/89) (merge commit `103e069`, 2026-09-04). Two parallel jobs: **frontend** (`npm ci` → lint → `tsc --noEmit` → vitest → production build) and **backend** (`go build` → `go vet` → `go test`). **Resolution evidence, on `main` and not merely on the PR branch:** workflow run `33843149477`, event `push`, `head_branch=main`, sha `103e069` — Backend green in 1m15s, Frontend green in 4m52s, **zero annotations**. Locally re-verified on merged `main` in the same pass: frontend `tsc`/lint clean, 48 files / 235 tests, production build clean; backend `go build`/`vet`/`test` clean. **The pipeline proved itself before merge**: on its fourth run it failed on a genuine pre-existing flake in `CreateEmployee`/`CreateAsset` that had passed every local run (see **F-40**). **Deliberately out of scope, so F-14's image-build half stays open above**: no image build/push, no deploy — hosting is F-13. `gofmt` was intentionally not a gate. **Correction, 2026-09-07 (F-49 → R-34): the reason recorded here was false.** It said Go sources "are committed with CRLF … so `gofmt -l` flags every file in the module on a Linux runner". They are committed with **LF** — all 64 verified by extracting `git show :<path>`, zero CRs, and `gofmt -l` over that content flags nothing. The `gofmt -d` check cited above was real but run against the **Windows working tree**, where `core.autocrlf=true` had translated on checkout; the mistake was inferring that a Linux runner would see the same thing. The gate was never blocked and is now added |

| R-21 | Frontend bundle size (F-18) | The production build emitted a **single 694 KiB JS chunk** containing all **27** routes — `React.lazy` was used **zero** times — so a visitor landing on `/login` downloaded the Asset Registry, the AI Decision Center and every admin screen before seeing a password field. Vite warned about it on every build | [PR #91](https://github.com/boonthepkstl-alt/stl_asset_service/pull/91) (merge commit `02ae966`, 2026-09-04). Every authenticated page became a `React.lazy` chunk behind one `Suspense` boundary. **Measured on the production build, not estimated:** entry chunk **694 KiB → 305 KiB (312.5 kB, gzip 102 kB), −56%**; chunks **1 → 80**; total JS 694 KiB → 789 KiB (**+95 KiB of chunking overhead, paid only for pages actually visited, against a >50% cut to the load everyone pays**); Vite's >500 kB warning gone. **Three components deliberately left eager**, each for a stated reason: `Login` (entry point of every unauthenticated visit — a lazy chunk would add a round-trip to the most common first paint), and `NotFound`/`Forbidden` (small, and rendered as the destination of a catch-all or redirect, where a loading fallback before an error page is worse than the bytes saved). The `Suspense` fallback is deliberately identical to `ProtectedRoute`'s auth-loading state so a cold chunk fetch and an auth check read as one wait. **No test needed changing, verified before assuming** — lazy routes resolve asynchronously, the same timing class as **F-40**, so the App-level tests were checked first: only `App.rbac.test.tsx` imports from `@/App` and it uses `ProtectedRoute` with stub elements, while the other seven `App.*.test.tsx` files build their own route trees from direct page imports. 48 files / 235 tests passed unchanged. Live-verified through the real UI (login → dashboard, then `/assets`, `/employees`, `/handovers`, `/notifications`, `/settings`) with no console errors and no failed requests |

| R-22 | Backend 5xx error responses (F-19) | Eighteen 5xx handlers across 7 controllers put `err.Error()` straight into the JSON body beside a human-readable `message`. Those errors originate in the database layer, so a response could carry driver text, SQL fragments, schema names or connection detail to the caller | [PR #93](https://github.com/boonthepkstl-alt/stl_asset_service/pull/93) (merge commit `449a751`, 2026-09-04). All 18 sites now return `message` only; **0 5xx `err.Error()` sites remain** (verified on merged `main`). **Proved on the wire, not by inspection:** with the database container stopped, `GET /api/assets` previously would have returned `dial tcp: lookup db on 127.0.0.11:53: server misbehaving` in the body; it now returns exactly `{"message":"Failed to retrieve assets"}` — while the server log still records the full error, so no diagnosability was lost. Every one of the 18 sites was checked individually to confirm it already logged before responding, including the two that delegate to `mapHandoverError` (both callers log first). **Scope was deliberately held to F-19's own wording — 5xx only, 18 of 46 `err.Error()` sites.** The other 28 are 4xx, many carrying service-layer sentinels (`ErrAssetNotITHardware`, `ErrHandoverWrongStage`, ...) whose text is meaningful validation feedback; removing those would degrade the API rather than harden it. **Safe for the frontend, verified:** `types/api.ts` declares `APIError.error`, but nothing in `frontend/src` reads it — the response interceptor branches only on status 401. **Guarded by `controller/internalErrorLeak_test.go`**, a source-level invariant check (the existing controller harness needs live MSSQL/Postgres/Tarantool handles and a signed token before it can serve a request, so driving 18 real 500s would cost far more than the invariant is worth; the trade is recorded in the test). **Mutation-tested** — re-adding the field to `dashboardController` fails it with the exact file and line — and it asserts it scanned ≥15 sites so it cannot pass vacuously after a future refactor |

| R-23 | Alert trigger rules and severity (F-05) | `RAISE-FR-ALERT-001`'s Acceptance Criteria said only "relevant conditions can trigger an alert"; which conditions, at what severity, was undefined. The shipped Alerts screen therefore had exactly one trigger (expired warranty) and rendered severity honestly as "Not yet defined" rather than inventing High/Medium/Low | [PR #95](https://github.com/boonthepkstl-alt/stl_asset_service/pull/95) (merge commit `ae56120`, 2026-09-04). Business confirmed by direct Q&A: **five** MVP trigger conditions — Warranty EXPIRED → **High**, Ticket OVERDUE (`targetResolutionDate` passed and status ≠ `DONE`) → **High**, Warranty EXPIRING (reusing the existing per-Asset-Category threshold, default 90 days) → **Medium**, Ticket ON_HOLD → **Medium**, Handover PENDING → **Low**. Severity is **fixed per condition type**; the alternatives (derive from days-overdue, or from asset value/criticality) were both offered and rejected, and no asset-criticality field was invented. **No new field and no data-model change** — every condition reads state that already exists, and Design v0.13 §14 records alerts as a **read-time derivation with no Alert entity, table or persisted record**. Propagated through the full chain in order, each layer verified against the real file: PRD 0.14→**0.15** (Resolved Question 44; §17 row `TBD`→`APPROVED`), Design 0.12→**0.13**, Prototype 0.13→**0.14**, AC 0.11→**0.12** (AC-ALERT-001 extended 2→10 criteria), Test Plan 0.11→**0.12**, Test Cases 0.16→**0.17** (8 new cases, **none marked PASS**), Matrix 1.8→**1.9**. **Specification only — `RAISE-FR-ALERT-001` stays `PASS (partial)` and was deliberately not upgraded**: the reason for the partial status changed from "trigger rules undefined" to "rules defined, four of five conditions unimplemented", recorded as the still-open **Gap 16**. The bell-icon scope contradiction is separately recorded as the still-open **Gap 17**; no side was picked |

| R-24 | `TC-ALERT-001-09` unexecutable procedure (F-42) | The test verifying that an alert disappears once its condition stops holding instructed the tester to *edit an Asset's `warrantyExpiry`* — something the product cannot do at all (`asset-repository.ts` exposes only `create`, `assign`, `checkIn`). It was the single case keeping **Gap 16** open | Corrected and executed 2026-09-04 on merged `main` `30f176c`. **The correction landed before the execution, deliberately** — the procedure was rewritten in `RAISE-TEST-CASES.md` v0.19 and left *unexecuted*, then run in v0.20, so it could not be shaped around whatever happened to pass. Re-pointed at a state change the product genuinely supports and verified in source first: Ticket Detail's real **Update Status → `Done`** control (`ticketService.updateExecutionStatus`). **Executed result: PASS.** Alerts showed **19** with seeded ticket `REQ-2026-0041` present as **two** rows (`High` Overdue + `Medium` On Hold); completing it to `DONE` through the product's own path took Alerts to **17** — a drop of exactly 2 — with zero rows referencing it. A scan of every button on the screen found **no** acknowledge/dismiss/clear/mark-read/snooze affordance, so the rows vanished purely because both conditions stopped holding. The corrected procedure tests the property **more strongly** than the original would have: one state change cleared **two different conditions at once**. **The fix was to the test, not the product** — adding an asset-edit capability would have been unrequested scope with no requirement behind it |

| R-25 | Alerts access gate + role-enforcement location (F-08, partial) | `RAISE-FR-ALERT-001`'s Acceptance Criteria said alert information is *"visible to an authorized user"*, but "authorized" had never been defined — so `AC-ALERT-001-01`'s access-gate half sat **NOT TESTABLE YET**, and it was the last reason the requirement was not a full `PASS` | Business confirmed by direct Q&A 2026-09-04, recorded as PRD v0.16 **§16 Resolved Question 45**: **authorized = authenticated** for the Alerts screen — all four roles (`EMPLOYEE`, `IT_STAFF`, `IT_MANAGER`, `ADMIN`) may open it, none excluded — and **enforcement stays declared in code per route** (`ProtectedRoute allowedRoles`), not data-driven. Both **confirm behaviour already built** rather than changing it. Propagated through the chain: PRD 0.15→**0.16**, Design 0.13→**0.14**, Prototype 0.14→**0.15**, AC 0.12→**0.13** (access-gate note closed; new `AC-ALERT-001-11` for the unauthenticated-redirect negative case), Test Plan 0.12→**0.13**, Test Cases 0.20→**0.21**, Matrix 2.1→**2.2**. **Executed against merged `main` `d8ad01c`:** signed in as each of the four demo accounts in turn with the session cleared between each — all four reached `/notifications` and saw all 19 alerts, no Forbidden and no redirect; and with storage cleared, `/notifications` redirected to `/login` showing no alert data. **All 11 `TC-ALERT-001-*` cases now execute and PASS**, taking `RAISE-FR-ALERT-001` to a full **`PASS`**. **A procedural note worth keeping:** the negative case first *appeared* to let an unauthenticated visitor through. That was a false result — the browser still held an `ADMIN` session in `localStorage` from an earlier run. It was verified, cleared, and re-run properly. Recorded so nobody repeats it in a dirty profile and reports a false failure. **F-08 is only narrowed, not closed** — see its row above |
| R-26 | Raw driver errors in 404 response bodies (F-41) | `GET /api/assets/does-not-exist` answered **404** with `{"error":"sql: no rows in result set","message":"Asset not found"}`. F-19 had scoped its fix to 5xx on the stated basis that 4xx carries only sentinel errors whose text is meaningful feedback — true of many sites, but not of any "get one record by id/code" handler, which passed the repository error straight through. **Fix:** Audited **all 28** 4xx sites before changing anything, then fixed **7**: the five not-found handlers (Asset/Employee/Ticket/Sample/Handover), `CreateTicket`'s service-error branch (reachable by a raw `repo.List` error), and `mapHandoverError`'s not-found case. That last one is the only place where dropping the field would have lost real information — it could no longer say *which* record was missing — so the branch was **split in two** and the distinction moved into the documented `message` field, where an API caller should have been reading it anyway. **Nothing was swept**: 5 `errors.Is`-guarded sentinel sites keep their text, and 16 request-parse sites are left untouched and tracked as **F-43**. New guard `TestNoRawErrorTextIn404Responses` makes 404 a blanket rule, with the same anti-vacuity threshold as F-19's scan; verified by mutation — reinstating the leak fails it by name and line. F-19's own test comment, which asserted 4xx carries only sentinels, was corrected since that claim is what F-41 disproved. `APIError.error` in the frontend was also made optional: the type declared it **required** while two whole classes of response no longer send it. | 2026-09-05, PR #103 |
| R-27 | Non-deterministic local test runs (F-44) | `npx vitest run` intermittently under-collected test files on Windows — 47/219, 49/240, 49/244 on an unchanged tree — each time with an `EBUSY` error under vitest's temp cache. **Fix:** traced to the pool rather than guessed at: Vitest 2 defaults to `forks`, whose child process cannot share the parent's module cache and so writes every transformed module to a file under `os.tmpdir()` (`cacheFs: true`, set only on the child-process channel). Two workers race that write and one loses. `pool: 'threads'` shares memory and never writes there. Verified by measurement, not assumption: threads 50/257 on **3 of 3** runs, forks 50/257 on **3 of 4**, both ~12s. `--no-file-parallelism` was rejected as the fix — also deterministic, but 160s against 12s. F-44's own text was corrected in the same pass: it claimed the failing run "still exits 0", which had never been measured (the original command was piped through `grep`, so `$?` was grep's). A failing run exits **1**. | 2026-09-05, PR #104 |
| R-28 | Executive Dashboard KPI definitions (F-03, partial) | `RAISE-FR-EXEC-001` named three "Proposal-defined KPIs" — NBV, Risk, Utilization — none of which was on the shipped dashboard, all tracked together as one decision gap. Checking the source before asking showed that framing was wrong for one of them. **Fix:** **Utilization** needed no decision at all (RQ27/RQ29 settled it on 2026-08-21) and was simply built — PR #102, live-verified at 66.7% / "8 of 12 assignable assets". **NBV's formula** was confirmed by business (**RQ46**) — straight-line, per-category useful life in Settings following RQ41's precedent, salvage zero — but its default values were **not** supplied, so it is recorded as specified-but-not-buildable under new **Open Question 3a** rather than guessed at; the numbers become money on an executive dashboard. **Risk** was confirmed **out of MVP scope** (**RQ47**), which only re-states what `RAISE-AI-RISK-001`'s existing "Pilot (not confirmed MVP)" priority already meant — the single most useful correction in the round, since it means §16 Q4 never blocked this P0 requirement. Propagated through all seven chain documents (PRD 0.17 / Design 0.15 / Prototype 0.16 / AC 0.14 / Test Plan 0.14 / Test Cases 0.22 / Matrix 2.3). The sync also repaired a criterion that had gone **factually false**: `AC-DASH-03` asserted none of the three KPIs was present, which stopped being true the moment Utilization shipped. **F-03 stays open** on the useful-life defaults. | 2026-09-05, PR #105 |
| R-29 | Test-case step named a role the app does not have (F-45) | `TC-EXEC-001-01` and `-03a` opened with "Log in as Executive", but `UserRole` has exactly `ADMIN`, `IT_MANAGER`, `IT_STAFF`, `EMPLOYEE` — "Executive" is a PRD persona, not a system role. **Fix:** both step 1s changed to "Log in as any authenticated user", each noting the dashboard route carries no role restriction (PRD §16 Resolved Question 45), so nothing is narrowed. Deliberately **not** fixed during the Gap 19 execution that found it — rewriting a procedure mid-run destroys the evidence it was wrong, the discipline established by F-42 — and fixed in the next revision that touched the file instead. Both cases keep their recorded PASS and execution history untouched: wording only. | 2026-09-07, PR #109 |
| R-30 | `AC-ALERT-001-12` over-specified a header numeral the product does not render (F-46) | The criterion claimed the closed header bell displays the numeric alert total; it renders only a presence dot, with the numeral in the `aria-label` and the dropdown panel header badge. `TC-ALERT-001-12` inherited the error and could not be executed as written. **Fix — and the order is the point:** the case was **not** marked PASS and its procedure was **not** rewritten mid-execution (the F-42 discipline). Business confirmed the fix was to correct the specification, not the product — PRD §16 Resolved Question 49 never asked for a header numeral and the pre-existing bell already used a dot, so the product matched the decision and the spec had over-reached. `AC-ALERT-001-12` was rewritten in place (AC v0.16), Test Plan v0.16 followed, `TC-ALERT-001-12` was corrected in Test Cases v0.25 and **left unexecuted there** so the steps could not be shaped around whatever passed, then executed in v0.26 — **PASS**: closed button text empty with a single class-only dot span, `aria-label` "Notifications, 19 alerts", panel badge "19", P-012's own pagination "Showing 1-10 of 19", and an automated equality check across all three returning true. **Recorded as an AI-introduced specification error**, since that is what it was. | 2026-09-07, PR #109 |
| R-31 | Compliance Review stale and overstating compliance (F-47) | The chain's terminal deliverable sat at v1.0 against matrix v1.4 while the matrix reached v2.6, reporting `RAISE-FR-EXEC-001` as a full `PASS` when it is `PASS (partial)`. **Fix:** re-verified to v1.1 against v2.6 — `RAISE-FR-EXEC-001` corrected **down**, `RAISE-FR-ALERT-001` upgraded, §4 rewritten to state that the headline 8-of-17 figure was unchanged **while its membership swapped** (a stable number concealing two opposite movements is exactly why currency matters), §5 corrected for four findings wrongly listed as open, §7 corrected from "Gaps 1–13" to Gaps 1–20 with none open. **The process gap was closed with it:** v1.0 said "no action is required to finish this review", which is why nothing pointed back at it — the Next Action now requires re-verification in the same pass that moves a matrix verdict, and `CLAUDE.md` (which still claimed the file did not exist) now lists it with that warning. | 2026-09-07, PR #111 |
| R-32 | A token-signing failure was reported as a 401 with its own error text (F-43, half b) | `authService.Login` returned three bare `errors.New` values the controller could not tell apart, so it answered **401** for all three — including `util.GenerateToken` failing, which is a **server-side** fault, not a rejected password. The response also carried that error's own text. **A premise correction came first:** this finding had recorded that fixing it "changes login response text the frontend may rely on", which **was never verified and is false** — `HttpAuthRepository.login` returns `response.data` and reads no error body, and `pages/Login/index.tsx` catches and displays its own fixed string. With that checked, the (b) half stopped being a product decision and became a plain defect. **Fix:** three exported sentinels (`ErrMissingCredentials`, `ErrInvalidCredentials`, `ErrTokenGeneration`), the two authentication cases keeping **401** and their **byte-identical original wording** — deliberately not tidied up in passing, since that text is observable API behaviour — and the signing failure mapped to **500** with a fixed message, detail logged only. The existing F-19 guard (`TestNoRawErrorTextIn5xxResponses`) covers the new 5xx automatically. **4 new tests, verified by mutation** — swapping the two sentinels fails them by name. The 4xx audit now re-classifies this site from `PARSE` to `SENTINEL`, so the heuristic agrees with reality. | 2026-09-07, PR #112 |
| R-33 | Findings register misclassified two findings and misdescribed a third (F-48) | The **"Blocking (gates an MVP requirement)"** section carried **F-09** and **F-35**, neither of which gates any requirement's verdict anywhere in the chain — F-09 appears zero times in the traceability matrix and the Compliance Review says explicitly that it does not block `RAISE-FR-ASSET-001`'s PASS; F-35 appears in neither document, and that requirement is a full `PASS` on the current scheme. **F-15** separately read "`/api` vs `/api/v1` undecided" when both sides use `/api` and `RAISE-API-DB-SPEC.md` already records the base URL as unversioned. **Fix:** both findings reclassified to "Unresolved (scope question, not yet blocking a build)" with the reason recorded in place, and F-15's description corrected to separate the settled prefix from the genuinely open question of whether to introduce versioning later. **This audit found no hidden buildable work** — unlike the two before it — and it also confirmed one finding was parked *correctly*: **F-36**'s inconsistency is inert, because `EditEmployee` never touches `employeeCode`, so the hypothesis that a user would be blocked editing seeded data was wrong. | 2026-09-07, PR #114 |
| R-34 | No `gofmt` gate, on a reason that was never true (F-49) | `ci.yml` and R-20 both recorded that Go sources are committed with CRLF, so `gofmt -l` would flag every file on a Linux runner. **They are committed with LF** — 64 of 64 verified via `git show :<path>`, zero CRs, `gofmt -l` clean over that content. The local CRLF is `core.autocrlf=true`. **Fix:** the gate is added (it reports the offending files and a `gofmt -d` diff on failure, so a red build is actionable rather than just red); `.gitattributes` pins `*.go text eol=lf` as prevention, not as a fix for anything broken; and both false records are corrected in place rather than quietly replaced. **Nothing in the repository needed normalising** — the change that had been described as "its own change, normalising line endings across the repo" turned out to be zero files. **Corrected in review:** this resolution first said gofmt over repository content flagged nothing. It flagged one file — `controller/internalErrorLeak_test.go`, a real missing blank line from PR #103 that CRLF noise had masked locally. The set-equality evidence proved the sets matched, not that line endings were the only cause. The gate caught it on its first run and it is fixed here; the module is now verified clean over LF content, 64 files, 0 flagged. | 2026-09-07, PR #115 |
| R-35 | Employee audit rows could never reflect a write in a mounted page (F-38, partial) | `EmployeeDetail` held the shared `employeeAuditLogs` fixture in `useState` and derived its rows through a `useMemo` keyed on that value, while `EditEmployee` appends with `fixtureAudit.unshift(...)`. An array's identity never changes when its contents do, so the memo could not recompute from a write. Masked only because `EditEmployee` navigates back and the page remounts. **Fix:** the derived list is no longer cached at all — the filter runs on every render over the live array. **The route there is the useful part.** Two earlier attempts were wrong: `[fixtureAudit.length]` in the deps, which ESLint rejected on exactly the right grounds (*"outer scope values … aren't valid dependencies because mutating them doesn't re-render the component"*), and reading the live array while keeping the memo, which is merely **cosmetic**. That second attempt was exposed by mutation-testing the guard: it **still passed with the original bug restored**, because the first version of the test appended *before* mount — a vacuous test. The test now appends **after** mount, the only arrangement that separates broken from fixed, and it fails against the restored aliasing. Suite 51 files / 262 tests → **52 / 263**. **Scope held:** no backend was invented for Employee audit — there is no `RAISE-FR-EMP-*` requirement to trace one to. | 2026-09-07, PR #116 |
| R-36 | NBV formula implemented ahead of its defaults (F-03, groundwork) | F-03 had been reported as blocked outright on five numbers. **It was not, and that framing was mine.** RQ46 confirms the formula, the configuration shape, the salvage value and the clamp; only the per-category defaults are missing. The precedent for building the rest without them already existed in the same codebase: `lib/alerts.ts` takes `warrantyThresholdFor` as a parameter rather than embedding RQ41's threshold. **Built:** `lib/nbv.ts` — `computeAssetNbv` and `computePortfolioNbv`, pure, `asOf` injectable, useful life injected, **zero defaults defined**. 15 tests; suite 52 files / 263 tests → **53 / 278**. **Verified by three mutations against RQ46's own clauses**, each failing a different count of tests: removing the clamp (1), ignoring the asset's category (2), and returning 0 instead of purchase cost for an unconfigured category (3). **Two judgement calls recorded rather than silently made:** an unconfigured or NaN useful life returns `purchaseCost` unchanged, not 0 and not NaN — "not yet depreciated" is honest where 0 would claim the asset is worthless and NaN would render as "NaN" on a KPI tile; and **no status filter is applied**, because RQ29(b)'s Retired/In-Maintenance exclusion is Utilization's confirmed rule and RQ46 states no equivalent — a retired asset still has a book value, so excluding one would have been inventing a business rule. Both are pinned by tests. **Also pinned: the module never reads `Asset.currentValue`**, the trap RQ46 warns about — a test feeds a wildly contradictory value and asserts the result does not move. **F-03 stays open** on the defaults. | 2026-09-07, PR #117 |
| R-37 | Register row read as open 656 characters before disclosing it was closed (F-50) | F-42's status cell opened with *"Open — and it is the single item keeping Gap 16 open"* and revealed **RESOLVED 2026-09-04 (R-24)** only far downstream, with no separator. **Caught by its own victim:** a script written to enumerate open findings during the 2026-09-07 assessment read that lead and counted F-42 as open. **Fix:** the cell leads with the resolution; the superseded reasoning is kept under an explicit *History* label rather than deleted, and the cell names F-50 so the defect is discoverable from the row it affected. All 34 `F-` rows were scanned — **only this one** had the late-disclosure shape. Blast radius confirmed contained: no other document describes F-42 as open. **Value stated honestly: this unblocks nothing** and moves no verdict; it repairs the surface used to decide what to work on next, which had already produced one wrong reading. | 2026-09-07 |
| R-38 | Three source comments cited PRD §16 Q3/Q4 as open (F-51) | `utilization.ts`, `dashboard-repository.ts` and `dashboardModel.go` justified excluding NBV/Risk on the grounds that Q3/Q4 were unanswered. **RQ46 answered Q3's formula half, and RQ47 removed the Risk KPI tile from the MVP dashboard**, so the rationale had outlived its usefulness by the time `lib/nbv.ts` shipped (R-36). **The questions themselves are not closed, and saying so was this fix's own first error:** the initial version wrote *"neither is an open question any more"* / *"stale on both halves"*, which `RAISE-PRD.md` §16 contradicts directly — **Q4 is marked `Still open`** and **Q3a** (the per-category useful-life defaults, i.e. F-03) is open as well. **Caught in review of PR #119 before merge, by reading §16 instead of trusting the PR's own summary** — the same failure mode as F-49's set-equality overstatement, and the fourth time this session that a stated premise did not survive being checked. **Fix as shipped:** each comment cites RQ46/RQ47, gives the module's real reason for excluding the figure, and states that Q3a and Q4 remain open. **Deliberately unchanged: the boundaries themselves.** None of the three modules should compute NBV or Risk, and none now does — the diffs are comment-only, so `tsc`, ESLint, `gofmt`, `go vet` and all 278 tests exercise byte-identical behaviour. | 2026-09-07 |
| R-39 | Chain and PRD disagreed on how NBV useful life is keyed (F-53) | The chain specified `NBVSettings: Record<AssetCategory, usefulLifeYears>` across all six documents while PRD v0.20 §16 Resolved Question 52 confirmed **per Asset Type**. **Fixed by a second full sequential chain sync** through the `.claude/skills` subagents — Design v0.18, Prototype v0.19, AC v0.18, Test Plan v0.19, Test Cases v0.28, Matrix v2.8 — with `TC-WARRANTY-001-07` rewritten from *"all 5 categories with editable useful-life inputs"* to one row per Asset Type. **Deliberately not recorded as a rule at any layer: a fixed count of ten types**, because `type` is a free-text `varchar(100)` that grows as assets are added; and **RQ51 needed no edit**, because a newly-added type with no configured value already hits its `purchaseCost` fallback — the rule generalised without being touched. **Verified per stage rather than taken on the subagents' reports:** each of the six diffs filtered for all ten type names and all five category names near a year figure — **all six empty**; the old key survives only inside "re-keyed from…" notes and historical changelog entries, audited occurrence by occurrence (nine of them in the Test Plan alone); **recorded execution evidence untouched** — `TC-DASH-01`'s row has identical counts before and after (2026-08-31 ×1, 2026-09-05 ×2, PASS ×4); Warranty's genuinely per-Category threshold (RQ41) and `ASSET-002`'s Category→Type hierarchy left alone at every layer; Gap 21 still open with its superseded PASS records intact; no NBV case marked PASS; `RAISE-FR-EXEC-001` still `PASS (partial)`. **A dating slip was corrected in the same pass** (five documents dated their prior sync 2026-09-07, the date RQ50/RQ51 were *confirmed*, though those syncs ran 2026-09-08) — each subagent judged its own dates individually rather than running a blanket replace, which would have corrupted recorded evidence. **Design turned out to have no slip at all** and the Matrix was already correct. **One out-of-scope defect was fixed rather than filed:** the Test Plan carried a trailing footer still citing AC v0.14 and a nine-tile grid, contradicting its own header — its subagent flagged it as pre-existing and out of scope, and it was sent back, because that is the same stale-statement class as F-53 itself (hence Test Plan 0.17 → 0.19). | 2026-09-08 |
| R-40 | Shipped SLA commitments had no business authority (F-54) | The app defined `SLA_HOURS = { Critical: 2, High: 8, Medium: 24, Low: 48 }` in **both tiers** (`ticket-service.ts:14`, `ticketService.go:19-26`), stamped `slaTargetHours` on every ticket, and labelled the priority selector *"Critical (2h SLA)"* … *"Low (48h SLA)"* — a time commitment shown to a requester — while the PRD said SLA was **TBD**, no §16 Resolved Question covered it, Prototype §15 P-009 listed *"Priority (conceptual — TBD)"*, and AC §12 marked SLA **NOT TESTABLE YET**. **Resolution: business confirmed the values as they stand** (PRD §16 **Resolved Question 53**), so **no code changed** — the shipped behaviour was right, it simply had no authority. **The distinction the whole fix turned on, checked before anything was written:** the PRD's *"SLA per stage"* TBD is a **different question** — confirmed is one overall target per ticket keyed by priority, still open is how long each of the four workflow stages may take. Had those been conflated, a per-priority answer would have silently closed a per-stage question nobody answered — the same shape as the **Q3/Q4** and **Q3a** errors this project already had to correct. *"SLA per stage"* survives as TBD in all six documents, along with the vendor model, cost model and delegated-approver rules that business said nothing about. **What was deliberately not done:** removing the SLA labels or altering the numbers on the AI's own initiative, which would have been inventing the business decision in the opposite direction. **New in the chain:** `AC-MAINT-001-10` and `TC-MAINT-001-10`, classified **testable today — not BLOCKED** (unusual here; the behaviour is already shipped) but **not marked PASS**, because neither has been executed. **Matrix v2.9 kept `RAISE-FR-MAINT-001` at full `PASS`** — that verdict rests on the executed 4-stage transitions and **predates** both new items, so **no executed test has ever asserted an SLA value** — and opened **Gap 23** for the unexecuted case rather than folding it in as coverage. **Verified per stage rather than taken on the subagents' reports:** each of the six diffs filtered for hour figures contains only **2h, 8h, 24h, 48h**, so no number beyond the four was written anywhere. | 2026-09-08 |
