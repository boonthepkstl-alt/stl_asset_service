# RAISE — Development Log

**Purpose:** the raw, technical, PR-by-PR engineering log — one entry per
merged pull request, in merge order, with enough detail to know what
actually changed without opening GitHub. This is the most granular file in
`docs/project-management/` — for a zoomed-out narrative grouped by phase,
see [`PROJECT-TIMELINE.md`](PROJECT-TIMELINE.md); for stakeholder-facing
"what's new," see [`CHANGELOG.md`](CHANGELOG.md); for the current
point-in-time snapshot, see [`CURRENT-STATUS.md`](CURRENT-STATUS.md).

**Maintenance rule:** append one row per merged PR, immediately after
merging (same step as updating `CHANGELOG.md`). Never edit past rows except
to fix a factual error. Regenerate/verify with:
```
git log --merges --oneline --format="%h %ad %s" --date=short
gh pr view <n> --json title,mergedAt,body
```

---

| # | Date | Branch | Summary | Layer(s) touched |
|---|---|---|---|---|
| [#1](https://github.com/boonthepkstl-alt/stl_asset_service/pull/1) | 2026-08-21 | `wip/backend-rbac-wiring-unconfirmed` | Wire `RequireRole` middleware to admin-gated sample routes | Backend (middleware) |
| [#2](https://github.com/boonthepkstl-alt/stl_asset_service/pull/2) | 2026-08-22 | `prd/close-needs-prd-confirmation-items` | Close all 6 `NEEDS_PRD_CONFIRMATION` items from FRONTEND-FOUNDATION-BASELINE.md | Docs (PRD) |
| [#3](https://github.com/boonthepkstl-alt/stl_asset_service/pull/3) | 2026-08-22 | `chain-sync/license-maint-rbac-workflow` | Sync deliverable chain for Maintenance workflow, License scope (Roadmap), RBAC enforcement level | Docs (Design/Prototype/AC/Test Plan/Test Cases) |
| [#4](https://github.com/boonthepkstl-alt/stl_asset_service/pull/4) | 2026-08-22 | `frontend/gate-roadmap-license-ai-features` | Gate Roadmap-only Licenses/AI Decision Center pages behind a feature flag | Frontend |
| [#5](https://github.com/boonthepkstl-alt/stl_asset_service/pull/5) | 2026-08-22 | `frontend/login-restyle-and-dev-server-autoport` | Restyle Login page (split-panel); make dev server port configurable | Frontend |
| [#6](https://github.com/boonthepkstl-alt/stl_asset_service/pull/6) | 2026-08-22 | `template/fix-react-template-build-errors` | Fix react-template-main build failure (unused imports) | Template |
| [#7](https://github.com/boonthepkstl-alt/stl_asset_service/pull/7) | 2026-08-22 | `backend/asset-domain` | **Add RAISE Asset Registry domain** to go-template-main (`RAISE-FR-ASSET-001`) | Backend |
| [#8](https://github.com/boonthepkstl-alt/stl_asset_service/pull/8) | 2026-08-23 | `frontend/wire-asset-repository-to-api` | Wire frontend `asset-repository` to the new go-template-main Asset API | Frontend |
| [#9](https://github.com/boonthepkstl-alt/stl_asset_service/pull/9) | 2026-08-23 | `backend/employee-domain` | **Add RAISE Employee domain** to go-template-main + wire frontend | Backend + Frontend |
| [#10](https://github.com/boonthepkstl-alt/stl_asset_service/pull/10) | 2026-08-23 | `backend/fix-login-response-contract` | Fix login response contract: bare object, camelCase, token in body | Backend + Frontend |
| [#11](https://github.com/boonthepkstl-alt/stl_asset_service/pull/11) | 2026-08-24 | `backend/maintenance-domain` | **Add RAISE Maintenance/Ticket domain** (`RAISE-FR-MAINT-001`) | Backend + Frontend |
| [#12](https://github.com/boonthepkstl-alt/stl_asset_service/pull/12) | 2026-08-24 | `docs/sync-nfr-backlog-full-chain` | Sync deliverable chain: NFR backlog acknowledgment + traceability Gap 6 fix | Docs |
| [#13](https://github.com/boonthepkstl-alt/stl_asset_service/pull/13) | 2026-08-24 | `backend/asset-checkin` | **Add Asset Check-in** (`RAISE-FR-OPS-002`), wire real Assign flow | Backend + Frontend |
| [#14](https://github.com/boonthepkstl-alt/stl_asset_service/pull/14) | 2026-08-24 | `docs/project-timeline-tracker` | Add project timeline/checkpoint tracker (single-file version, later restructured) | Docs |
| [#15](https://github.com/boonthepkstl-alt/stl_asset_service/pull/15) | 2026-08-24 | `docs/technical-design-hla-api-db-detailed` | Add High-Level Architecture, API/DB Spec, and Detailed Design docs | Docs |
| [#16](https://github.com/boonthepkstl-alt/stl_asset_service/pull/16) | 2026-08-24 | `docs/project-management-restructure` | Split `RAISE-PROJECT-TIMELINE.md` into six focused files (this one, `PROJECT-TIMELINE.md`, `PROJECT-CHECKPOINTS.md`, `CHANGELOG.md`, `OPEN-FINDINGS.md`, `CURRENT-STATUS.md`) | Docs |
| [#17](https://github.com/boonthepkstl-alt/stl_asset_service/pull/17) | 2026-08-24 | `docs/timeline-10-phase-roadmap` | Redesign `PROJECT-TIMELINE.md` as a 10-phase capability roadmap | Docs |
| [#18](https://github.com/boonthepkstl-alt/stl_asset_service/pull/18) | 2026-08-24 | `docs/checkpoints-structured-template` | Rewrite `PROJECT-CHECKPOINTS.md` with the structured template (17 PRs retroactively converted) + add `SESSION-CLOSEOUT-PROTOCOL.md` (14-step close-out checklist, 3-level checkpoint model) | Docs |
| [#19](https://github.com/boonthepkstl-alt/stl_asset_service/pull/19) | 2026-08-24 | `docs/claude-md-refresh` | Refresh `CLAUDE.md` to match actual project state | Docs |
| [#20](https://github.com/boonthepkstl-alt/stl_asset_service/pull/20) | 2026-08-24 | `docs/claude-md-session-protocol-rule` | Make the Development Session Close-Out Protocol a binding `CLAUDE.md` rule | Docs |
| [#21](https://github.com/boonthepkstl-alt/stl_asset_service/pull/21) | 2026-08-24 | `docs/baseline-checkpoint-2026-08-24` | Add `BASELINE-CHECKPOINT-2026-08-24` from a live git/source-code scan | Docs |
| [#22](https://github.com/boonthepkstl-alt/stl_asset_service/pull/22) | 2026-08-24 | `docs/next-step-protocol` | Add the Next-Step Development Protocol and run it against current state | Docs |
| [#23](https://github.com/boonthepkstl-alt/stl_asset_service/pull/23) | 2026-08-24 | `frontend/login-brand-styling` | Apply stakeholder brand proposal to the Login page (generic pass, later superseded by Singer CI) | Frontend |
| [#24](https://github.com/boonthepkstl-alt/stl_asset_service/pull/24) | 2026-08-24 | `frontend/singer-ci-login-branding` | Re-theme Login to Singer's confirmed Corporate Identity (`#E50040`/`#A80331` red accent, sourced from the live `singerthai.co.th` site) | Frontend + Docs (PRD Resolved Q39, Brand Style Guide) |
| [#25](https://github.com/boonthepkstl-alt/stl_asset_service/pull/25) | 2026-08-24 | `frontend/singer-ci-shell-logo` | Extend the Singer CI logo mark (`RaiseMark`) to the app shell sidebar | Frontend |
| [#26](https://github.com/boonthepkstl-alt/stl_asset_service/pull/26) | 2026-08-24 | `frontend/login-panel-revert-to-blue` | Revert Login illustration panel from black back to blue, keeping Singer-red accents | Frontend |
| [#27](https://github.com/boonthepkstl-alt/stl_asset_service/pull/27) | 2026-08-24 | `frontend/dashboard-singer-ci-kpi` | Add Singer CI accent to the Software Licenses KPI card only (Total Assets left blue to avoid colliding with the adjacent Expired Warranty alert card) | Frontend |
| [#28](https://github.com/boonthepkstl-alt/stl_asset_service/pull/28) | 2026-08-25 | `docs/timeline-checkpoints-18-27` | Add PR #18-27 checkpoint links to `PROJECT-TIMELINE.md` | Docs |
| [#29](https://github.com/boonthepkstl-alt/stl_asset_service/pull/29) | 2026-08-25 | `feature/qr-barcode-identification` | **Implement QR/Barcode Identification** (`RAISE-FR-OPS-001`) — real scannable QR generation, working Scan QR lookup-and-navigate flow, dual id/code lookup (frontend mock repo + backend SQL) | Backend + Frontend |
| [#30](https://github.com/boonthepkstl-alt/stl_asset_service/pull/30) | 2026-08-25 | `docs/qr-barcode-closeout` | Close out QR/Barcode session per `SESSION-CLOSEOUT-PROTOCOL.md` — checkpoint, backfilled PR #18-29 log rows, status/timeline/changelog updates | Docs |
| [#31](https://github.com/boonthepkstl-alt/stl_asset_service/pull/31) | 2026-08-25 | `feature/audit-log-first-cut` | **Implement Audit Log first cut** (`RAISE-FR-AUDIT-001`) — new `audit_logs` table/model/repository/service/controller (read-only, no update/delete path), recording wired into Asset create/assign/check-in, real Audit tab on Asset Detail replacing the old fake static list | Backend + Frontend |
| [#32](https://github.com/boonthepkstl-alt/stl_asset_service/pull/32) | 2026-08-25 | `docs/audit-log-closeout` | Close out Audit Log session per `SESSION-CLOSEOUT-PROTOCOL.md` — checkpoint, status/timeline/changelog updates, `NEXT-STEP.md` recalculation | Docs |
| [#33](https://github.com/boonthepkstl-alt/stl_asset_service/pull/33) | 2026-08-25 | `feature/executive-dashboard-kpi-backend` | **Move Executive Dashboard KPI computation to backend** (`RAISE-FR-EXEC-001`) — new `GET /dashboard/stats` composing over the existing Asset domain (status counts, expired warranty, department/type distribution); no NBV/Risk formula added, none of that PRD-open math existed before or exists now | Backend + Frontend |
| [#34](https://github.com/boonthepkstl-alt/stl_asset_service/pull/34) | 2026-08-25 | `docs/executive-dashboard-closeout` | Close out Executive Dashboard KPI session per `SESSION-CLOSEOUT-PROTOCOL.md` — checkpoint, status/timeline/changelog updates, `NEXT-STEP.md` recalculation | Docs |
| [#35](https://github.com/boonthepkstl-alt/stl_asset_service/pull/35) | 2026-08-25 | `feature/ticket-domain-audit-hookin` | **Extend Audit Log to the Ticket domain** (`RAISE-FR-AUDIT-001`) — wires `AuditService.Record` into `CreateTicket`/`DecideApproval`/`Dispatch`/`UpdateExecutionStatus` (backend) and the equivalent `MockTicketRepository` methods (frontend), closing PR #31's deferred Ticket-domain gap | Backend + Frontend |
| [#36](https://github.com/boonthepkstl-alt/stl_asset_service/pull/36) | 2026-08-25 | `docs/ticket-audit-closeout` | Close out Ticket-domain audit hook-in session per `SESSION-CLOSEOUT-PROTOCOL.md`, plus backfill Level 1 checkpoints for PR #19-28 (closing F-20 as R-04) | Docs |
| [#37](https://github.com/boonthepkstl-alt/stl_asset_service/pull/37) | 2026-08-26 | `docs/tc-execution-ops-audit-exec` | **Run formal test case execution** for `TS-OPS-001`/`TS-AUDIT-001`/`TS-EXEC-001` (8 test cases) against the real running app — updates `RAISE-TRACEABILITY-MATRIX.md` with real evidence-based Test Status, corrects two stale "no code exists yet" passages, finds two new confirmed defects (F-21, F-22) | Docs |
| [#38](https://github.com/boonthepkstl-alt/stl_asset_service/pull/38) | 2026-08-26 | `frontend/fix-scan-qr-invalid-code-state` | **Fix F-21** — Scan QR now shows a distinct "invalid code" message for malformed input, separate from "not found" (`AC-OPS-001-03`); `RAISE-FR-OPS-001` now `PASS` on all 3 test cases | Frontend |
| [#39](https://github.com/boonthepkstl-alt/stl_asset_service/pull/39) | 2026-08-26 | `docs/tc-execution-asset-registry-detail-custody` | **Run formal test case execution** for `TS-ASSET-001`/`TS-ASSET-001-DETAIL`/`TS-ASSET-002`/`TS-ASSET-003` (11 test cases) against the real running app — finds 4 new confirmed defects (F-23: no Category filter, F-24: Asset Detail missing Financial/Lifecycle sections, F-25: no Category & Hierarchy screen, F-26: Custody History not append-only). A second commit on this same branch then **fixes F-23** — adds a working Category filter to the Assets page's Filters panel, wired into the mock repository (not the HTTP one, since `go-template-main`'s `GET /assets` has no category query param yet); `RAISE-FR-ASSET-001` now only fails on the still-open F-24. *(Row content updated 2026-08-26 to reflect the second commit.)* | Docs + Frontend |
| [#40](https://github.com/boonthepkstl-alt/stl_asset_service/pull/40) | 2026-08-27 | `frontend/fix-asset-detail-financial-lifecycle` | **Fix F-24** — Asset Detail now has "Financial" (Purchase Cost/Current Value/Purchase Date) and "Lifecycle" (a connectivity summary linking to Custody/Warranty/Maintenance/Audit) sections, the last 2 of the 9 sections `AC-ASSET-001-D-01` requires; `RAISE-FR-ASSET-001` now `PASS` on all 6 test cases. | Frontend |
| [#41](https://github.com/boonthepkstl-alt/stl_asset_service/pull/41) | 2026-08-27 | `frontend/asset-detail-lifecycle-review-fixes` | **Code review fixes for PR #40** — Procurement/Warranty rows in Asset Detail's Lifecycle section no longer look clickable (were a dead `setTab('overview')` no-op, since that tab was already active); the warranty-expired check is now computed once (`isWarrantyExpired`) and reused by both the Lifecycle row and the pre-existing Warranty & Coverage badge, instead of being evaluated 3 separate times. | Frontend |
| [#42](https://github.com/boonthepkstl-alt/stl_asset_service/pull/42) | 2026-08-27 | `frontend/fix-custody-history-append-only` | **Fix F-26** — Asset Detail's History tab now renders from the same per-asset audit trail `RAISE-FR-AUDIT-001` already builds (append-only by construction) instead of deriving a single "current custody state" row; a Check-in no longer overwrites the prior custody entry. `RAISE-FR-ASSET-003` now `PASS` on all 3 test cases. | Frontend |
| [#43](https://github.com/boonthepkstl-alt/stl_asset_service/pull/43) | 2026-08-28 | `frontend/category-hierarchy-first-cut` | **Fix F-25** — adds a scoped-down first-cut Category & Hierarchy screen (`/categories`) that lists each category with a live asset count and expands to show the real assets registered under it, linking through to Asset Detail. Deliberately does not build Prototype P-005's illustrative sub-category tree (tracked separately as new finding F-27). `RAISE-FR-ASSET-002` now `PASS (scoped)` on `TC-ASSET-002-01`. | Frontend |
| [#44](https://github.com/boonthepkstl-alt/stl_asset_service/pull/44) | 2026-08-28 | `frontend/category-hierarchy-fold-into-assets` | **Fold Category & Hierarchy into Asset Management** — per user request, removes the standalone `/categories` page and sidebar nav entry added in PR #43, replacing it with a "By Category" tab inside the existing Asset Management page (same content/behavior: category → its real assets, click-through to Asset Detail). No requirement, AC, or Test Status change — a UX relocation, not a defect fix. | Frontend |
| [#45](https://github.com/boonthepkstl-alt/stl_asset_service/pull/45) | 2026-08-28 | `docs/tc-execution-ops002-maint001` | **Run formal test case execution** for `TS-OPS-002`/`TS-MAINT-001` (12 test cases) against the real running app — both requirements previously carried only a pre-code-era `BLOCKED` guess, never re-verified. `RAISE-FR-OPS-002` now `PASS` on all 3 test cases; `RAISE-FR-MAINT-001` is `FAIL (partial)` (7 of 9 pass), finding 2 new confirmed defects (F-28: Maintenance record list missing date/cost fields, F-29: stage-progress indicator doesn't distinguish Current from Pending). | Docs |
| [#46](https://github.com/boonthepkstl-alt/stl_asset_service/pull/46) | 2026-08-28 | `frontend/fix-maintenance-record-date-cost` | **Fix F-28** — each maintenance record on Asset Detail's Maintenance & Tickets tab now shows its created date and cost (preferring actual cost once known, falling back to the dispatch-time estimate, or an honest "—" if not yet dispatched); `RAISE-FR-MAINT-001` now `PASS` on `TC-MAINT-001-01` (8 of 9 test cases pass — F-29 remains open). | Frontend |
| [#47](https://github.com/boonthepkstl-alt/stl_asset_service/pull/47) | 2026-08-28 | `frontend/fix-governance-current-stage-indicator` | **Fix F-29** — Ticket Detail's 4-stage governance indicator now derives the current stage from `ticket.status` and renders it with a distinct brand-colored circle, ring, and "Current" badge, instead of looking identical to a not-yet-reached stage; `RAISE-FR-MAINT-001` now `PASS` on all 9 test cases. | Frontend |
| [#48](https://github.com/boonthepkstl-alt/stl_asset_service/pull/48) | 2026-08-29 | `docs/tc-execution-login` | **Run formal test case execution** for `TS-LOGIN` (3 test cases) against the real running app. `TC-LOGIN-03` (access-denied for an unauthorized area) **PASS**. `TC-LOGIN-01`/`-02` (valid/invalid login) **BLOCKED** — `auth-service.ts` has no mock fallback, unlike Asset/Employee/Ticket, and no backend/database is reachable in this environment (`ERR_CONNECTION_REFUSED`); new infrastructure finding **F-30**. | Docs |
| [#49](https://github.com/boonthepkstl-alt/stl_asset_service/pull/49) | 2026-08-29 | `docs/resolve-warranty-field-list` | **Resolve F-01** — the Warranty field-list open question (PRD §16 Q15) is closed: user confirmed `warrantyExpiry` is the only MVP field, rejecting a draft 8-field proposal. Recorded as PRD §16 Resolved Question 40 and propagated through the full deliverable chain (`/update-prd` + `/run-full-chain`) — Design §5.2, Prototype P-010, Acceptance Criteria AC-WARRANTY-001, Test Plan TS-WARRANTY-001, Test Cases TC-WARRANTY-001-01/-02, Traceability Matrix Gap 7. No code change — a requirements resolution, not a defect fix. | Docs |
| [#50](https://github.com/boonthepkstl-alt/stl_asset_service/pull/50) | 2026-08-29 | `docs/tc-execution-warranty-and-asset-registry-column` | **Implement F-01's resolved Warranty field** — per explicit user direction, declines a standalone P-010 screen; adds a "Warranty" column to the Assets Registry list instead (expiry date + Active/Expired badge, sortable). `RAISE-FR-WARRANTY-001` now `PASS (partial)` — `TC-WARRANTY-001-01/-02` pass; `TC-WARRANTY-001-03`'s separate 90-day-window question remains open. *(Row added before the PR is confirmed merged, per this file's PR-prediction convention; verify against `gh pr view 50` before treating it as final.)* | Frontend |
| [#51](https://github.com/boonthepkstl-alt/stl_asset_service/pull/51) | 2026-08-29 | `docs/tc-execution-dash` | **Run formal test case execution** for `TS-DASH` (Main Dashboard, 3 test cases) against the real running app — all 3 **FAIL**: NBV/Risk tiles absent, "Warranty Expiry" mislabeled "Expired Warranty", no "Asset by Category"/"Lifecycle / Maintenance Overview"/"Recent Alerts" sections. Prototype P-002's tile/section spec is word-for-word identical to P-014's (already tested via `TC-EXEC-001`) and both map to the same single built page (`frontend/src/pages/Dashboard/index.tsx`) — **broadens finding F-22** rather than minting a new finding number. No code change. | Docs |
| [#52](https://github.com/boonthepkstl-alt/stl_asset_service/pull/52) | 2026-08-29 | `docs/tc-execution-oracle` | **Run formal test case execution** for `TS-ORACLE-001` (Oracle FA / Financial View, 4 test cases) against the real running app — all 4 **FAIL**: the `/reconciliation` route maps `RAISE-FR-ORACLE-001` to a generic "foundation placeholder" `EmptyState` (`ModulePage`), with no Asset Number/NBV/Depreciation/Oracle Source/Sync Status fields and no data-unavailable/error/conflict states at all. New finding **F-31** (distinct from F-04's integration-mechanism gap — this is a build gap, confirmed by execution). No code change. | Docs |
| [#53](https://github.com/boonthepkstl-alt/stl_asset_service/pull/53) | 2026-08-29 | `docs/tc-execution-alert` | **Run formal test case execution** for `TS-ALERT-001` (Alerts, P-012, 2 test cases) against the real running app — both **FAIL**: the "Notification Center" route (`/notifications`) renders the app's generic 404 page, not even a placeholder stub; the header bell-icon dropdown is hardcoded empty with a static "later phase" message. New finding **F-32** (distinct from F-05's trigger-rule gap — worse than F-31's Oracle FA placeholder, this route is entirely unbuilt). No code change. | Docs |
| [#54](https://github.com/boonthepkstl-alt/stl_asset_service/pull/54) | 2026-08-29 | `docs/tc-execution-ai-search` | **Run formal test case execution** for `TS-AI-SEARCH-001` (Natural Language Search, P-015, 3 test cases) and `TS-AI-STATES` (AI Response States, P-015 §22, 5 test cases) against the real running app — all 8 **FAIL**: the header "AI Assistant" drawer accepts no input at all (static placeholder only); the Assets page's "Ask AI" box is a hardcoded keyword-to-filter matcher, not a natural-language answer engine — no "Sources / Data Used" section, no affected-asset count, no Asset/Warranty/Age/Maintenance/Status table for the PRD's illustrative 90-day question, and none of the 5 required response-state messages exist anywhere in source. New finding **F-33** (distinct from F-06's citation-format gap — a build gap, confirmed by execution); `TS-AI-STATES` broadens the same finding rather than opening a new one, since both suites trace to the same two surfaces and root cause. **Completes formal execution of every suite in `RAISE-TEST-CASES.md` at least once.** No code change. | Docs |
| [#55](https://github.com/boonthepkstl-alt/stl_asset_service/pull/55) | 2026-08-31 | `frontend/dedupe-warranty-expired-check` | **Fix a `/code-review` finding on PR #50** — the "is warranty expired" check (`new Date(warrantyExpiry) < now`) was independently duplicated in 3 places (Assets list Warranty column, Asset Detail's Warranty & Coverage section, dashboard-repository's expired-warranty count). Extracts a shared `isWarrantyExpired(warrantyExpiry, asOf?)` helper (`frontend/src/lib/warranty.ts`) and imports it at all 3 call sites — no behavior change, 144/144 tests still pass. | Frontend |
| [#56](https://github.com/boonthepkstl-alt/stl_asset_service/pull/56) | 2026-08-31 | `docs/resolve-f22-dashboard-spec-correction` | **Propagate F-22's business decision** ("แก้ Prototype ให้ตรงกับของจริง") through the full deliverable chain — `RAISE-DESIGN.md` §13 (v0.8→0.9), `RAISE-PROTOTYPE.md` P-002/P-014 (v0.7→0.8), `RAISE-ACCEPTANCE-CRITERIA.md` AC-DASH/AC-EXEC-001 (v0.6→0.7), `RAISE-TEST-PLAN.md` TS-DASH/TS-EXEC-001 (v0.6→0.7), `RAISE-TEST-CASES.md` TC-DASH-01..03/TC-EXEC-001-01..02 (v0.6→0.7), and `RAISE-TRACEABILITY-MATRIX.md` (v0.6→0.7, new Gap 8). All 5 upstream documents now describe the actually-shipped dashboard (8-tile KPI grid, 10-section list) instead of the never-built NBV/Risk/Utilization wireframe; NBV/Risk/Utilization is explicitly retained as a documented, not-yet-scheduled enhancement (F-03), not deleted. **Not marked Resolved at merge time** — this was a documentation correction, not a fresh test execution; `RAISE-FR-EXEC-001`'s Test Status was `NOT_TESTED (re-derived; formal re-execution pending)`. Re-verified same day, see PR #57. No code change. | Docs |
| [#57](https://github.com/boonthepkstl-alt/stl_asset_service/pull/57) | 2026-08-31 | `docs/tc-execution-dash-reverify` | **Re-execute `TC-DASH-01..03`/`TC-EXEC-001-01..02`** against the real running app after F-22's spec correction (PR #56) — all **PASS**: all 8 KPI tiles and all 10 sections confirmed present via real page text (`/dashboard`, page title literally "Executive Dashboard"). `TC-DASH-03` (NBV/Risk/Utilization absence-check) **PASS** on the absence-check itself; the deeper "should they be added" question stays BLOCKED (partial), tied to F-03. Closes Traceability Matrix Gap 8 (v0.7→0.8) and marks `OPEN-FINDINGS.md` F-22 **Resolved (R-13)**. No code change. | Docs |
| [#58](https://github.com/boonthepkstl-alt/stl_asset_service/pull/58) | 2026-09-01 | `frontend/resolve-f27-category-type-hierarchy` | **Resolve F-27** — per confirmed business decision, sub-category = the existing Asset `type` field (2-level Category → Type → assets, no new field/data model). Propagated via `/run-full-chain`: `RAISE-PROTOTYPE.md` P-005 (v0.9), `RAISE-ACCEPTANCE-CRITERIA.md` AC-ASSET-002 (v0.8, new AC-ASSET-002-03), `RAISE-TEST-PLAN.md`/`RAISE-TEST-CASES.md` (v0.8/v0.9). Implemented same day: extended the Assets page's "By Category" view one level deeper (`frontend/src/pages/Assets/index.tsx`) — expanding a category reveals its real Type sub-groups, expanding a Type reveals its individual assets. Re-executed against the real app: all cases **PASS**; 3 automated tests (1 new, 1 rewritten), full suite 145/145, `tsc`/lint clean. `RAISE-TRACEABILITY-MATRIX.md` reaches **v1.0** (Gap 9 closed — the last open gap). `OPEN-FINDINGS.md` F-27 **Resolved (R-14)**. | Frontend + Docs |
| [#59](https://github.com/boonthepkstl-alt/stl_asset_service/pull/59) | 2026-09-01 | `frontend/resolve-f30-mock-auth` | **Resolve F-30** — per confirmed business decision, adds a `MockAuthRepository` following the established Mock/Http pattern (`frontend/src/services/auth-repository.ts`), gated by a new `AUTH_API_ENABLED` flag (default OFF, matching every other domain). 4 demo accounts, one per Role: `admin@raise.dev`/`manager@raise.dev`/`itstaff@raise.dev`/`employee@raise.dev`, all `demo1234`. New `auth-service.test.ts` (2 tests) passes; full suite 147/147 (was 145), `tsc`/lint clean. Live-verified through the real Login page UI (not a localStorage bypass): invalid credentials rejected, valid credentials log in and land on the Dashboard. `RAISE-TRACEABILITY-MATRIX.md` (v1.1) updated to PASS, Gap 10 closed (infrastructure half only — the separate PRD-content question, auth mechanism/role-matrix, remains open). `OPEN-FINDINGS.md` F-30 **Resolved (R-15)**. | Frontend + Docs |
| [#60](https://github.com/boonthepkstl-alt/stl_asset_service/pull/60) | 2026-09-01 | `docs/defer-f31-oracle-fa` | **Record decision on F-31** — presented 3 options for the unbuilt Oracle FA Financial View (P-011); user chose to explicitly defer until real Oracle FA integration lands, since (unlike F-22/F-27/F-30) there's no existing field/data to reuse without either fabricating data or pre-empting the still-open F-04 formula question. `OPEN-FINDINGS.md` F-31 updated to record the deferral — remains Open, not Resolved (nothing was built). No code change. | Docs |
| [#61](https://github.com/boonthepkstl-alt/stl_asset_service/pull/61) | 2026-09-01 | `frontend/resolve-f32-alerts-screen` | **Resolve F-32** — per confirmed business decision, builds a scoped Alerts screen (`frontend/src/pages/Alerts/index.tsx`, new `ROUTES.NOTIFICATIONS`) that derives alerts from the one condition already confirmed elsewhere: expired warranty (`isWarrantyExpired`, same helper the Assets Warranty column and Dashboard already use). Severity rendered honestly as "Not yet defined" (not invented) since severity/trigger rules remain undefined (F-05, unaffected). `RAISE-ACCEPTANCE-CRITERIA.md` deliberately untouched — its existing scope already covered this. `RAISE-TEST-CASES.md` (v0.10) TC-ALERT-001-01/-02 both **PASS**; `RAISE-TRACEABILITY-MATRIX.md` (v1.2) updated to PASS (partial), Gap 11 closed. 2 new tests, full suite 149/149 (was 147), `tsc`/lint clean. Live-verified: `/notifications` shows 11 alert rows matching the Dashboard's "Expired Warranty" count exactly, asset links navigate correctly, no delivery-channel UI. `OPEN-FINDINGS.md` F-32 **Resolved (R-16)**. | Frontend + Docs |
| [#62](https://github.com/boonthepkstl-alt/stl_asset_service/pull/62) | 2026-09-01 | `docs/defer-f33-ai-assistant` | **Record decision on F-33** — presented 3 options for the AI Assistant (P-015, 8 test cases across `TS-AI-SEARCH-001`/`TS-AI-STATES`); user chose to explicitly defer until real AI backend integration lands, over building a scoped canned-answer engine for the PRD's illustrative example question. `OPEN-FINDINGS.md` F-33 updated to record the deferral — remains Open, not Resolved (nothing was built). No code change. *(Row added before the PR is confirmed merged; verify against `gh pr view 62` before treating it as final.)* | Docs |
| [#63](https://github.com/boonthepkstl-alt/stl_asset_service/pull/63) | 2026-09-01 | `frontend/resolve-warranty-expiring-threshold` | **Resolve the Warranty "Expiring" threshold open question (R-17)** — per confirmed business decision, the threshold is per-Asset-Category configurable (not a single global constant), defaulting to 90 days for all 5 current categories, admin-adjustable via a new Settings screen. Recorded as PRD §16 Resolved Question 41 and propagated through the full deliverable chain: `RAISE-DESIGN.md` (v0.10, §5.2 3-state model + new §5.4 Settings Domain), `RAISE-PROTOTYPE.md` (v0.10, P-010 rewritten + new §23A P-018 Settings), `RAISE-ACCEPTANCE-CRITERIA.md` (v0.9, AC-WARRANTY-001-03 rewritten + new -04/-05/-06), `RAISE-TEST-PLAN.md` (v0.9, TS-WARRANTY-001 fully unblocked), `RAISE-TEST-CASES.md` (v0.11, 6 test cases). Implemented same day: `frontend/src/lib/warranty.ts`'s `getWarrantyStatus` 3-state function, new `WarrantySettings` (`types/settings.ts`, `services/settings-service.ts`/`settings-repository.ts`), the Warranty badge on Assets list (`pages/Assets/index.tsx`) and Asset Detail (`pages/AssetDetail/index.tsx`) updated to 3-state, and a new Settings "Warranty" section (`pages/Settings/index.tsx`). Live-verified: setting IT Hardware's threshold to 5000 flags only IT Hardware assets as "Expiring" on both Assets list and Asset Detail, while an unrelated Mobile-category expired asset stays "Expired" — no cross-category leakage. 2 new automated tests, full suite 151/151 (was 149), `tsc`/lint clean. `RAISE-TRACEABILITY-MATRIX.md` reaches **v1.3** — Gap 12 opened and closed same revision; Gap 13 opened, still OPEN (`TC-WARRANTY-001-06` non-admin-denial test written but not yet executed, tracked as **F-34**). `OPEN-FINDINGS.md` R-17 added; F-34 added. **Merged.** | Frontend + Docs |
| [#64](https://github.com/boonthepkstl-alt/stl_asset_service/pull/64) | 2026-09-01 | `frontend/execute-tc-warranty-001-06` | **Execute `TC-WARRANTY-001-06` (F-34/R-18)** — the last unexecuted Warranty test case, non-admin access/write denial to the new Settings (P-018) screen. Execution surfaced a real defect, not just a coverage gap: `frontend/src/App.tsx`'s `ROUTES.SETTINGS` was not actually gated to ADMIN, sitting in the general authenticated-user route block instead of the existing `<ProtectedRoute allowedRoles={['ADMIN']}>` block already used for Administration/User/Role Management. Fixed by moving Settings into that block — no new RBAC mechanism invented. 2 new tests added to `frontend/src/App.rbac.test.tsx`; full suite 153/153 (was 151), `tsc`/lint clean. Live-verified: an EMPLOYEE-role user hitting `/settings` sees the real "403 — Access denied" Forbidden page; an ADMIN-role user sees Settings render correctly. `RAISE-TEST-CASES.md` (v0.12) `TC-WARRANTY-001-06` now **PASS**; `RAISE-TRACEABILITY-MATRIX.md` reaches **v1.4** — Gap 13 closed, `RAISE-FR-WARRANTY-001` now a full unqualified **PASS** (all 6 test cases). `OPEN-FINDINGS.md` R-18 added. **Merged.** | Frontend + Docs |
| [#65](https://github.com/boonthepkstl-alt/stl_asset_service/pull/65) | 2026-09-01 | `docs/draft-compliance-review` | **Draft `RAISE-COMPLIANCE-REVIEW.md` v1.0** — the first artifact in the deliverable chain that consumes real source code rather than producing spec for it (`docs/11-compliance-review/`, new folder, mirroring the `08-architecture`/`09-api-db-spec`/`10-detailed-design` as-built-extension convention). Consolidates `RAISE-TRACEABILITY-MATRIX.md` v1.4 §3/§4 into a per-requirement verdict table (`PASS`/`PARTIAL`/`FAIL`/`BLOCKED`/`NOT_IMPLEMENTED`) — no new test execution performed, every verdict cites its source Traceability Matrix row. Summary: of 17 MVP-traceable requirements, 8 `PASS`, 1 `PASS (partial)`, 2 `FAIL` (`RAISE-FR-ORACLE-001`, `RAISE-AI-SEARCH-001` — both explicitly deferred by business decision, F-31/F-33, not silent defects), 1 `BLOCKED (partial)`, 1 `BLOCKED`, 4 `BLOCKED (full)` (the `RAISE-AI-DOC-*` group). Plus 2 cross-cutting items (`RAISE-NFR-SEC-RBAC-001` PASS, Dashboard/Navigation PASS (partial)). No code change. **Merged.** | Docs |
| [#66](https://github.com/boonthepkstl-alt/stl_asset_service/pull/66) | 2026-09-01 | `docs/recalculate-next-step` | **Recalculate `NEXT-STEP.md`** after PRs #63–#65 shipped — every buildable-now engineering item is done; Primary Next Step becomes a business check-in on which Blocking finding (F-02–F-09) to resolve next, per `RAISE-COMPLIANCE-REVIEW.md`'s findings. Docs-only, no code change. **Merged.** | Docs |
| [#67](https://github.com/boonthepkstl-alt/stl_asset_service/pull/67) | 2026-09-01 | `docs/resolve-f02-checkin-checkout` | **Resolve Open Finding F-02** — Check-in/Check-out workflow shape, permission gate, and Custody holder data model (PRD §16 Q11–Q13). User confirmed all three recommended options via `AskUserQuestion`, each matching already-built, already-tested behavior exactly: (1) immediate state-change, no approval workflow (deliberately simpler than `RAISE-FR-MAINT-001`'s 4-stage workflow); (2) any authenticated user, no role restriction; (3) direct 1:1 Employee link for the holder model. **No code change required.** Recorded as PRD §16 Resolved Question 42 and propagated through the full chain: `RAISE-DESIGN.md` (v0.11, §4.2), `RAISE-PROTOTYPE.md` (v0.12, P-008/P-006 — a v0.11 drafting pass briefly and incorrectly over-resolved a separate, unconfirmed question (Custody History write-path exclusivity, F-10); caught and corrected same-session in v0.12, restoring F-10 to genuinely open), `RAISE-ACCEPTANCE-CRITERIA.md` (v0.10, AC-OPS-002-01/-02 rewritten to state the confirmed rule directly), `RAISE-TEST-PLAN.md` (v0.10, TS-OPS-002 fully unblocked), `RAISE-TEST-CASES.md` (v0.13, `TC-OPS-002-01..03` reclassified from partially-blocked to fully PASS, reusing the existing 2026-08-28 execution evidence — no new test run needed or claimed). `RAISE-TRACEABILITY-MATRIX.md` reaches **v1.5** — Gap 14 opened and closed same revision. `OPEN-FINDINGS.md` R-19 added; F-02 removed from the Blocking table. **Explicitly unaffected:** Open Finding F-10 (Custody History write-path exclusivity) and Open Finding F-08 (general RBAC role/permission-matrix content for other domains) both remain genuinely open. *(Row added before the PR is confirmed merged; verify against `gh pr view 67` before treating it as final.)* | Docs |
| [#68](https://github.com/boonthepkstl-alt/stl_asset_service/pull/68) | 2026-09-01 | `infra/docker-compose-3-container-stack` | **Add local 3-container Docker infrastructure** (F-13/F-14) — `docker-compose.yml` (root) orchestrating `frontend`/`backend`/`db`, plus `frontend/Dockerfile` (Node 20 → nginx multi-stage), `go-template-main/Dockerfile` (Go 1.23 multi-stage), `frontend/nginx.conf`, both `.dockerignore` files, `docker.env.example`, and `DOCKER.md`. The `db` service (`postgres:16-alpine`) auto-applies `sql/pg/V0..V4` via its init-script mechanism on first run against an empty volume. Verified end-to-end via a real `docker compose up --build`: migrations applied (confirmed via `\dt`), backend login (`admin`/`password`) issued a valid JWT, that token successfully queried a DB-backed endpoint, CORS preflight from the frontend origin passed, and the frontend served correctly. **Found and fixed a real bug during verification**: the initial `nginx.conf` (`try_files $uri $uri/ /index.html`) 301-redirected the app's own `/assets` client-side route (Asset Registry) into Vite's build-output `assets/` directory (hashed JS/CSS chunks) — same name, real collision. Fixed by dropping the `$uri/` directory-match fallback. Does **not** resolve F-13 (production hosting target) or F-14 (CI/CD pipeline) — both remain genuinely open; this is local dev/demo tooling only, documented as such in `DOCKER.md`'s scope note. No app code was modified. | Infra + Docs |
| [#69](https://github.com/boonthepkstl-alt/stl_asset_service/pull/69) | 2026-09-02 | `infra/fix-docker-real-backend-and-dashboard-null-guard` | **Fix the Docker stack to actually exercise the real backend, plus a real dashboard bug found doing so.** Found while live-testing `admin`/`password` login through the running stack: (1) `frontend/Dockerfile` was built without any `VITE_*_API_ENABLED` flag, so it silently ran on in-memory mock data — added all 6 flags as build args (default `true`), passed through `docker-compose.yml`, documented in `docker.env.example`. Login now genuinely calls the real `go-template-main` backend (verified via network log). (2) With real API calls wired up, the Executive Dashboard crashed against an empty (unseeded) database — Go marshals a nil slice as JSON `null`, not `[]`, and `HttpDashboardRepository.getAssetStats()` trusted the TS type's claim of non-null arrays without guarding; `Dashboard/index.tsx`'s `.map()` calls threw. Fixed with a null-to-`[]` normalization in the repository, plus 1 new regression test. (3) Also hardened `frontend/nginx.conf` with an explicit no-cache header on `index.html`, since a rebuilt container's new JS hash could otherwise stay invisible behind a browser's cached copy of the previous `index.html`. Full suite 154/154 (was 153), `tsc`/lint clean. Live-verified end-to-end via the real browser: login succeeds as "Template Admin"/ADMIN (the real backend's demo user), Dashboard renders all "0" KPI tiles correctly against the empty DB, no crash, no console errors. **Merged.** | Frontend + Infra |
| [#70](https://github.com/boonthepkstl-alt/stl_asset_service/pull/70) | 2026-09-02 | `fix/code-review-docker-port-derivation-and-null-guards` | **`/code-review max --fix` on the Docker infra diff (PRs #68-#69).** 8 finder angles + 8 verify passes produced 6 findings, 5 fixed: (1)/(2) `docker-compose.yml`'s `CORS_ALLOW_ORIGINS`/`VITE_API_BASE_URL` now derive their defaults from `FRONTEND_PORT`/`BACKEND_PORT` via nested compose interpolation instead of two independently-hardcoded literals that could silently drift apart if a user changed one port without the other; `docker.env.example` updated to match (override lines now commented out by default so the new derivation actually applies when the file is copied verbatim). (3) `DOCKER.md` now documents that the stack is only reachable from the Docker host machine, not other machines on the LAN — `VITE_API_BASE_URL` bakes in an absolute `localhost` URL and `nginx.conf` has no `/api` reverse proxy. (4) `frontend/Dockerfile`: removed 8 redundant `ENV VAR=$VAR` lines mirroring `ARG` declarations — Docker already exposes ARG values to same-stage RUN instructions, and the build stage's environment is discarded anyway. (6) `frontend/src/services/dashboard-repository.ts`: added a whole-object null guard (`response.data ?? {}`) on top of the existing per-field array guard, defensive against a 2xx-with-empty-body response the current Go handler doesn't produce today but nothing guarantees it never will; 1 new regression test. (5) One finding — `frontend`'s `depends_on: backend` lacking a health condition, since `backend` has no healthcheck at all — was deliberately **not** auto-fixed: an incorrect healthcheck risked making `docker compose up` hang forever, a worse failure mode than the narrow, self-healing race window it would close. Full suite 155/155 (was 154), `tsc`/lint clean (had to restructure the null-guard from spread+override to explicit `?? 0`/`?? []` per field after `tsc` flagged the spread pattern as TS2783). Live re-verified: `docker compose config` confirms the new interpolation resolves correctly with and without overrides; full Docker rebuild + browser re-check shows no regression (login, CORS, dashboard all still work). | Frontend + Infra |
| [#71](https://github.com/boonthepkstl-alt/stl_asset_service/pull/71) | 2026-09-02 | `fix/dashboard-expired-warranty-date-parsing` | **Fix `ExpiredWarranty` always reporting 0** — found while seeding 10 demo assets into the local Docker stack's Postgres to populate the dashboard. Root cause: `dashboardService.go`'s `GetDashboardStats()` parsed `WarrantyExpiry` with the strict layout `"2006-01-02"`, but `lib/pq` actually scans a Postgres `date` column into a Go string as RFC3339 (`"2025-06-10T00:00:00Z"`) — the parse silently failed for every real row (existing unit tests used bare-date fixtures, which never exercised the real driver's actual wire format, so this pre-existing bug went uncaught). Fixed with a new `parseAssetDate` helper tolerating both layouts, plus 1 new regression test. `go build`/`go vet`/`go test ./...` all pass. Live-verified against the real seeded stack: `expiredWarranty` went from `0` to `7`, matching manual counting of the 10 seed assets' warranty dates against today's date exactly; confirmed visually in the browser. The 10 seed asset rows themselves are ephemeral (inserted directly into the running container, not a committed migration). | Backend |
| [#72](https://github.com/boonthepkstl-alt/stl_asset_service/pull/72) | 2026-09-02 | `feature/it-hardware-handover-approval-workflow` | **Add IT Hardware Assignment Approval Workflow** (`RAISE-FR-OPS-002` category-scoped exception, PRD §16 Resolved Question 43) — narrows, does not reverse, Resolved Question 42's general no-approval Check-in/Check-out rule. A real 4-stage approval workflow now gates assigning **IT Hardware category assets only**: Initiation (IT/Admin clicks Assign, unchanged trigger) → Recipient Confirmation (the assigned employee confirms receipt themselves, merging the source paper form's recipient + recipient-supervisor signatures into one digital step, a deliberate business simplification) → IT Processing (`IT_STAFF`) → IT Supervisor Approval (`IT_MANAGER`) — only the final stage flips `Asset.status` to `"Assigned"` (no new status value). Rejection at Stage 3 or 4 returns the asset immediately to `"Available"`, terminal. Every other category and all Check-in are unaffected — verified via regression test. Backend only (`go-template-main`): new Asset Handover domain (`model`/`repository`/`service`/`controller` + `V5__AssetHandovers_Table.sql`), 6 new routes (`GET /handovers`, `GET /handovers/:code`, `POST /assets/:id/handover`, `POST /handovers/:code/{confirm,process,decision}`), `POST /assets/:id/assign` now returns `409` + a `nextStep` hint for IT Hardware assets instead of assigning immediately. 24 new unit tests, `go build`/`vet`/`test` clean, `gofmt` clean. A self-initiated code-review pass (5 parallel reviewer agents, 8 finder angles, 8 candidates) fixed 3 real issues found in the new code before merge: an empty-recipient validation gap that let Stage 2's identity check be bypassed, a handover-code sequence number that wasn't year-scoped despite its `AHO-<year>-<seq>` format, and a validation error incorrectly returning HTTP 500 instead of 400 — each re-verified live after the fix. 5 lower-severity findings (a count-then-insert race on the sequence number, a non-atomic two-write at Stage 4 approval, a TOCTOU gap on asset state at approval time, error-masking consistent with `AssignAsset`'s pre-existing convention, a cosmetic hardcoded route-string hint) were deliberately left as documented residual risks with inline code comments, not fixed. Live-verified end-to-end against the real running Docker stack (backend + Postgres): applied the new migration, rebuilt/restarted the backend, confirmed via `curl` all 4 stages, both terminal rejection paths, list/filter, and the non-IT-Hardware regression guard. Full RAISE deliverable chain synced (PRD v0.14 → Design v0.12 → Prototype v0.13 → AC v0.11 → Test Plan v0.11 → Test Cases v0.15 → Traceability Matrix v1.7, Gap 15 closed). **Frontend UI for this workflow does not exist yet** (no "My Pending Assignments"/`IT_STAFF`/`IT_MANAGER` queue surfaces) — a distinct, not-yet-started follow-up. Backend RBAC enforcement for `IT_STAFF`/`IT_MANAGER`, the Stage 2 e-signature/acknowledgment-text question (user explicitly dismissed this session), and the Stage 2 recipient-decline path all remain genuinely open, not decided. Session close-out recorded as `CHECKPOINT-2026-09-02-004`. **Merged.** | Backend + Docs |
| [#73](https://github.com/boonthepkstl-alt/stl_asset_service/pull/73) | 2026-09-02 | `docs/it-hardware-handover-closeout` | **Docs-only session close-out for PR #72** — adds PR #72's `DEVELOPMENT-LOG.md` row and `CHANGELOG.md` entry (deferred until #72 was confirmed merged, per this project's own convention that those two files only get entries for **merged** PRs) and updates `CHECKPOINT-2026-09-02-004`'s Git section from `pending` to the real merged commit/branch. No code change. Recorded retroactively as `CHECKPOINT-2026-09-02-005` in this same close-out pass — this PR itself had no `DEVELOPMENT-LOG.md` row or Task Checkpoint until now, despite established convention giving every prior pure-docs-closeout PR its own row (e.g. #30, #32, #34, #36, #66). **Merged.** | Docs |
| [#74](https://github.com/boonthepkstl-alt/stl_asset_service/pull/74) | 2026-09-02 | `feature/it-hardware-handover-workflow-frontend` | **Add frontend UI for the IT Hardware Assignment Approval Workflow** (backend was PR #72) — `types/handover.ts`, `services/handover-repository.ts` (Mock+Http, mirrors `ticket-repository.ts`), `services/handover-service.ts`, `hooks/useHandover(s).ts`, three new pages at the time (`MyPendingAssignments`, `ITProcessingQueue`, `ITSupervisorApprovalQueue` — later deleted and replaced by PR #76's consolidated page) plus `HandoverDetail` (a 4-stage governance indicator mirroring `TicketDetail`'s pattern). `AssetDetail`'s existing Assign button now intercepts IT Hardware category assets and routes through this flow instead of assigning immediately; every other category unaffected (regression-tested). New routes role-gated client-side (`IT_STAFF`/`ADMIN` for the processing queue, `IT_MANAGER`/`ADMIN` for the approval queue) via the existing `ProtectedRoute` pattern — UI-only, no backend RBAC enforcement (consistent with this codebase's pre-existing MVP decision). A self-initiated code-review pass (5 parallel reviewer agents, 8 finder angles) found and fixed 3 real bugs before merge: (1) the mock repository's Approve action never actually completed the underlying asset assignment (the default, `HANDOVER_API_ENABLED=false` demo path silently left assets stuck "Available" forever despite the handover record showing "Assigned") — fixed in `handoverService.decideHandover` to complete the assignment itself when mocked, mirroring the real backend's `CompleteHandoverAssignment`; (2) the pending-handover badge/quick-action on Asset Detail was category-blind — fixed with defense-in-depth; (3) the Custody Lifecycle row on Asset Detail said "Currently Available" while a handover was actively pending, contradicting the header badge on the same page — fixed to show "Assignment Pending — {recipient}". 4 lower-severity findings (name-only recipient matching between the User/auth model and Employee/recipient model — no `employeeId` link exists anywhere in this codebase, a documented accepted MVP limitation; no double-submit guard on queue row actions, mitigated by the backend's own stage guard; a `GovernanceStep` component duplicated from `TicketDetail` instead of shared; unformatted raw timestamps) were left as documented residual risk, not fixed. 47 test files / 196 tests passing at merge time, `tsc`/lint clean. Live-verified end-to-end against the real running Docker stack through the actual UI (not just API calls): all 4 stages via real button clicks, both rejection paths with correct stage-attribution in the governance indicator, the non-IT-Hardware regression guard. Session close-out recorded as `CHECKPOINT-2026-09-02-006`. **Merged.** | Frontend |
| [#75](https://github.com/boonthepkstl-alt/stl_asset_service/pull/75) | 2026-09-03 | `docs/it-hardware-handover-frontend-closeout` | **Sync deliverable chain: IT Hardware handover frontend now shipped** — updated `RAISE-TEST-CASES.md` (v0.15 → v0.16) and `RAISE-TRACEABILITY-MATRIX.md` (v1.7 → v1.8) to remove the "backend/API-level scope only, no frontend UI yet" caveat on `TC-OPS-002-04..09`/`RAISE-FR-OPS-002`, now that PR #74's frontend shipped and was live-verified through the real UI. Still-open items (backend RBAC enforcement, Stage 2 e-signature, Stage 2 recipient-decline path, name-string-based recipient matching) explicitly preserved as unresolved. No code change. Recorded retroactively as `CHECKPOINT-2026-09-03-001` in this same close-out pass — this PR also had no `DEVELOPMENT-LOG.md` row or Task Checkpoint until now, the same gap as PR #73. **Merged.** | Docs |
| [#76](https://github.com/boonthepkstl-alt/stl_asset_service/pull/76) | 2026-09-03 | `frontend/consolidate-handover-nav-into-single-page` | **Consolidate 3 handover nav items into one "Asset Handovers" page** — a navigation/IA refactor requested directly by the user after reviewing a screenshot of the sidebar: 3 separate top-level nav items (My Pending Assignments / IT Processing Queue / IT Supervisor Approval Queue) for one workflow's different stages/actors was bad IA. Follows this codebase's own established precedent — "IT Requisition & Maintenance" (the Ticket/Maintenance domain) is also a multi-stage, multi-actor workflow but is a single nav item/page, not one per stage. Consolidated the 3 pages into one new page `frontend/src/pages/Handovers/index.tsx` with role-aware tabs (My Pending for everyone; IT Processing for `IT_STAFF`/`ADMIN`; Supervisor Approval for `IT_MANAGER`/`ADMIN`; default active tab matches the user's primary role). Deleted the 3 old page folders. Collapsed `ROUTES.MY_PENDING_ASSIGNMENTS`/`ROUTES.IT_PROCESSING_QUEUE`/`ROUTES.IT_SUPERVISOR_APPROVAL_QUEUE` into one `ROUTES.HANDOVERS = '/handovers'`; `HANDOVER_DETAIL` route unchanged. Collapsed 3 nav items into one "Asset Handovers" nav item, positioned next to "IT Requisition & Maintenance" in the Assets group. Updated `HandoverDetail`'s hardcoded back-references from the old `my-pending-assignments` id/path to the new `handovers` one. Updated `App.rbac.test.tsx` (removed 3 route-level role-gating tests since role-gating is now a page-level tab-visibility concern, added a test confirming `/handovers` is reachable by every authenticated role). 45 test files / 205 tests passing (net -2 files / +9 tests vs. pre-consolidation), `tsc`/lint clean. Live-verified against the real Docker stack: sidebar now shows exactly one "Asset Handovers" entry; tabs switch correctly with per-tab counts. No backend/API change — frontend-only IA refactor. Session close-out recorded as `CHECKPOINT-2026-09-03-002`. **Merged.** | Frontend |
| [#78](https://github.com/boonthepkstl-alt/stl_asset_service/pull/78) | 2026-09-03 | `frontend/employee-create-full-page` | **Convert "Add Employee" from a Modal into a full page** at `/employees/create` (`frontend/src/pages/CreateEmployee/index.tsx`, new `ROUTES.EMPLOYEE_CREATE`), mirroring the then-existing `CreateAsset` 4-step wizard pattern exactly: horizontal step indicator, one `SectionCard` per step, inline footer. 3 steps — Basic Info → Organization & Location → Review. **Layout-only; no new fields.** The substance of the PR was the scope decision that preceded it: the user proposed a much larger redesign (Employment Type, Cost Center/Business Unit, Emergency Contact, Personal Email, Alternate Phone, Matrix Manager, System Role, AD/SSO toggle, Profile Picture, auto-generate-ID button). Each was checked against the real `Employee` type and `RAISE-PRD.md` before any code: several already existed (Reporting Manager, Location Campus, Physical Desk); the rest were unconfirmed new scope; and **Matrix Manager directly contradicts an explicit prior business rejection** — the PRD records a second manager/supervisor relationship on Employee being rejected during the Check-out workflow discussion. User agreed to ship layout-only and defer the field set. 210 tests passing (was 205), `tsc`/lint clean, live-verified through the real UI. **No governing `RAISE-FR-EMP-*` requirement exists** — the PRD has none at all. Session close-out recorded as `CHECKPOINT-2026-09-03-003`. **Superseded by PR #82** (the wizard pattern it mirrored was itself removed). **Merged.** | Frontend |
| [#79](https://github.com/boonthepkstl-alt/stl_asset_service/pull/79) | 2026-09-03 | `frontend/employee-duplicate-email-phone-check` | **Live duplicate-check validation for Work Email and Phone on Create Employee** — checked on blur and again on submit, blocking the form from advancing on a match against the existing employee list. `EmployeeDetail`'s "Edit Identity" modal gained the same check for phone only (that modal has no email field), excluding the employee being edited so an unchanged save isn't rejected by its own value. Chosen deliberately as the first buildable-now item from PR #78's deferred proposal, because it is a safeguard on **fields that already exist**, not new scope needing a PRD decision. 215 tests passing (was 210), `tsc`/lint clean, live-verified. **Client-side only** — no backend unique constraint exists on employee email or phone, so a concurrent API create can still duplicate. Session close-out recorded as `CHECKPOINT-2026-09-03-004`. **Merged.** | Frontend |
| [#80](https://github.com/boonthepkstl-alt/stl_asset_service/pull/80) | 2026-09-03 | `feature/employee-code-optional-field` | **Add an optional "Employee Code" field to Create Employee**, mirroring `CreateAsset`'s optional Asset Code field ("leave blank for auto-generation") with the same duplicate-check pattern from PR #79. **Found and fixed a real backend parity gap while implementing**: `AssetService.CreateAsset` already accepted an optional client-supplied code with fallback to auto-generation, but `EmployeeService.CreateEmployee` **always overwrote the input with a generated code regardless** — so the new frontend field would have been purely cosmetic, the user's typed value silently discarded server-side. Mirrored the asset pattern in `go-template-main/model/employeeModel.go` and `service/employeeService.go`; `gofmt` had to be run on both afterwards (the edit left them unformatted). `POST /employees` now honours a supplied `employeeCode` — a behaviour change on an existing endpoint, no new endpoint, no schema change (the column already existed). 218 frontend tests passing (was 215); `go build`/`vet`/`test`/`gofmt` clean; live-verified against the real Docker stack (a typed code survives the round-trip to Postgres; blank still auto-generates). **No Go unit test covers the newly-honoured path** — verified live only. Session close-out recorded as `CHECKPOINT-2026-09-03-005`. **Merged.** | Frontend + Backend |
| [#81](https://github.com/boonthepkstl-alt/stl_asset_service/pull/81) | 2026-09-03 | `feature/employee-id-format-validation` | **Validate Employee ID against the company's real 8-digit format.** **Business confirmed 2026-09-03** (direct Q&A): the company's Employee ID is exactly **8 digits, first 2 = the Gregorian join year** (e.g. `26725898` = joined 2026); the remaining **6 digits are issued by HR/payroll and RAISE cannot derive or generate them**. Confirmed scope is therefore **validate the format on input only — deliberately NOT auto-generate**. Employee Code now rejects anything that isn't exactly 8 digits (blur + submit), with format checked **before** uniqueness since a duplicate check on a malformed value is useless. Two existing tests used non-conforming codes and were updated; the duplicate-code test had to **change shape**, because seeded fixtures use the legacy `EMP-####` form which the new format check now rejects outright. 219 tests passing (was 218), `tsc`/lint clean, live-verified. Recorded as new finding **F-36** in `OPEN-FINDINGS.md` (the direct sibling of F-35, asset code generation). **Status is 🟡 Partial, not Complete** — the app now enforces a format its own data does not follow: `employeeService.go`'s auto-generation still emits `EMP-<8 hex>` and the seed fixtures still use `EMP-####`, both rejected by the new check. Resolving that needs a rule for the HR-issued 6-digit portion, which RAISE does not own. Session close-out recorded as `CHECKPOINT-2026-09-03-006`. **Merged.** | Frontend |
| [#82](https://github.com/boonthepkstl-alt/stl_asset_service/pull/82) | 2026-09-03 | `frontend/flatten-create-forms-single-page` | **Flatten Create Asset and Create Employee into single-page forms.** The user questioned whether the Create Asset wizard should be a wizard at all and asked for analysis **before** any code. Field counts were measured, not estimated: `CreateAsset` holds **13 fields total**, with step 2 (Financial) and step 3 (Assignment) holding **only 3 fields each** — too thin to justify a step. The analysis supported the instinct, so **both** forms were flattened in the same PR: `CreateAsset` 4 steps → 3 stacked section cards, `CreateEmployee` 3 steps → 2. Employee was included because it had only been built to mirror the stepper two days earlier (PR #78); flattening Asset alone would have re-introduced the exact inconsistency PR #78 existed to remove. The Review step was dropped on both as redundant once every field is visible — and on Asset it was **actively misleading**, listing only 6 of the 13 fields. The action bar is now sticky to the viewport bottom, and validation became a single pass on submit, so all errors surface at once instead of one step at a time. **Net −137 lines.** Both test suites substantially rewritten for the single-page flow. `tsc`/lint clean, live-verified through the real UI. **This deliberately reverses PR #78's pattern choice** — PR #78 was not wrong for what was known then (it correctly copied the codebase's existing pattern); the pattern itself is what measuring the field counts showed to be unjustified. Session close-out recorded as `CHECKPOINT-2026-09-03-007`. **Merged.** | Frontend |
| [#83](https://github.com/boonthepkstl-alt/stl_asset_service/pull/83) | 2026-09-03 | `frontend/remove-global-new-asset-button` | **Remove the "New Asset" button from the global app header** (`frontend/src/components/AppShell.tsx`). It rendered on **every** page (Employees, Settings, Licenses, Dashboard, …) despite doing one asset-specific thing, and duplicated the Assets page's own button on the single page where it was relevant. The Assets page button stays, as does the asset table's empty-state one — every sensible path to creating an asset is preserved. Also removed the now-unused `Plus` import (it would otherwise have failed lint), and tightened `App.route.test.tsx` from `getAllByRole(...)[0]` to `getByRole` — the loose form existed only to tolerate two identical buttons, so tightening it now also guards against the duplicate returning. 2 files, `+4/-6`. `tsc`/lint clean, live-verified. Session close-out recorded as `CHECKPOINT-2026-09-03-008`. **Merged.** | Frontend |
| [#84](https://github.com/boonthepkstl-alt/stl_asset_service/pull/84) | 2026-09-03 | `frontend/clickable-breadcrumbs-home-root` | **Make breadcrumbs clickable and rename the root crumb to "Home"**, per direct user request. **Found a latent bug in the process**: the breadcrumb prop type has always been `{ label: string; href?: string }[]` and roughly 9 pages were already passing real `href` values, but the rendering **ignored `href` entirely** and emitted a plain `<span>` for every crumb — every one of those hrefs was dead code, correct data nothing ever read. Fixed so non-last crumbs with an `href` render as navigating buttons; the last crumb stays inert (it's the current page). Also moved ownership of the root crumb into `AppShell`, which now prepends `Home` (→ dashboard) itself — **all 39 call sites across 19 page files** dropped their hardcoded `{ label: 'RAISE' }`. Navigation goes through the existing `onNavigate(id)` prop rather than importing react-router, keeping `AppShell` router-agnostic as designed. 21 files changed (`+70/-45`). Final verified state for the whole #78–#84 series: **46 test files / 221 tests passing**, `tsc`/lint clean; backend `go build`/`vet`/`test` clean. Live-verified through the real UI. **No automated breadcrumb test was added** — the same coverage gap that let the dead `href` props go unnoticed. Session close-out recorded as `CHECKPOINT-2026-09-03-009`. **Merged.** | Frontend |
| [#86](https://github.com/boonthepkstl-alt/stl_asset_service/pull/86) | 2026-09-03 | `frontend/breadcrumb-tests` | **Add breadcrumb regression tests and label the breadcrumb nav landmark** — closes the coverage gap `CHECKPOINT-2026-09-03-009` had recorded against itself as Remaining Work. New `frontend/src/components/AppShell.breadcrumb.test.tsx` with 6 tests pinning the three rules PR #84's dead-`href` bug violated: the `Home` root crumb is prepended by `AppShell` and navigates to the dashboard; an intermediate crumb with an `href` renders as a navigating button and calls `onNavigate` with the **bare nav id** (not the `/`-prefixed path — every call site does `navigate('/' + id)`, so a leading slash would double it); the last crumb is never linked even when it carries an `href`. Two edge cases also pinned (an intermediate crumb without an `href` stays plain text; `Home` goes inert when it is the only crumb). `AppShell.tsx` gained `aria-label="Breadcrumb"` on the trail's `<nav>` — **not cosmetic**: the sidebar renders nav items with the same labels ("Employee Management", "Asset Management"), so unscoped queries matched both surfaces; the label makes the trail addressable as a landmark for `within(...)` scoping and is the WAI-ARIA breadcrumb pattern's own requirement. **Each test was mutation-tested** — re-run against the original bug (every crumb rendered as a plain `<span>`) and confirmed to fail, so the suite genuinely guards the regression rather than merely passing beside it. 2 files (`+79/-2`), **47 test files / 227 tests** (was 46/221, re-measured at the merge commit in an isolated `git worktree` during close-out, not derived), `tsc`/lint/build clean. **No governing FR** — breadcrumb behaviour is specified nowhere in the chain, so these are engineering regression tests and were deliberately **not** added to `RAISE-TEST-CASES.md`. Session close-out recorded as `CHECKPOINT-2026-09-03-010`. **Merged.** | Frontend |
| [#87](https://github.com/boonthepkstl-alt/stl_asset_service/pull/87) | 2026-09-04 | `frontend/edit-employee-modal-to-full-page` | **Convert the "Edit Identity & Organization" modal into a full page** at `/employees/:employeeId/edit` (`pages/EditEmployee/index.tsx`, new `ROUTES.EMPLOYEE_EDIT`), mirroring `CreateEmployee`'s post-PR-#82 single-page layout. The request as phrased ("make Employee Detail a full page in the same style") did not match reality — Employee Detail **already was** a full page — so the actual target was identified and **confirmed with the user before any code**: the last record-editing form still living in a modal. Kept **exactly** the modal's field set (Job Title, Phone, Status, Department, Location Campus, Physical Desk, Reporting Manager) and its `updateEmployee` payload — no field added, removed or renamed — and preserved the phone duplicate check **including its exclude-self behaviour** from PR #79. `EmployeeDetail` shed `isEditProfileModalOpen`, 8 `edit*` state vars, the save/duplicate handlers and the `<Modal>` (−118 lines); both "Edit Identity" and "Modify Specs" now navigate. The Assign Equipment and Create IT Requisition modals were left untouched. **This completes the modal→full-page conversion begun in PR #78 — no record-creation or record-editing form in the app is a modal any more.** A self-initiated max-effort code review (8 finder angles) produced **9 findings, 7 fixed before merge**: (1) audit rows were written into the shared fixture **even with `EMPLOYEE_API_ENABLED` on**, which would have shown client-invented entries on the Audit tab that exist nowhere server-side and look identical to real ones — now gated to mock mode; (2) the phone duplicate check treated `useEmployees`' empty array as "no duplicate" while discarding its `loading`/`error` flags, so **a failed list request silently disabled the validation entirely** — it now distinguishes "ok" from "could not verify" and refuses to save on the latter; (3) the audit actor was hardcoded to `'Current Admin'` instead of the signed-in user, despite `services/audit-repository.ts` already establishing that convention; (4) two real test flakes reproduced during the review (typing before the pre-fill `useEffect` flushed, so the save persisted the *seeded* value — observed as `expected 'Desk S-999', got 'Desk S-204'`; and asserting the form's removal synchronously off the success toast, when the toast and the route change commit independently); (5) the test file was **order-dependent** — `MockEmployeeRepository` is a module-level singleton, so the exclude-self test left `e1` renamed and the pre-fill test passed only by running first, now fixed with snapshot/restore and verified across three `--sequence.shuffle` runs; plus a `renderWithProviders` `extraRoutes` option so the navigation test uses the shared helper instead of a hand-rolled provider tree. 8 files across 2 commits (`+415/-108`), **48 test files / 235 tests** (was 47/227), `tsc`/lint/build clean, live-verified through the real UI (pre-fill, save + toast + return, Cancel discards, duplicate phone blocks, own phone does not, and the audit row now attributed to "Demo Admin" with no spurious desk row). **2 findings were deliberately NOT fixed** and are now tracked as **F-38** (deferred tech debt — the Employee audit trail is a mock-mode fixture shim and `EmployeeDetail:71` aliases it as state, so its `useMemo` can never recompute from a write; harmless today, and the real fix is a backend audit endpoint, not hardening scaffolding) and **F-39** (**product decision required** — "Modify Specs" routes to a page with no workstation/OS spec fields; pre-existing, but costlier now that it is a full route change; add the fields or remove the button, undecided, do not pick without asking). **No governing FR** — the PRD still has no `RAISE-FR-EMP-*` requirement at all, so no chain document was touched. Session close-out recorded as `CHECKPOINT-2026-09-04-001`. **Merged.** | Frontend |
| [#85](https://github.com/boonthepkstl-alt/stl_asset_service/pull/85) | 2026-09-03 | `docs/close-out-prs-78-84` | **Docs-only session close-out for PRs #78–#84** — added `CHECKPOINT-2026-09-03-003` through `-009` (one per merged PR), the corresponding `DEVELOPMENT-LOG.md` rows and `CHANGELOG.md` entries, new findings **F-36** (Employee ID convention) and **F-37** (employee login provisioning), and refreshed `CURRENT-STATUS.md`/`PROJECT-TIMELINE.md`. No code change. **This row was added retroactively on 2026-09-04** during PR #89's close-out: like PRs #73 and #75 before it, this PR shipped without its own `DEVELOPMENT-LOG.md` row despite the file's own one-row-per-merged-PR rule — the third recurrence of the same lapse, which is why the docs-close-out PR now gets its row in the *following* close-out by convention rather than being expected to add its own. | Docs |
| [#88](https://github.com/boonthepkstl-alt/stl_asset_service/pull/88) | 2026-09-04 | `docs/close-out-prs-86-87` | **Docs-only session close-out for PRs #86–#87** — added `CHECKPOINT-2026-09-03-010` (breadcrumb regression tests; test count re-measured at the merge commit in an isolated `git worktree` rather than derived by arithmetic) and `CHECKPOINT-2026-09-04-001` (Edit Identity modal → full page, completing the modal→full-page conversion begun in PR #78, and recording all 9 code-review findings and which 7 were fixed in-PR). Recorded two findings deliberately **not** fixed, kept in separate categories so the distinction survives: **F-38** (Minor/Tech Debt — the Employee audit trail is a mock-mode fixture shim and `EmployeeDetail:71` aliases it as state) and **F-39** (Unresolved/scope — "Modify Specs" routes to a page with no spec fields; both available fixes change product scope and no `RAISE-FR-EMP-*` requirement exists to derive either from). Emptied `CURRENT-STATUS.md`'s "Buildable now" list, its only entry having shipped in PR #86. No phase Status or Actual date changed. No code change. Recorded retroactively as part of PR #89's close-out, same convention as #85 above. **Merged** (`43fde1b`). | Docs |
| [#89](https://github.com/boonthepkstl-alt/stl_asset_service/pull/89) | 2026-09-04 | `ci/github-actions-build-test` | **Add the project's first CI pipeline, closing F-14 (R-20).** `.github/workflows/ci.yml` runs two parallel jobs on every PR and every push to `main`: **frontend** (`npm ci` → lint → `tsc --noEmit` → vitest → production build) and **backend** (`go build` → `go vet` → `go test`). Chosen by a Next Work Discovery pass over the entire backlog as the **only** candidate buildable without a business or product decision. Before this, all 88 merged PRs were verified locally by hand, relying entirely on `SESSION-CLOSEOUT-PROTOCOL.md` step 3 being honoured each session, with multiple Claude sessions working the same directory concurrently. **The pipeline proved itself before it merged**: on its fourth run it failed on `CreateEmployee/index.test.tsx:70` (`expect(element).not.toBeInTheDocument()` — `found <input id="name" value="Test Employee" />`), a genuine pre-existing flake that had passed every local run and the three CI runs before it. The test waited for the success toast and then asserted the form's unmount *synchronously*, but the toast and the route change commit independently; the code under test was correct and the assertion was racing. Grepping for the same shape found `CreateAsset/index.test.tsx:81` identical. Both now wrap the removal in `waitFor`, matching the fix PR #87's review had already applied to `EditEmployee` — recorded as **F-40**, kept open as a pattern to watch since nothing prevents reintroduction and no lint rule covers it. Also added `TestCreateEmployee_EmployeeCode`, closing the gap this log recorded against PR #80 ("No Go unit test covers the newly-honoured path" — verified live only; confirmed still true). It covers both branches and asserts the auto-generated fallback **by shape**, so resolving F-36 by changing that fallback will fail this test and force the decision to surface. **Mutation-tested**: reverting PR #80's fix makes it fail with `"EMP-10274830" should not contain "EMP-"`. Performance was measured, not assumed — `npm ci` was 7m02s cold and still 4m12s with `setup-node`'s `cache: npm` warm (that option caches only npm's *download* cache; `npm ci` rebuilds `node_modules` from scratch regardless), so the built tree is cached instead on an exact lockfile key with no `restore-keys`, taking the frontend job 8m17s → **1m10s**, verified by re-running the same SHA and confirming `Install dependencies: skipped`. Deliberate scope limits, each recorded with its reason: **`gofmt` is not a gate** (Go sources are committed with CRLF and no `.gitattributes` exists, so `gofmt -l` flags every file in the module on Linux — confirmed line-endings-only with `gofmt -d`); **no `paths` filter** (a path-skipped job never reports a status, so branch protection would later leave docs PRs un-mergeable); **no image build/push or deploy**, so F-14's image half and **F-13 (hosting) remain open**. Verified green **on `main` itself**, not just the PR branch — run `33843149477`, event `push`, sha `103e069`, both jobs green, zero annotations. Session close-out recorded as `CHECKPOINT-2026-09-04-002`. **Merged** (`103e069`). | CI / Infrastructure |
| [#91](https://github.com/boonthepkstl-alt/stl_asset_service/pull/91) | 2026-09-04 | `perf/route-level-code-splitting` | **Route-level code splitting, closing F-18 (R-21).** The production build emitted a **single 694 KiB JS chunk** containing all **27** routes — `React.lazy` was used **zero** times — so a visitor landing on `/login` downloaded the Asset Registry, the AI Decision Center and every admin screen before seeing a password field, and Vite warned about it on every build. Every authenticated page is now a `React.lazy` chunk behind one `Suspense` boundary. **Measured, not estimated:** entry chunk **694 KiB → 305 KiB (312.5 kB, gzip 102 kB), −56%**; chunks **1 → 80**; total JS 694 → 789 KiB (**+95 KiB of chunking overhead — recorded rather than omitted, since it is the real trade: overhead is paid only for pages actually opened, against a >50% cut to the load every user pays**); Vite's >500 kB warning gone. **Three components deliberately left eager**, each with a stated reason: `Login` (entry point of every unauthenticated visit — lazy would add a round-trip to the most common first paint) and `NotFound`/`Forbidden` (small, and rendered as the destination of a catch-all or redirect, where a loading fallback before an error page is worse than the bytes saved). The `Suspense` fallback deliberately matches `ProtectedRoute`'s existing auth-loading markup so a cold chunk fetch and an auth check read as one wait. **No test needed changing, and that was verified rather than assumed** — lazy routes resolve asynchronously, the same timing class as **F-40**, so the App-level suites were checked first: only `App.rbac.test.tsx` imports from `@/App` and it uses `ProtectedRoute` with stub elements, while the other seven `App.*.test.tsx` files build their own route trees from direct page imports. 48 files / 235 tests passed unchanged, `tsc`/lint/build clean, **CI green on `main`** (run `33845187022`, event `push`). Live-verified through the real UI across six routes with no console errors and no failed requests. **Scope note kept explicit:** this advances **no `RAISE-FR-*`, no AC and no traceability coverage** (matrix stays v1.8, all 15 gaps closed), and **performance targets remain undefined (F-17)** — it clears a self-identified build warning, it does not meet a stated NFR. Session close-out recorded as `CHECKPOINT-2026-09-04-003`. **Merged** (`02ae966`). | Frontend |
| [#93](https://github.com/boonthepkstl-alt/stl_asset_service/pull/93) | 2026-09-04 | `fix/no-raw-errors-in-5xx-responses` | **Stop leaking raw Go error text in 5xx responses, closing F-19 (R-22).** Eighteen 5xx handlers across 7 controllers put `err.Error()` straight into the JSON body beside a human-readable `message`; those errors originate in the database layer, so a response could carry driver text, SQL fragments, schema names or connection detail to the caller. All 18 now return `message` only — **0 5xx `err.Error()` sites remain**. **Proved on the wire rather than by inspection:** with the database container stopped, `GET /api/assets` previously would have returned `dial tcp: lookup db on 127.0.0.11:53: server misbehaving` in the body; it now returns exactly `{"message":"Failed to retrieve assets"}`, while the server log still records the full error — so no diagnosability was lost. Each of the 18 sites was checked individually to confirm it already logged before responding, including the two delegating to `mapHandoverError` (both callers log first). **Scope held deliberately to F-19's own wording — 5xx only, 18 of 46 `err.Error()` sites.** The other 28 are 4xx, many carrying service-layer sentinels (`ErrAssetNotITHardware`, `ErrHandoverWrongStage`, ...) whose text is meaningful validation feedback; removing those would degrade the API rather than harden it. **Verified safe for the frontend**: `types/api.ts` declares `APIError.error` but nothing in `frontend/src` reads it — the interceptor branches only on status 401. Guarded by new `controller/internalErrorLeak_test.go`, a **source-level** invariant check whose trade-off is recorded in the file itself (the existing controller harness needs live MSSQL/Postgres/Tarantool handles and a signed token before it can serve a request, so driving 18 real 500s would cost far more than the invariant is worth). **Mutation-tested** — re-adding the field to `dashboardController` fails it with the exact file and line — and it asserts it scanned ≥15 sites so it cannot pass vacuously after a future refactor. **Recorded F-41, found while validating this very change and deliberately not fixed here** — it partly contradicts the assumption the 5xx/4xx split rested on: `GET /api/assets/does-not-exist` returns `{"error":"sql: no rows in result set","message":"Asset not found"}`, a raw driver error in a **4xx** body rather than a sentinel. Fixing it is a per-site audit, not a sweep, since the 28 remaining sites mix genuine sentinels with wrapped driver errors; widening this PR would have meant guessing which messages are load-bearing. `go build`/`vet`/`test` clean, `gofmt` clean on all changed files, frontend 48 files / 235 tests unaffected, **CI green on `main`** (run `33851908649`, event `push`). **Scope note:** backend hygiene only — advances **no `RAISE-FR-*`, no AC and no traceability coverage**, security NFRs remain undefined (F-17), and no deployment exists (F-13), so the fix is preventative. Session close-out recorded as `CHECKPOINT-2026-09-04-004`. **Merged** (`449a751`). | Backend |
| [#95](https://github.com/boonthepkstl-alt/stl_asset_service/pull/95) | 2026-09-04 | `docs/alerts-f05-chain-sync` | **Resolve F-05 (R-23) — alert trigger rules and severity — across all seven chain documents.** Docs only, no code. Business confirmed by direct Q&A: five MVP trigger conditions with a fixed 3-level severity assigned **per condition type** — Warranty EXPIRED → High, Ticket OVERDUE (`targetResolutionDate` passed and status ≠ `DONE`) → High, Warranty EXPIRING (reusing the existing per-Asset-Category threshold, default 90 days) → Medium, Ticket ON_HOLD → Medium, Handover PENDING → Low. **No new field and no data-model change** — every option put to the user was derived by reading `types/asset.ts`, `requisitionData.ts` and `lib/warranty.ts` first, so none could have introduced one; the two alternative severity rules (days-overdue, asset value/criticality) were explicitly offered and rejected, and no asset-criticality field was invented. **The finding's own premise was partly wrong and this is recorded rather than glossed:** F-05 read "trigger rules *and channels*", but the channels half was never open — the requirement's own Scope line already said "MVP (single-channel; multi-channel is Roadmap)", so only stale TBD wording was removed, not a decision taken the user was never asked for. Propagated in order with each layer verified against the real file before the next ran: PRD 0.14→**0.15** (Resolved Question 44, §17 row `TBD`→`APPROVED`), Design 0.12→**0.13** (§14: alerts are a **read-time derivation**, no Alert entity/table/persisted record, contrasted with Maintenance tickets which do persist), Prototype 0.13→**0.14** (P-012: five conditions, real severities, three link targets, no new screen invented), AC 0.11→**0.12** (AC-ALERT-001 2→10 criteria), Test Plan 0.11→**0.12**, Test Cases 0.16→**0.17** (8 new cases, **none marked PASS**), Matrix 1.8→**1.9**. **Specification only — `RAISE-FR-ALERT-001` deliberately stays `PASS (partial)`**: only one of the five conditions is built, so the reason for partial status changed from "trigger rules undefined" to "rules defined, four of five unimplemented" rather than disappearing. That build gap is **Gap 16**, opened and left open. The bell-icon scope contradiction (PRD Resolved Question 35 vs. `ESAPS-UI-FOUNDATION-BASELINE.md` line 88) is **Gap 17**, also left open with no side picked. `TC-ALERT-001-01/-02` keep their genuine 2026-09-01 PASS, untouched. Validated on merged `main` (`ae56120`): frontend `tsc`/lint clean, 48 files / 235 tests; backend `go build`/`vet`/`test` clean; CI green. **This is the first work in several sessions that advances requirement coverage at all** — F-14, F-18 and F-19 each advanced none. Session close-out recorded as `CHECKPOINT-2026-09-04-005`. **Merged** (`ae56120`). | Docs |
| [#96](https://github.com/boonthepkstl-alt/stl_asset_service/pull/96) | 2026-09-04 | `docs/close-out-pr-95` | **Docs-only close-out for PR #95** — added `CHECKPOINT-2026-09-04-005` and recorded **F-05 as Resolved (R-23)**, specification only. Chain versions verified on merged `main` by reading the committed blobs rather than the working tree. Records that the finding's own premise was partly wrong: F-05 read "trigger rules *and channels*", but the channels half was never open — the requirement's Scope line already said MVP is single-channel, so only stale TBD wording was removed, not a decision the user was never asked to make. Marked "Buildable now" as no longer empty, since resolving F-05 opened Gap 16. No code change. **Merged** (`8a14154`). | Docs |
| [#97](https://github.com/boonthepkstl-alt/stl_asset_service/pull/97) | 2026-09-04 | `feature/gap-16-alert-conditions` | **Implement Gap 16 — the four unbuilt alert conditions — then formally execute `TC-ALERT-001-03..10`.** The Alerts screen had one condition (expired warranty) and rendered severity as the literal "Not yet defined"; PR #95 closed that decision and this builds it. All five conditions now derive with their confirmed fixed severities. New `frontend/src/lib/alerts.ts` holds the derivation beside the warranty helper it reuses (Design v0.13 §14): a pure function over already-fetched lists — **no Alert entity, table, persisted record, new field or data-model change** — with the severity mapping in a single `Record` so it cannot drift, and `asOf` injectable so tests never depend on the clock. **Two judgement calls settled by reading the spec rather than choosing:** a ticket both `ON_HOLD` and overdue emits **both** alerts, because Resolved Question 44 defines five *independent* conditions with no precedence between them and suppressing one would invent a rule (`REQ-2026-0041` appears twice in the seeded data); and the Expiring threshold is read from Settings per Asset Category rather than hardcoded, pinned by a test that derives the same asset under a 90- and a 10-day threshold. 12 new unit tests cover each condition's positive **and** negative case; per-rule tests build their own fixtures rather than the shared module-level seed data (the coupling behind **F-40**), with one deliberate exception running over real seed data since the page test cannot see past pagination. Suite 48 files / 235 tests → **49 / 250**; `tsc`/lint/build clean; CI green. **Formal execution followed on merged `main` (`c2e6b76`) against the real running app: 7 PASS, 1 BLOCKED.** `TC-ALERT-001-03..08` and `-10` all PASS with concrete evidence — 19 alerts across five conditions, correct severities, and every navigation leg verified to render the named record rather than just change the URL; `-08` proved severity is fixed per type (warranties expired 2.5 years and 6 weeks ago both render `High`). **`TC-ALERT-001-09` was NOT executed and NOT marked PASS**: its step 2 says "edit that Asset's `warrantyExpiry`", but the product has **no asset-edit capability at all** (`asset-repository.ts` exposes only `create`/`assign`/`checkIn`; verified in source before concluding). Classified a **test-case defect**, not an implementation or specification defect — the implementation was separately shown correct by lowering the Expiring threshold 90 → 3 via the real Settings UI, which took the alert total 19 → 18 and back. That evidence was recorded as *supporting only* and the case left BLOCKED, and its steps deliberately **not** rewritten mid-execution, since editing a procedure while executing it destroys the evidence that it was unrunnable. Recorded as **F-42** / **Gap 18**. **Therefore Gap 16 stays OPEN and `RAISE-FR-ALERT-001` stays `PASS (partial)`** — the build half is done and verified, but no gap closes while a case in its scope is unexecuted, and F-08's access gate remains a second independent reason. Chain re-verified after execution: PRD 0.15 / Design 0.13 / Prototype 0.14 / AC 0.12 / Test Plan 0.12 all **unchanged** (the evidence required no requirement change); only Test Cases 0.17→**0.18** and Matrix 1.9→**2.0** moved. Session close-out recorded as `CHECKPOINT-2026-09-04-006`. **Merged** (`c2e6b76`). | Frontend |
| [#98](https://github.com/boonthepkstl-alt/stl_asset_service/pull/98) | 2026-09-04 | `docs/close-out-pr-97-alert-execution` | **Docs-only close-out for PR #97, recording the formal execution of `TC-ALERT-001-03..10`** against the real running app on merged `main` `c2e6b76`: **7 PASS, 1 BLOCKED**. Added `CHECKPOINT-2026-09-04-006`. `TC-ALERT-001-09` was **not** executed and **not** marked PASS — its step 2 said "edit that Asset's `warrantyExpiry`", which the product cannot do (`asset-repository.ts` exposes only `create`/`assign`/`checkIn`; verified in source before concluding). Classified a **test-case defect**, recorded as **F-42 / Gap 18**; the implementation was separately shown correct by lowering the Expiring threshold 90 → 3 via the real Settings UI (alert total 19 → 18 and back), recorded as *supporting evidence only* and not substituted for the specified procedure. Gap 16 therefore stayed OPEN and `RAISE-FR-ALERT-001` stayed `PASS (partial)`. Chain re-verified: PRD 0.15 / Design 0.13 / Prototype 0.14 / AC 0.12 / Test Plan 0.12 all unchanged; only Test Cases 0.17→0.18 and Matrix 1.9→2.0 moved. **Merged** (`30f176c`). | Docs |
| [#99](https://github.com/boonthepkstl-alt/stl_asset_service/pull/99) | 2026-09-04 | `docs/tc-alert-009-execution-gap16-closure` | **Correct `TC-ALERT-001-09`'s unexecutable procedure, execute it, and close Gap 16 + Gap 18.** Its step 2 said "edit that Asset's `warrantyExpiry`" — impossible, since `asset-repository.ts` exposes only `create`/`assign`/`checkIn`. Done in a deliberate order: **verified the replacement trigger existed in source first** (Ticket Detail's real Update Status → `Done` control), **corrected the procedure and left it unexecuted** (Test Cases v0.19) so it could not be shaped around whatever passed, **then executed it** (v0.20) — **PASS**. Alerts 19 → **17** after completing `REQ-2026-0041` to `DONE`, both its rows gone, and a scan found **no** acknowledge/dismiss/snooze affordance anywhere, so they vanished purely because both conditions stopped holding. **No production code was changed to make the test pass** — the fix was to the test, not the product; adding an asset-edit capability would have been unrequested scope. The corrected procedure tests the property *more strongly* than the original: one state change cleared **two** conditions at once. Gap 18 closed; **Gap 16 closed** legitimately, since all 10 `TC-ALERT-001-*` cases were by then executed and passing. **F-42 → R-24.** `RAISE-FR-ALERT-001` deliberately stayed `PASS (partial)`, held by F-08 alone. Session close-out recorded as `CHECKPOINT-2026-09-04-007`. **Merged** (`d8ad01c`). | Docs |
| [#100](https://github.com/boonthepkstl-alt/stl_asset_service/pull/100) | 2026-09-04 | `docs/f08-alerts-access-gate` | **Resolve the Alerts access gate — the part of F-08 blocking `RAISE-FR-ALERT-001` — and take the requirement to a full `PASS`.** After Gap 16 and Gap 18 closed, every test case in the requirement's scope passed and it was *still* `PASS (partial)`, held there by one thing: `AC-ALERT-001-01` required alert information to be visible to an **"authorized user"** and nobody had ever defined "authorized". That is a decision gap — no amount of code closes it. Business confirmed (PRD §16 **Resolved Question 45**): the gate is **any authenticated user** (all four roles, none excluded), and role enforcement stays **declared in code per route** (`ProtectedRoute allowedRoles`), not data-driven. Both answers **confirm behaviour already built** rather than change it. **The options were derived from source before asking** — roles read out of `types/auth.ts`, the gating map out of `App.tsx` — so no option could invent a role or a screen; **no business rule was authored by the AI**. A consequence was recorded rather than left implied: the app ships a **Role Management screen with an editable, persisted 15-module × 6-action permission matrix that nothing reads** — under decision 2 it enforces nothing and is presentational only, now stated in PRD v0.16 and Design v0.14 so no reader mistakes it for working access control. Chain propagated end to end: PRD 0.15→**0.16** (Resolved Question 45; new open question **Q22a**), Design 0.13→**0.14** (route gating map), Prototype 0.14→**0.15**, AC 0.12→**0.13** (access-gate NOT TESTABLE YET closed; new **`AC-ALERT-001-11`** for the unauthenticated-redirect negative case), Test Plan 0.12→**0.13**, Test Cases 0.20→**0.21**, Matrix 2.1→**2.2**. **Formally executed against merged `main` `d8ad01c`**, signing in as each demo account in turn and clearing the stored session between each: `ADMIN`, `IT_MANAGER`, `IT_STAFF` and `EMPLOYEE` all reached `/notifications` and all saw **19** alerts — no Forbidden page, no redirect. Negative case `TC-ALERT-001-11`: with `localStorage`/`sessionStorage` cleared, `/notifications` redirected to `/login` and displayed no alert data. **A false result was caught rather than filed**: the negative case *first appeared to pass an unauthenticated visitor straight through*, which was not real — the browser still held an `ADMIN` session in `localStorage` from an earlier run in the same profile. It was inspected, cleared and re-run properly, and the trap is recorded in the test case so a later tester does not repeat it in a dirty profile; **reporting that first observation as a defect would have been wrong.** **`RAISE-FR-ALERT-001` upgraded `PASS (partial)` → full `PASS`** — all 11 `TC-ALERT-001-01..11` executed and passing, the first requirement in this series to make that move. The verdict was reached against the Compliance Review's own definition, not because two gaps happened to close: **PRD Q22a** (should a user see only *their own* alerts?) was weighed explicitly and judged **outside the confirmed scope** — raised but unspecified, the AC layer deliberately wrote no criterion for it, and it is not specifiable today anyway since **no link exists between the authenticated `User` and an `Employee`**. One correction was made by hand: the matrix row's status cell had been rewritten so it still *opened* with `**PASS (partial)**` and disclosed the upgrade only ~14,000 characters later, meaning anyone scanning the matrix — its entire purpose — would read the wrong verdict; it now leads with the current verdict, superseded history retained beneath. **F-08 narrowed, not closed (R-25)**: two sub-questions resolved, the role/permission *content* for every other screen and the authentication mechanism itself (Q21, F-11, F-12) remain open. **Gap 17 untouched, no side picked.** **Code changes: none** — a decision, a chain sync and an execution. Session close-out recorded as `CHECKPOINT-2026-09-04-008`. **Merged** (`a7f0d42`, 2026-09-05). | Docs |
| [#102](https://github.com/boonthepkstl-alt/stl_asset_service/pull/102) | 2026-09-05 | `feature/f03-exec-kpis` | **Build the Utilization KPI tile — F-03, part 1 of 2.** F-03 tracked three missing proposal-defined KPIs of `RAISE-FR-EXEC-001` (NBV, Risk, Utilization) as one decision gap; checking the source before starting showed that framing was wrong for one of them. **Utilization was never blocked on a decision** — PRD §16 Resolved Question 27 fixed its definition and Resolved Question 29 fixed its calculation mechanics, both on 2026-08-21. It was missing because nobody had built it. New `frontend/src/lib/utilization.ts` is a pure function over stats the dashboard already fetches: RQ29(a)'s *real-time snapshot* is what makes it computable with no assignment-history table, since at a single instant "share of available time spent assigned" collapses to "share of eligible assets currently assigned"; RQ29(b)'s exclusion is applied by taking `Available + Assigned` only. **Recorded rather than papered over:** RQ29 names Disposed, Retired and Under Maintenance as excluded, but `AssetStatus` has only four values and there is **no Disposed status** — Disposal is confirmed Enterprise Roadmap (Resolved Question 26) — so the comment says so, and says a future Disposed status must be excluded here too. Divide-by-zero guarded explicitly (empty register, or every asset retired/in maintenance, renders `0%` not `NaN%`). 6 unit tests + 1 page test; the page test asserts **66.7%, not 53.3%**, because the seeded register holds 15 assets but only 12 assignable ones and those two numbers differ *exactly* by RQ29(b) — **verified by mutation**, adding 3 to the denominator fails 5 of the 8. Live-verified as ADMIN: the tile reads 66.7% and "8 of 12 assignable assets" beside "15 Total Assets", so the exclusion is visible on the page. Suite 49 files / 250 tests → **50 / 257**; `tsc`/lint/build clean. Two other premise corrections found in the same pass and recorded for the chain sync rather than acted on: **`RAISE-AI-RISK-001` already says `Pilot (not confirmed MVP)`**, so a risk-scoring formula was never a precondition for completing the MVP requirement; and **`currentValue` exists on the Asset record but cannot stand in for NBV** — `assetService.go:101` sets it equal to `purchaseCost` on create and never recomputes it, and the fixture values are hand-written at inconsistent rates. **NBV deliberately not included**: its method is decided (straight-line, per-category useful life in Settings, salvage 0) but the default useful-life values are a business input not yet supplied, and inventing them would put wrong money on an executive dashboard. **F-03 narrowed, not closed. Merged** (`321265f`). | Frontend |
| [#103](https://github.com/boonthepkstl-alt/stl_asset_service/pull/103) | 2026-09-05 | `fix/f41-4xx-error-audit` | **Stop leaking raw driver errors in 404 bodies, closing F-41 (R-26).** F-41's own text said the fix "needs a per-site audit, not a sweep" — so all **28** 4xx `err.Error()` sites were classified before a line changed, and **7** were fixed. The audit corrected the finding's framing twice. **(1) There were three groups, not two.** F-41 described a two-way mix of sentinels and driver errors; the largest group is neither — **16 request-parse sites** whose text is Go's json/schema decoder output, internal struct names but genuinely useful client feedback and not infrastructure detail. Left untouched, raised as **F-43**. **(2) The leak was worse than recorded.** F-41 called it "lower severity than F-19 was" because `sql: no rows in result set` is less sensitive than connection detail — true only of the one observed case. The not-found handlers reach the repository through `GetPGReadDB`, which returns a **connection** error when the pool cannot be built (`masterAsRead` → `GetPGWriteDb` → `openPGPool` → `db.Ping()`), so **a database outage answered 404 with the driver's dial text, host and port included** — the same class of detail F-19 removed from 5xx, reached through a status F-19's guard did not watch. Fixed: the five not-found handlers (Asset/Employee/Ticket/Sample/Handover), `CreateTicket`'s service-error branch (reachable by a raw `repo.List` error), and `mapHandoverError`'s not-found case. **One fix could not be a deletion** — `mapHandoverError` was the only site where dropping the field lost real information (it could no longer say *which* record was missing), so the branch was **split in two** and the distinction moved into the documented `message` field. Kept: 5 `errors.Is`-guarded sentinel sites. New guard `TestNoRawErrorTextIn404Responses` makes 404 a blanket rule with the same anti-vacuity threshold as the 5xx scan; **verified by mutation** — reinstating the leak fails it by file and line. F-19's own test comment asserted 4xx carries only sentinels, which is what F-41 disproved, so it was corrected rather than left standing. `APIError` in the frontend was made honest too: it declared `error` as **required** while two whole classes of response no longer send it. Backend `go build`/`vet`/`test` clean; frontend `tsc`/lint clean, 50 files / 257 tests. **F-44 raised while validating.** Session close-out recorded as `CHECKPOINT-2026-09-05-001`. **Merged** (`f75f28b`). | Backend |
| [#105](https://github.com/boonthepkstl-alt/stl_asset_service/pull/105) | 2026-09-05 | `docs/f03-exec-kpi-chain-sync` | **Sync all seven chain documents for the Executive Dashboard KPI decisions (F-03 narrowed → R-28).** PR #102 had shipped the Utilization tile **without** a chain sync, deliberately, so the chain could move once alongside NBV — a reasonable trade that stopped being reasonable when NBV blocked on an input that never arrived. By then `AC-DASH-03` asserted *"none of the three PRD-proposal KPIs is present in the shipped grid"*, which had been **false since the tile went live**, and `TC-DASH-03` carried a PASS recorded against that absence check. **The documents were actively misdescribing the product.** Three business decisions were confirmed and recorded: **RQ46** — NBV formula confirmed (straight-line from the existing `purchaseDate`/`purchaseCost` fields, useful life per Asset Category in Settings following RQ41's precedent, salvage zero, clamped at 0), with the **default per-category values deliberately NOT invented** and recorded instead as new **Open Question 3a**, since those numbers become money on an executive dashboard; **RQ47** — Risk confirmed **out of MVP scope**, which *confirms rather than changes* what the PRD already said (`RAISE-AI-RISK-001` was already Priority "Pilot (not confirmed MVP)"), meaning §16 Q4 **never blocked this P0 requirement** — the most useful correction in the round, and it propagated everywhere: an absent Risk tile became a **decision, not a gap**, and the test plan's blocked-item count genuinely went *down* rather than being reworded; **RQ48** — Utilization recorded as built and live, having never needed a decision at all (RQ27/RQ29 settled it on 2026-08-21; it was simply unbuilt). **The critical repair** was `AC-DASH-03`, split into `-03a` (Utilization — a positive, testable criterion), `-03b` (NBV — absent because *unbuilt*, formula confirmed: "specified but not yet buildable", explicitly distinguished from "unspecified") and `-03c` (Risk — absent **by decision**, not a gap). **The honest consequence was a downgrade:** `RAISE-FR-EXEC-001` `PASS` → **`NOT_TESTED`** and Dashboard/Navigation `PASS (partial)` → the same — **not a product regression** but a criterion change plus a newly shipped feature awaiting execution. `TC-DASH-01`/`TC-EXEC-001-01` had been executed against an **eight**-tile grid that now has **nine**; `TC-DASH-03`'s PASS tested an absence whose criterion no longer exists. All were marked **SUPERSEDED with original dates retained**, none deleted or quietly re-marked, and **no new or restructured case was marked PASS**. **Gap 19** opened to track the execution those four cases needed. Recorded to prevent a wrong future assumption: the Asset record's `currentValue` looks like a ready-made NBV but cannot serve as one — `assetService.go:101` sets it equal to `purchaseCost` on create and never recomputes it, and the fixtures hold hand-written values at inconsistent rates, so it would render a number that never moves. Each layer was produced by its own `.claude/skills` subagent in sequence; **no chain document was edited directly in the main thread**. One verification catch: the matrix agent bumped its footer to v2.3 but left the **top-of-file header reading v2.2** — the first thing a reader sees, and the second time that document has opened with a stale verdict — and was sent back to fix its own header. PRD 0.17 / Design 0.15 / Prototype 0.16 / AC 0.14 / Test Plan 0.14 / Test Cases 0.22 / Matrix 2.3. **No code changed.** Session close-out recorded as `CHECKPOINT-2026-09-05-003`. **Merged** (`5f232a8`). | Docs |
| [#106](https://github.com/boonthepkstl-alt/stl_asset_service/pull/106) | 2026-09-05 | `docs/gap19-execution` | **Execute the Gap 19 sweep — `RAISE-FR-EXEC-001` back to `PASS (partial)`.** Formal execution of the four cases Gap 19 was opened for, against merged `main` `5f232a8` on the real running app. **All four PASS; Gap 19 closed.** `localStorage`/`sessionStorage` were cleared to zero entries **before** signing in — not ceremony: a stale `ADMIN` session produced a **false result** during the F-08 execution on 2026-09-04, and reporting that first observation as a defect would have been wrong. `TC-DASH-01` PASS (nine tiles — 15, 4, 8, **66.7%**, 2, 11, 10, $42.8K, $156.2K, order matching exactly); `TC-DASH-03a` PASS ("66.7%" / "Utilization" / "8 of 12 assignable assets", read from the tile's own DOM subtree); `TC-EXEC-001-01` and `-03a` PASS on the same built page. **The evidence worth keeping, because it verifies the rule rather than the code:** the same page's "Asset Status" section independently reports Available 4, Assigned 8, In Maintenance 2, Retired 1 — fifteen total, matching the "Total Assets 15" tile — while the Utilization denominator is **12**, exactly Available + Assigned. **RQ29(b)'s denominator exclusion is demonstrated by two independent readings on the same screen**, not asserted from the implementation this session wrote. **Verdict re-derived, not assumed:** `NOT_TESTED` → **`PASS (partial)`**, deliberately *not* back to a full `PASS`, because `TC-*-03b` (NBV) remains BLOCKED on Open Question 3a and a full PASS would misstate the evidence; the matrix row names the outstanding component and leads with the current verdict. Gap 19 closed following the document's own Gap 16/18 convention (original "OPENED" paragraph retained as history, separate closure paragraph appended). **A test-case defect found while executing (F-45):** `TC-EXEC-001-01`/`-03a` say *"Log in as Executive"*, but **there is no Executive role** — `UserRole` has exactly `ADMIN`/`IT_MANAGER`/`IT_STAFF`/`EMPLOYEE`, and "Executive" is a PRD persona. Executed as `ADMIN`; the substitution does not weaken the result (the route carries no role restriction, per RQ45). **The wording was deliberately not fixed during the execution that found it** — rewriting a procedure mid-run destroys the evidence it was wrong, the same discipline applied to F-42; unlike F-42 this one did not block. Test Cases 0.22 → **0.23**, Matrix 2.3 → **2.4**. **F-03 stays OPEN** on the useful-life defaults; **Gap 17 is now the only open gap in the entire matrix.** No code changed. Session close-out recorded as `CHECKPOINT-2026-09-05-004`. **Merged** (`5100678`). | Docs |
| [#108](https://github.com/boonthepkstl-alt/stl_asset_service/pull/108) | 2026-09-07 | `feature/gap17-header-bell` | **Wire the header bell to real alerts, resolving Gap 17 — the last gap open in the matrix.** Gap 17 was a straight contradiction between two documents written the *same day*: PRD §16 **Resolved Question 35** (2026-08-21) put `NotificationCenter.tsx` among seven ESAPS pages **entirely out of RAISE scope**, while `ESAPS-UI-FOUNDATION-BASELINE.md` mapped it to `RAISE-FR-ALERT-001` as **EXTEND**. **Why it survived a month, which is the useful part: both documents said "NotificationCenter" while meaning different artifacts** — RQ35 meant the ESAPS *page* (103 lines, reference only); the baseline's reviewer meant in-app alert capability, which RAISE does have. Nobody was wrong about the product; they named different things identically. So it was put to business as **two** questions rather than forcing one side. **PRD v0.18 §16 Resolved Question 49:** (1) **RQ35 stands, the baseline row is the error** — corrected in place to **DO NOT USE**, the reason recorded there, and settled by the baseline's *own* source table, which already names the PRD chain the **"Sole requirement authority"**; (2) **RAISE's own `AppShell` bell is a different artifact and IS in scope** — built in RAISE, never ported; (3) the bell shows the **first five** alerts plus a link to `/notifications`. **The ordering is where the AI stopped instead of guessing:** business first asked for "the 5 most recent", which is **not computable** — `Alert` carries **no timestamp of any kind**, alerts being a read-time derivation with no persisted record, and `deriveAlerts` sorts by **severity**. Quietly redefining "recent" as "severe" would have been inventing a business rule; the problem went back to business, which confirmed **the first five of the existing severity ordering** (sorting by the underlying records' own dates was considered and **rejected**). **Implementation:** new `frontend/src/hooks/useAlerts.ts` extracted from the Alerts page and consumed by **both** surfaces, so the bell and the page **cannot disagree** about what an alert is, how many there are, or their order — the property the decision turns on, enforced **structurally rather than by convention**. Two things fixed because they were genuinely broken, not to make tests pass: the **dead `notifications` prop was removed** (no caller ever passed it, so the bell had rendered permanently empty behind a placeholder reading *"this will be wired to the real API in a later phase"* — a prop that does nothing is a type that lies about its component, same reasoning as `APIError.error` in PR #103), and **`aria-label`/`aria-expanded` were added** to what had been an icon-only button with **no accessible name at all**, found because a test could not address it. **5 new tests**, all rendering the real Alerts page so both surfaces are compared **in one render**; one asserts no Low-severity alert reaches the bell, which is what proves the shared ordering is actually shared. **Verified by mutation** — slicing 7 instead of 5 fails 2. Suite 50 files / 257 tests → **51 / 262**; `tsc`/lint/build clean. **Live-verified as ADMIN:** badge 19, exactly five rows (`AST-0003`…`AST-0007`, all High), `aria-label` "Notifications, 19 alerts", "View all alerts" → `/notifications` whose first rows were identical. Chain propagated through all six downstream layers: Design **0.16**, Prototype **0.17** (bell recorded as global chrome, deliberately *not* a new `P-0xx` ID), AC **0.15** (six new criteria), Test Plan **0.15**, Test Cases **0.24** (six new cases, all left unexecuted), Matrix **2.5**. **The honest consequence: closing Gap 17 did NOT leave the matrix gap-free.** `RAISE-FR-ALERT-001` was **re-derived downward** from a full `PASS` to `PASS (partial)` — the confirmed scope grew faster than execution evidence for it — and **Gap 20** was opened to track the sweep, with every place that read *"only Gap 17 remains"* corrected. Session close-out recorded as `CHECKPOINT-2026-09-07-001`. **Merged** (`6a6bcac`). | Frontend |
| [#109](https://github.com/boonthepkstl-alt/stl_asset_service/pull/109) | 2026-09-07 | `docs/gap20-execution` | **Execute Gap 20, correct the specification it exposed — `RAISE-FR-ALERT-001` to a full, unqualified `PASS`, and the matrix to ZERO open gaps for the first time in its history.** All six header-bell cases executed against merged `main` `6a6bcac` on the real running app, storage cleared to zero before sign-in (not ceremony: a stale `ADMIN` session produced a **false result** during the F-08 execution, and reporting it would have been wrong). `-13` **PASS** (exactly five rows, each with label/description/`CODE · Name`); **`-14` PASS — the case the whole ordering decision rested on** — run exactly as its procedure demands, in **one session with no reload**: the bell's five rows recorded, then "View all alerts" used to reach P-012 in that same session, and the two lists were **identical in content, count and order** (automated comparison returned `true`), so the shared-derivation property is now **verified rather than asserted**; `-15` **PASS**; `-16` **PASS** (enumerating every `button`/`a`/`input`/`select`/`[role=button]` in the panel returned **exactly one**: "View all alerts"); `-17` **PASS**. **`TC-ALERT-001-12` could not be executed as written and was NOT marked PASS** — its step asked for a numeral on the *closed* bell button, which renders only a presence dot — **and its procedure was not rewritten mid-execution**, the discipline **F-42** established. **The cause was the AI's own and is recorded as such (F-46, raised and resolved in the same revision): an AI-introduced specification error, not a product defect and not a business decision.** The criterion was drafted during the 2026-09-05 chain sync from the AI's own imprecise description — *"the bell badge shows the TOTAL alert count"* — true of the **panel** badge and mis-read as the **header** badge. **PRD RQ49 never asked for a header numeral** and the pre-existing bell already used a dot, so **the product matched the business decision and the specification over-reached beyond it.** Business chose to correct the specification, not the product, and the fix ran in the order that makes it trustworthy: `AC-ALERT-001-12` rewritten in place (AC **0.16**) → Test Plan **0.16** → `TC-ALERT-001-12` corrected in Test Cases **0.25** and **left unexecuted there** so the steps could not be shaped around whatever passed → executed in **0.26**: **PASS** — the closed button's rendered text **empty** with a single class-only dot span (a *positive* confirmation, not merely a failure to find a numeral), `aria-label` "Notifications, 19 alerts", panel badge "19", P-012's own pagination "Showing 1-10 of 19", and an automated equality across all three returning **true**. **All seventeen `TC-ALERT-001-01..17` are now executed and passing — none BLOCKED, none unexecuted.** **The full `PASS` was weighed, not assumed:** PRD §16 **Q22a** (per-user filtering) is raised but **unspecified**, the AC layer deliberately wrote **no criterion** for it, and it is **not specifiable today** — no `User`↔`Employee` link exists — and the matrix records *how* that was weighed rather than leaving it implicit. **Gap 20 CLOSED; matrix v2.6 carries zero open gaps, Gaps 1–20 all resolved.** **F-45 fixed in the same pass (R-29):** `TC-EXEC-001-01`/`-03a` step 1 now read "Log in as any authenticated user" instead of naming an "Executive" role the app does not have — **wording only**, both keeping their recorded PASS and 2026-09-05 execution history untouched, since the defect was in how the step was written, never in what was verified. **No code changed.** Session close-out recorded as `CHECKPOINT-2026-09-07-002`. **Merged** (`282e758`). | Docs |
| [#111](https://github.com/boonthepkstl-alt/stl_asset_service/pull/111) | 2026-09-07 | `docs/compliance-review-resync` | **Re-verify `RAISE-COMPLIANCE-REVIEW.md` against Traceability Matrix v2.6 (F-47 → R-31)** — the chain's terminal deliverable, the document that answers *"is this MVP compliant?"*, had fallen **twelve matrix revisions behind** at v1.0/matrix v1.4 and was **overstating** one requirement. Two verdicts had gone stale in opposite directions: `RAISE-FR-EXEC-001` read a full **`PASS`** where the matrix records **`PASS (partial)`**, and `RAISE-FR-ALERT-001` read `PASS (partial)` where the matrix records a full **`PASS`**. **Overstating compliance is the worst failure mode for this document specifically.** **Why it was subtle:** v1.0 excused the NBV gap as "a separate, still-open question" and called the dashboard's confirmed scope fully PASS regardless — **sound reasoning when written**, which stopped holding once PRD §16 Resolved Questions 46–48 confirmed the NBV formula, put Risk out of MVP scope and recorded Utilization as built, turning an *external* question into **an unmet part of the requirement's own confirmed scope** with an AC criterion and a `BLOCKED` case behind it. No single PR broke it; the ground moved underneath it. **The §4 finding worth keeping:** the headline figure — 8 of 17 at a full PASS, 47% — **did not change**, because the two movements cancelled exactly. **A stable number concealing two opposite movements is precisely why this document's currency matters**; the summary would have read unchanged and correct while one of its members was wrong, and §4 now says so in place rather than presenting a quietly different table. Also corrected: §5 listed **F-02, F-05, F-18, F-19** as open when all four were resolved (R-19, R-23, R-21, R-22), with F-03/F-08 now recorded as *narrowed* and F-14/F-41/F-44/F-45/F-46 added as resolved; §7 claimed "Gaps 1–13 resolved" when the matrix records **Gaps 1–20 resolved with none open**; a new §1a Revision Note records what moved and why. **The root cause was a process gap and was closed with the finding:** v1.0 itself said *"no action is required to finish this review"*, so nothing in the close-out protocol pointed back at it and every close-out since updated the matrix without touching the document that consumes it — the Next Action now requires re-verification **in the same pass that moves a matrix verdict**, and **`CLAUDE.md`, which still claimed this file did not exist**, now lists `11-compliance-review/` with that warning. Compliance Review 1.0 → **1.1**. **No code changed.** Session close-out recorded as `CHECKPOINT-2026-09-07-003`. **Merged** (`f74a92a`). | Docs |
| [#112](https://github.com/boonthepkstl-alt/stl_asset_service/pull/112) | 2026-09-07 | `fix/f43-401-status` | **Stop reporting a token-signing failure as a 401 (F-43 half b → R-32).** `authService.Login` returned three bare `errors.New` values the controller could not tell apart, so it answered **401 for all three** — including `util.GenerateToken` failing, which is a **server-side signing fault, not a rejected password** — and the body carried that error's own text. **A premise correction came first, and it is the reason this was buildable at all:** F-43's own row recorded that fixing it *"changes login response text the frontend may rely on"*. **That was never verified and is false** — `HttpAuthRepository.login` returns `response.data` and reads no error body, and `pages/Login/index.tsx` catches and displays its own fixed string `"Invalid username or password"`. No UI text depends on the server's message, so the (b) half **stopped being a product decision and became a plain defect**; it had been parked in the "needs a business decision" column on a premise nobody had tested, including me when I wrote it. **Fix:** three exported sentinels — the two genuine authentication cases keep **401** and their **byte-identical original wording**, deliberately *not* tidied up in passing since that text is observable API behaviour a caller legitimately reads, while the signing failure maps to **500** with a fixed message and the detail in the log only (F-19's rule extended to the one 4xx site that was reporting a server error). **No new guard was needed** — the existing `TestNoRawErrorTextIn5xxResponses` covers the new 5xx automatically and would fail if it ever carried `err.Error()`. **4 new tests**, one asserting the two messages **literally, byte for byte**, because changing them changes the API; **verified by mutation** — swapping the two sentinels fails `TestLoginWrongCredentialsIsItsOwnSentinel` by name. The happy path is deliberately **not** exercised (it needs real JWT config and a viper-backed demo account) and the tests say so rather than implying wider coverage. The 4xx audit script now re-classifies this site from `PARSE` to **`SENTINEL`**, a previously-known false positive of its 14-line context window, so the heuristic agrees with reality. **F-43 narrowed, not closed:** the **15 request-parse sites are untouched and still open** — their `err.Error()` is Go's decoder text, internal field naming but also genuinely useful client feedback and *not* infrastructure detail, so removing it is a real usability-versus-disclosure trade nobody has decided, and F-41 warned a blanket sweep is the wrong shape of fix. **API change:** a signing failure now answers 500 with `{"status":"error","message":"Failed to complete sign-in"}`; both authentication responses are unchanged, wording included. Backend `go build`/`vet`/`test` clean; frontend untouched at 51 files / 262 tests. Session close-out recorded as `CHECKPOINT-2026-09-07-004`. **Merged** (`96d076f`). | Backend |
| [#115](https://github.com/boonthepkstl-alt/stl_asset_service/pull/115) | 2026-09-07 | `chore/gitattributes-gofmt` | **Add the `gofmt` gate that was never actually blocked (F-49 → R-34).** Planned as "add `.gitattributes` so the gate becomes possible"; the gate was possible all along, and **the reason recorded for skipping it — in `ci.yml` and again in R-20, both written by this AI — was false**. `ci.yml` claimed Go sources *"are committed with CRLF line endings, so `gofmt -l` lists every file in the module on a Linux runner"*. Measured instead of assumed: all 64 Go files extracted with `git show :<path>`, the content a runner actually checks out, contained **zero CRs**. The CRLF is `core.autocrlf=true` translating on a Windows working tree, which no runner sees. **The root cause is worth keeping because the observation was real:** `gofmt -l` genuinely flags 42 of 64 locally, and R-20 even says it was confirmed with `gofmt -d` — but the check ran against the **working tree** and the conclusion was stated about **CI**. A true measurement, an untested inference. **Then the evidence was overstated a second time, and the gate caught that too.** Establishing that the 42 locally-flagged and 42 locally-CRLF files were the *same 42* was taken to mean "zero real formatting problems among them" — set equality proves the sets match, not that CRLF was the only reason each was flagged. **On its very first run the new gate failed** on `controller/internalErrorLeak_test.go`: a genuine missing blank line before the F-41 guard, introduced when that test was appended in PR #103, masked locally because the file was flagged regardless. Fixed, then re-verified properly (all 64 converted to LF as a set → **0 flagged**). **The gate justified itself on its first run.** What each piece does: the **gate** prints the offending files *and* a `gofmt -d` diff so a red build is actionable; **`.gitattributes`** pins `*.go text eol=lf` as **prevention, not a fix** — a contributor with `core.autocrlf=false` would otherwise turn the gate red for a reason unrelated to code quality; **renormalisation touched zero files**, so the work R-20 called *"its own change, normalising line endings across the repo"* did not exist. Deliberately narrow — **no `* text=auto`**, which would renormalise hundreds of files in one unreviewable commit while other sessions may share the directory. All three false records **corrected in place rather than deleted**. Backend clean; YAML validated. Session close-out recorded as `CHECKPOINT-2026-09-07-005`. **Merged** (`75fae5b`). | CI |
| [#116](https://github.com/boonthepkstl-alt/stl_asset_service/pull/116) | 2026-09-07 | `fix/f38-audit-aliasing` | **Fix the Employee audit stale-derivation defect (F-38 half → R-35).** `EmployeeDetail` held the shared `employeeAuditLogs` fixture in `useState(fixtureAudit)` and derived its rows through a `useMemo` keyed on that value, while `EditEmployee` appends with `fixtureAudit.unshift(...)` — mutating **in place**. An array's identity never changes when its contents do, so **the memo could not recompute from a write in a mounted instance**, masked only because `EditEmployee` navigates back and the page remounts. **The route to the fix is the record worth keeping, because it was wrong twice first.** (1) Adding `fixtureAudit.length` to the memo's deps — **ESLint rejected it on exactly the right grounds** (*"outer scope values … aren't valid dependencies because mutating them doesn't re-render the component"*); a module array cannot be a reactive input and no dependency list can make it one. (2) Reading the live array while keeping the memo with deps `[employee]` — **purely cosmetic**, since the memo still caches after mount. **Attempt 2 would have shipped as a bug fix.** What stopped it was mutation-testing the guard: restoring the original aliasing left the test **passing**, because the test's first version appended *before* mount — **a vacuous test that agreed with a broken fix**, the exact failure mode this project's other guards exist to avoid. **What works:** stop caching; the filter runs every render over the live array, and the list is a handful of rows so the memo bought nothing. The test now appends **after** mount, the only arrangement that separates broken from fixed, and **fails by name against the restored aliasing**. It asserts visibility **on the next render** and deliberately **not** reactivity — nothing notifies this page when another module mutates an array, and the code comment says so rather than implying more. Targeting the tab took two corrections, both recorded in the test: the sidebar has its own "Audit" item, and the tab's accessible name carries its row count while the sidebar's is exactly "Audit". **Deliberately not done:** a real backend for Employee audit — **no `RAISE-FR-EMP-*` requirement exists** to trace one to. Suite 51 files / 262 tests → **52 / 263**. **Merge-conflict note:** this branch and #115 both appended to `OPEN-FINDINGS.md`; resolved by keeping **both** sides — F-49 from `main` plus this branch's **updated** F-38 row, since taking `main`'s side wholesale would have silently reverted the R-35 record. Session close-out recorded as `CHECKPOINT-2026-09-07-006`. **Merged** (`9bca98b`). | Frontend |
| [#117](https://github.com/boonthepkstl-alt/stl_asset_service/pull/117) | 2026-09-07 | `feature/f03-nbv-groundwork` | **Implement RQ46's NBV formula ahead of its defaults (F-03 groundwork → R-36).** **The framing this corrects is the AI's own:** F-03 had been reported repeatedly, including in this project's own tracking, as blocked outright on five numbers. **It was not.** RQ46 confirms the formula, the configuration shape, the salvage value and the clamp; only the per-category defaults are missing — and the precedent for building the rest already existed in the same codebase, where `lib/alerts.ts` takes `warrantyThresholdFor` as a parameter rather than embedding RQ41's 90 days. **Built:** `frontend/src/lib/nbv.ts` — `computeAssetNbv` and `computePortfolioNbv`, pure, `asOf` injectable, useful life **injected**, and **zero defaults defined anywhere** in the module or its tests, so nothing invents a number business has not supplied and the tile still cannot render until Settings has real values. **Verified by three mutations against RQ46's own clauses**, each failing a different count: removing the clamp (1 test), ignoring the asset's category (2), returning 0 instead of purchase cost for an unconfigured category (3). **Two judgement calls recorded rather than silently made:** an unconfigured or NaN useful life returns `purchaseCost` unchanged — not 0, not NaN — because "not yet depreciated" is honest where 0 claims the asset is worthless and NaN renders as "NaN" on a KPI tile (and this is the case that occurs *first*, since the tile ships before every category has a life set); and **no status filter is applied**, because RQ29(b)'s Retired/In-Maintenance exclusion is **Utilization's** confirmed rule and RQ46 states no equivalent — a retired asset still has a book value, so excluding one would have been inventing a business rule. Both pinned by tests. **The trap RQ46 warns about is pinned too:** the module never reads `Asset.currentValue`, and a test feeds a wildly contradictory value asserting the result does not move — `assetService.go:101` sets that field to `purchaseCost` on create and never recomputes it, so any implementation reaching for it would look right on seeded data and be wrong forever after. **What the tests taught mid-task:** the first version hand-wrote dates and expected exactly 2.0 years; a 365.25-day year makes that 1.9986, so three assertions failed by cents — **the code was right and the expectations naive**, fixed by deriving dates as exact multiples of the module's own year length so the assertions could stay strict rather than loosening the tolerance until they passed. Suite 52 files / 263 tests → **53 / 278**. **Process note, reported rather than hidden:** this PR was merged while its Frontend check still read `pending`; the run was confirmed green afterwards and `main` re-validated locally, so no harm resulted, but the check should have been waited on as it was on every other PR this session. **F-03 stays open** on the defaults — no Settings field, no tile, no chain sync, all three of which need the numbers. Session close-out recorded as `CHECKPOINT-2026-09-07-007`. **Merged** (`c22c5e2`). | Frontend |
| [#119](https://github.com/boonthepkstl-alt/stl_asset_service/pull/119) | 2026-09-07 | `docs/f42-row-and-stale-q3q4-comments` | **Correct two stale statements that misreport what is still open (F-50 → R-37, F-51 → R-38).** Both are documentation-accuracy defects found by auditing this project's own tracking surfaces; **neither unblocks a requirement and neither changes an executable line**, stated first so the value is not oversold. **F-50:** the findings register's own **F-42 row read as open** — its status cell opened with *"Open — and it is the single item keeping Gap 16 open"* and disclosed **RESOLVED 2026-09-04 (R-24)** only at **character 656**, with no separator, though R-24 is in the Resolved table and the matrix records **Gap 16 and Gap 18 both CLOSED** as of v2.1. **Caught by its own victim:** a script written during the 2026-09-07 assessment to enumerate open findings read the lead and counted F-42 as OPEN. Fixed by placing the current status first and demoting the superseded reasoning to a labelled *History* clause — **nothing deleted**, per the file's own rule — and the row now names F-50 so the defect is discoverable from where it did its damage. **Measured, not asserted:** `leadcheck.py` `LATE-DISCLOSURE (resolved@656)` → `ok (resolved@2)`; `openlist.py` OPEN **20 → 19**. All 34 `F-` rows scanned — **only this one** had that shape (F-03/F-08/F-43 open with "Open" correctly, being open). Blast radius verified contained: `CURRENT-STATUS.md`, `NEXT-STEP.md` and the Compliance Review each checked, none describes F-42 as open. **F-51:** three source comments (`lib/utilization.ts`, `services/dashboard-repository.ts`, `model/dashboardModel.go`) justified excluding NBV/Risk on the grounds that *"PRD §16 Q3/Q4 remain open"*; all three now cite **RQ46** for the NBV formula and **RQ47** for Risk carrying no MVP dashboard tile, and each gives its own reason for not computing the figure. **The fences were correct and are untouched.** **This PR's own overstatement, caught in review before merge and the more useful half of the record:** the first version wrote *"neither is an open question any more"* / *"stale on both halves"* in all three comments and both register rows. Checked against `RAISE-PRD.md` §16 rather than the PR's own summary — **Q4 reads `Still open` verbatim** (RQ47 changed only that it no longer bears on `RAISE-FR-EXEC-001`), and Q3's remainder is **Q3a**, the useful-life defaults, **which is F-03**. What was stale is the *reason*, not the questions — the distinction that is the whole finding. **Same failure mode as F-49's set-equality overstatement**, and the fourth time this session a stated premise did not survive being checked. Corrected in place: every comment and both rows now quote the wrong wording, name it wrong, and state that Q3a and Q4 remain open. **Comment-only proved two independent ways:** the non-comment diff filter is empty, and all three files comment-stripped are **byte-identical to `main` `4b1f53d`**. No test added — there is no behaviour to guard; the guard is the two scripts' before/after readings. Suite unchanged **53 files / 278 tests**; `tsc` 0, ESLint clean, `go build`/`vet`/`test` clean, `gofmt` clean on LF content, CI Format check `success`. **F-03 explicitly NOT closed** and no verdict moved. Session close-out recorded as `CHECKPOINT-2026-09-07-008`. **Merged** (`1594fab`). | Docs + comments (frontend, backend) |
| **no PR** (`c4d2e24`…`3356e2f`, direct to `main`) | 2026-09-07 | — (no branch) | **F-03 Business Input Gate → F-52 raised, and two corrections to this project's own account of what F-03 needs.** **Deviation stated first, since this file's rule is one row per merged PR:** these four commits went **directly to `main` with no branch and no PR**, because the session instruction for this round forbade creating either. The row is keyed to the commit range rather than a PR number invented for it. **No product code was touched** — `git diff --stat 456dda2..HEAD` lists only `NEXT-STEP.md` and `OPEN-FINDINGS.md`. **The gate review:** no approved useful-life value exists anywhere in the repository (searched `docs/`, `frontend/src`, `go-template-main`, English and Thai; PRD §16 **Q3a still open**, highest Resolved Question **49**). The one number-bearing hit, `nbv.test.ts:65` (`// 2-year life`), is a **test input and was deliberately not treated as a business value.** **F-52** — the Dashboard/Executive **NBV tile has no forward specification**: `AC-DASH-03b`/`AC-EXEC-001-03b` say *"then no such tile is present"*, `TC-DASH-03b` says *"…not a target for NBV to be displayed"*, and `RAISE-PROTOTYPE.md`:783 says *"none can be built"*. **So `TC-DASH-03b`/`TC-EXEC-001-03b` would FAIL once the tile ships**, and the **nine-tile** assertion that `TC-DASH-01`/`TC-EXEC-001-01` already PASSED is invalidated by a tenth tile. Recorded as **not** a defect in those documents — each described a state in which the tile could not be built; it becomes a gap only when F-03 unblocks. Filed under **Blocking**, **left open** because the specification pass is gated on DoR-3 (the presentation decision beside the existing static illustrative "Monthly Depreciation" tile), and writing the criteria first would mean inventing that decision. **The Settings half is genuinely specified as presence** (Prototype §23A, `AC-WARRANTY-001-07`, `TC-WARRANTY-001-07`), so **the five numbers alone unblock Settings but not the tile.** **The two corrections, in order:** `NEXT-STEP.md` first understated F-03 ("the chain sync" outstanding, `TC-WARRANTY-001-07` omitted) — fixed at `a92aeb5`; **that fix then overshot** ("specified end-to-end … no specification work remains", false for the Dashboard half) — fixed at `eeefad7`. F-03's register row carried the same flattening and now names F-52, the cross-reference discipline from the F-42 fix. **Both were found by verifying a correction the AI had just written.** Validated on `3356e2f`: `tsc` 0, ESLint clean, **53 files / 278 tests**, `go build`/`vet`/`test` clean, `gofmt` clean over all 64 Go files on LF content; **CI green on all four commits**; register integrity 37 `F-` rows × 5 cells, `openlist.py` OPEN 19 → 20. **No verdict moved:** `RAISE-FR-EXEC-001` stays `PASS (partial)`, F-03 stays business-input pending. Session close-out recorded as `CHECKPOINT-2026-09-07-009`. | Docs only (project-management) |
| **no PR** (`48b2e31`, `4a442be`, direct to `main`) | 2026-09-08 | — (no branch) | **Propagate PRD §16 Resolved Questions 50 and 51 through the full chain — the nine-tile KPI grid becomes ten (F-52 specification half closed; Gap 21 opened).** **Deviation stated first**, since this file's rule is one row per merged PR: these commits went **directly to `main` with no branch and no PR**, because the standing instruction is that no PR opens until F-03's Definition of Ready is complete. Keyed to the commit range rather than a PR number invented for it. **Zero product code touched.** **What unlocked the day:** the remaining F-03 work splits into two halves needing **different** inputs — the Settings NBV section needs the five useful-life values, while the Dashboard tile *specification* needed only **DoR-3**, the presentation decision. `TC-WARRANTY-001-07` already proved a presence criterion can be written without any default value (*"no illustrative/placeholder default value is asserted"*). An earlier assessment had concluded no engineering work was available at all; **that was wrong**, and this is the correction. **RQ50:** NBV is specified as a **tenth tile** on P-002 and P-014; the static illustrative **"Monthly Depreciation"** tile is **kept unchanged**, business having explicitly accepted a real computed figure sitting beside a fabricated one. `AC-DASH-03b`/`AC-EXEC-001-03b` and `TC-DASH-03b`/`TC-EXEC-001-03b` flipped from **absence** to **presence** — the substance of **F-52**. **RQ51:** an Asset whose `category` has no configured useful life contributes **`purchaseCost` unchanged** and stays in the portfolio total; new `AC-DASH-04`, `AC-EXEC-001-04`, `TC-DASH-04`, `TC-EXEC-001-04`. It **confirms already-shipped test-pinned behaviour (R-36)**, so **no code follows from it**. **Handled rather than hidden:** `TC-DASH-01`/`TC-EXEC-001-01` were **PASS** against the superseded nine-tile assertion — their PASS text is preserved **verbatim as history**, labelled superseded, and both are reclassified **BLOCKED (partial)** pending re-execution; neither is carried forward into a verdict, neither is deleted. **Matrix v2.7 opened Gap 21** for that, so the matrix no longer has zero open gaps — correctly, since **a complete specification is not coverage**. **F-52 judged in two halves, not closed wholesale:** forward-specification **CLOSED**, build/execution **still OPEN** — the day's plan had said this would "close F-52"; **it closed half**. **`RAISE-FR-EXEC-001` was NOT upgraded** — stays `PASS (partial)`, with its component breakdown corrected *downward* (`-01` PASS → BLOCKED). Versions: PRD 0.18→0.19, Design 0.16→0.17, Prototype 0.17→0.18, AC 0.16→0.17, Test Plan 0.16→0.17, Test Cases 0.26→0.27, Matrix 2.6→2.7. All six syncs ran through the `.claude/skills` subagents **sequentially**, never edited in the main thread. **Verified per stage rather than taken on the subagents' reports:** each diff filtered for a category name near a year figure — **all six empty**, no useful-life number invented anywhere; no NBV case marked PASS; Risk cases still assert absence per RQ47; no active `NEEDS_PRD_CONFIRMATION`. Validation: `tsc` 0, ESLint clean, **53 files / 278 tests**, `go build`/`vet`/`test` clean. **Known issue recorded:** five of six documents date the sync **2026-09-07** (the decisions' date) though it ran **2026-09-08**; not blanket-corrected because that string appears ~80 times and most instances are legitimate. Session close-out recorded as `CHECKPOINT-2026-09-08-001`. | Docs only (chain + project-management) |
| **no PR** (`7ac6737`, direct to `main`) | 2026-09-08 | — (no branch) | **Record PRD §16 Resolved Question 52 — NBV useful life is configured per Asset Type, not per Category (F-53 raised; F-03 restated).** **Deviation stated first**, since this file's rule is one row per merged PR: committed **directly to `main`, no branch, no PR**, per the standing instruction that no PR opens until F-03's Definition of Ready is complete; keyed to the commit rather than a PR number invented for it. **PRD v0.19 → v0.20, written through the `prd-writer` subagent** — the only file in the commit. **Zero product code, zero chain documents below the PRD.** **What happened is not what the previous checkpoint expected:** asked for the five per-Category values, business answered that **IT Hardware has no fixed value** (*"it depends on the equipment purchased"*). **That is not a missing number — it is a statement that RQ46's per-Category model does not fit.** Given four options (per Type using the existing field; per Asset with a new field; Category default plus per-asset override; or keeping per-Category and naming one representative value), business chose **per Type**, and separately confirmed **only IT Hardware varies**. **Why one uniform Type key rather than a two-level model** — recorded because the two answers look contradictory until stated: **a per-Type table is a superset of a per-Category table**; for the four fixed categories the same value repeats across their types, which is exactly equivalent, so no special case for IT Hardware is needed. **No new field — verified, not assumed:** `Asset.type` exists end to end (`V1__Assets_Table.sql:7` `type varchar(100) NOT NULL`, `assetModel.go:23` `Type string`, frontend + fixtures), the same precedent as the **F-27** resolution that `type` *is* the sub-category. **Two things deliberately not recorded as rules:** a fixed count of ten types (`type` is free-text and grows exactly as `category` does), and **any number** — none was supplied, so the diff was filtered for **all fifteen** category and type names near a year figure and came back **empty**. **RQ51 needed no change**, because a newly-added type with no configured value already hits its `computeAssetNbv` → `purchaseCost` fallback — the rule generalised without being touched. **RQ46 superseded, not rewritten:** its §16 entry still reads *"configurable per Asset Category"* verbatim with an **AMENDED → RQ52** note appended and cross-linked both ways; verified that the one deletion of that phrase came from the §8 requirement row, not RQ46's entry. **F-53 raised as a deliberate deferral:** the chain synced hours earlier keys `NBVSettings` by **Category** (sharpest instance: `TC-WARRANTY-001-07`'s *"all 5 categories with editable useful-life inputs"*), and the second sync is **bundled with the values** so the six subagents run once rather than twice — raised so the deferral carries a tracking ID rather than becoming the stale specification **F-52** already was. Nothing reads the wrong key today (`NBVSettings` absent, `lib/nbv.ts` zero consumers, affected cases all BLOCKED). **F-03 restated** after its wording went stale twice in one day. **`RAISE-FR-EXEC-001` unchanged at `PASS (partial)`.** Validation on `7ac6737`: `tsc` 0, ESLint clean, **53 files / 278 tests**, `go build`/`vet`/`test` clean, CI green; register 38 `F-` rows × 5 cells. **DoR-5 regressed to FAIL** — the chain no longer matches the confirmed model. Session close-out recorded as `CHECKPOINT-2026-09-08-002`. | Docs only (requirements + project-management) |
| **no PR** (`39bb2b0`, direct to `main`) | 2026-09-08 | — (no branch) | **Re-key the chain from Asset Category to Asset Type (F-53 → R-39), and correct a dating slip across five documents.** **Deviation stated first**, since this file's rule is one row per merged PR: committed **directly to `main`, no branch, no PR**, per the standing instruction that no PR opens until F-03's Definition of Ready is complete. **A re-key, not new scope** — no requirement added, no verdict moved, **no code written**. Design 0.17→**0.18**, Prototype 0.18→**0.19**, AC 0.17→**0.18**, Test Plan 0.17→**0.19**, Test Cases 0.27→**0.28**, Matrix 2.7→**2.8**, sequential through the `.claude/skills` subagents. **Why the earlier deferral was reversed, which is the record worth keeping:** `CHECKPOINT-2026-09-08-002` deferred this sync to bundle it with the numeric values so the six subagents would run once — **on the premise that the values were imminent.** They had been asked for **three times across two days** and the last answer changed the *model* rather than supplying numbers, so the premise was unsafe; meanwhile PRD v0.20 and the chain contradicted each other on the key. **That is the same defect class as F-47, F-50, F-51 and F-52** — a document still asserting something that had stopped being true — and deferring would have made F-53 the fifth, **created on purpose**. **Sharpest instance fixed:** `TC-WARRANTY-001-07` read *"all 5 categories with editable useful-life inputs"*, now *"one row per Asset Type currently present in the data"*. **Deliberately not recorded as rules:** a fixed count of ten types (`type` is a free-text `varchar(100)` that grows as assets are added) and **any number** — none was supplied. **RQ51 needed no edit**: a newly-added type with no configured value already hits its `purchaseCost` fallback, so **the rule generalised without being touched**. **Dating slip:** five documents dated their prior sync 2026-09-07, the date RQ50/RQ51 were *confirmed*, though those syncs ran 2026-09-08; each subagent **judged its own dates individually** rather than blanket-replacing, which would have corrupted recorded evidence (Test Cases alone carries ~48 mentions). **Design had no slip at all** and the **Matrix was already correct**. **One out-of-scope defect fixed rather than filed:** the Test Plan's trailing footer still cited **AC v0.14** and a **nine-tile** grid against its own header — its subagent flagged it honestly as out of scope and was sent back, since that is F-53's own class (hence 0.17→0.19). **Verified per stage, not taken on the subagents' reports:** six diffs filtered for **all ten type and five category names** near a year figure — **all six empty**; the old key survives only in "re-keyed from…" notes and historical changelogs, audited occurrence by occurrence (nine in the Test Plan, all classified); **recorded execution evidence untouched** — `TC-DASH-01`'s row identical before and after (2026-08-31 ×1, 2026-09-05 ×2, PASS ×4); Warranty's per-Category threshold (RQ41) and `ASSET-002`'s hierarchy untouched; **Gap 21 still open** with superseded PASS records intact; no NBV case PASS; `RAISE-FR-EXEC-001` still **`PASS (partial)`**. **Matrix v2.8 reached the F-53 verdict independently**, opening and closing **Gap 22** to record it and stating the limit: closure does **not** cover the missing values or the unbuilt tile (Gap 21, F-03). Validation: `tsc` 0, ESLint clean, **53 files / 278 tests**, `go build`/`vet`/`test` clean, CI green; register 38 `F-` rows × 5 cells. **DoR-5 back to PASS.** Session close-out recorded as `CHECKPOINT-2026-09-08-003`. | Docs only (chain + project-management) |
| [#120](https://github.com/boonthepkstl-alt/stl_asset_service/pull/120) | 2026-09-08 | `feature/create-requisition-full-page` | **Make "Create IT Requisition" a full page, matching Asset Management's create flow.** The form was a `Modal` inside `pages/Maintenance/index.tsx`; it is now `pages/CreateRequisition` at **`/maintenance/create`**, laid out like `pages/CreateAsset` — `AppShell` + `SectionCard`s, one scrollable form, a single validation pass, sticky action bar. **The check that shaped the task, done before writing code:** the request was presentational, so the question was whether the chain constrains presentation. It does not — **`AC-MAINT-001-03` constrains the resulting *state*** (`PENDING_DEPT_APPROVAL`), **`TC-MAINT-001-03` step 1 reads *"open the maintenance-request form for an asset"*** (form-agnostic, still true), and **Prototype v0.19 §15 P-009**'s Stage 1 concept lists the fields and a Submit control **without specifying modal versus page**; a search of Prototype/AC/Test Cases found **no mention of a modal**. Had any layer specified a dialog this would have been an **F-52-shaped trap** — a built page contradicting the criteria meant to accept it. **Route placement and its collision, both handled up front:** it lives under `/maintenance` because `ESAPS-UI-FOUNDATION-BASELINE.md` records that *"IT Requisition is not its own slice"*; and since `TICKET_DETAIL` is `/maintenance/:ticketCode`, `REQUISITION_CREATE` is declared before it and matched ahead of it (React Router ranks static above dynamic) — **the same pairing `ASSET_CREATE`/`ASSET_DETAIL` already relies on in this app**, so a working precedent rather than an assumption. **Domain behaviour deliberately unchanged:** same `ticketService.createTicket()`, same fields, same hardcoded `requesterId: 'e1'` (no `User`→`Employee` link yet, PRD §16 Q22a), same asset preselection, and **the same prefilled Physical Location `"HQ - Floor 4, Desk E-412"`** — carried over verbatim rather than tidied, so nobody meets a silently different form. **One improvement, scoped so it rejects nothing that used to pass:** the Modal reported both required fields in one toast; the page marks each field, and **the required set is identical**. **Tests, mutation-tested:** 5 cases asserting on the **created record rather than the toast** (the toast is presentation; the Stage 1 transition is the criterion), one asserting `queryByRole('dialog')` is **absent** so restoring the Modal fails the suite — removing the title validation fails one test, clearing the Location default fails another, so neither is vacuous. **Two test-authoring errors recorded rather than quietly fixed:** `repository.list()` returns **`{ data, total }`**, not `{ tickets }`; and the mock repositories add **artificial latency on every hop**, so a 300 ms wait read stale totals and **looked exactly like a missing write** — isolated by calling the service directly, which created fine, locating the fault in the test's timing rather than the page. Suite **53 files / 278 tests → 54 / 283**; `tsc` 0, ESLint clean, `vite build` clean; backend untouched and clean; CI green. **Verified in the running app, not only in tests:** the list button navigates to `/maintenance/create`; the page renders full-page with **no dialog**; asset preselects to **AST-0001**; Location shows the carried-over default; submitting created **ITR-2026-007** at Workflow Stage **"1. Dept Approval"**, moved that KPI **1 → 2** and the ticket count **6 → 7**, and returned to `/maintenance` with the success toast; **no console errors**. **No chain document was touched and no verdict moved** — `RAISE-FR-MAINT-001` keeps its full `PASS`. **`CHANGELOG.md` was updated for this one**, unlike the rest of today, because its rule covers user-visible behaviour and this is the first change today a user would notice. **Left alone deliberately:** Prototype §15 P-009 still marks **Priority as "conceptual — TBD"** while the form ships a real Priority select — pre-existing (the Modal had it too), not a defect this change created. Session close-out recorded as `CHECKPOINT-2026-09-08-004`. **Merged** (`24bbd31`). | Frontend |
| [#121](https://github.com/boonthepkstl-alt/stl_asset_service/pull/121) | 2026-09-08 | `fix/create-edit-pages-full-width` | **Make the create/edit pages use the full screen width.** All four carried `max-w-3xl mx-auto`, which left most of a desktop screen empty either side of the form. Removed from all four. **No field, validation, submit-path or route change** — stated first so it is not read as functional progress; the three requirements involved (`RAISE-FR-ASSET-001`, the Employee pages, `RAISE-FR-MAINT-001`) are all at full `PASS` and **none moved**. **This corrects a claim made one PR earlier:** PR #120 said the new requisition page *"mirrors `pages/CreateAsset`"* — **accurate, and exactly why it still did not look full-screen**, because both used `max-w-3xl`. **Matching an existing page is not the same as meeting the request when the page being matched carries the same shortcoming**, and the user had to point that out. Worth keeping, because "match the existing pattern" is normally the right instinct here; this time it inherited the defect instead of the intent. **Scope confirmed before editing rather than after:** the request named **two** pages, grepping first found **four** sharing the container (`CreateAsset:88`, `CreateEmployee:162`, `CreateRequisition:129`, `EditEmployee:183`), and widening only the named two would have shipped **two wide create forms and two narrow ones** — the same half-applied shape that produced **F-53** hours earlier. Scope went back to the user, who chose all four. **Prevention, not just repair:** a comment on each container records why there is no max-width, so nobody re-narrows one and reopens the inconsistency. **Suite unchanged at 54 files / 283 tests, and no test was added deliberately** — none asserted on width, and a width assertion would pin a styling choice rather than a behaviour; the verification that mattered was visual. `tsc` 0, ESLint clean, `vite build` clean; backend untouched and clean; CI green. **Verified in the running app at 1440×900:** all four span the content area with the sticky action bar full width; **at 375×812** the requisition form stacks to one column with **no horizontal overflow** (grids stay `grid-cols-1 sm:grid-cols-2`, so only desktop is affected). **A console-error scare run down rather than waved off:** the tab showed `[vite] Failed to reload /src/pages/CreateRequisition/index.tsx` with 404s — **proved transient** by loading the page in a **fresh tab with an empty console buffer**, which showed only `[vite] connected` plus the React DevTools notice, **zero errors**, every field rendering. They came from the moment the edit script had the file mid-write. **Lesson about the tool:** `read_console_messages` returns an accumulated buffer, so a minute-old error reads exactly like a live one. **`CHANGELOG.md` updated** — visible on every create and edit screen. Session close-out recorded as `CHECKPOINT-2026-09-08-005`. **Merged** (`79f71eb`). | Frontend |
| [#122](https://github.com/boonthepkstl-alt/stl_asset_service/pull/122) | 2026-09-08 | `feature/requisition-entry-points-full-page` | **Route the remaining requisition entry points to the full page.** The form was **three Modals behind six buttons**, not the two I reported: Maintenance 2 (converted in PR #120), **AssetDetail 2** ("New Requisition" in the Maintenance tab, **"Request IT Service" in the action menu**), **EmployeeDetail 2** ("New IT Ticket", "Create IT Request"). **The count was wrong three times, and the sequence is the record worth keeping:** I reported two, `tsc` surfaced the fourth button, and a grep of the *triggers* — **the one that should have come first** — surfaced the last two. **This is the third half-applied change in one day, and the rule that would have prevented it had already been written down after the second** (`CHECKPOINT-2026-09-08-005`: *after fixing one instance, grep for its siblings*) — **I applied it to `max-w-3xl` and never to the form itself.** `RAISE-FR-MAINT-001` stays at full `PASS`; **no field, validation or state transition changed** and **no chain document was touched**, the chain constraining state rather than presentation. **Every prefill preserved via query params** (`assetId`, `requesterId`, `category`, `priority`, `title`, `location`, `returnTo`): AssetDetail passes the asset, **its own title per button** (*"Report issue with X"* / *"Service request for X"*) and the asset's location; EmployeeDetail passes **the employee as requester** — not the page default `'e1'` — plus its **High** priority and hardcoded **Hardware Fault & Repair** category. **Without those three, its requisitions would have been filed against the wrong person at the wrong priority with nothing in the UI showing it.** **`returnTo` fixes something PR #120 got wrong:** that page sent everyone to the Maintenance list on cancel/submit — right from the list, wrong from an asset or employee. **The no-assigned-asset guard deliberately stays in EmployeeDetail**, since only that page knows the employee's assets; verified live, Olivia Brown (`e7`) gets the warning and does **not** navigate. **Lost and recorded rather than papered over:** EmployeeDetail's handler also pushed a *"Ticket Creation"* row into **local** `historyEvents`, fixture-seeded and never persisted, so **it already vanished on remount** (the F-38 ephemerality); and the origin-specific blank-description placeholders collapse to `ticketService`'s default. **Tests: 3 new, each mutation-tested** — ignoring `?requesterId=`, ignoring `?returnTo=`, and letting the preselect override an explicit `?assetId=` each fail exactly one test. Suite **54 files / 283 → 54 / 286**; `tsc` 0, ESLint clean, `vite build` clean; backend untouched and clean; CI green. **Verified in the running app, all six triggers:** each lands on `/maintenance/create` with the right params and `isDialog: false`; AssetDetail prefills the asset's **real** location (*"Data Center East"*, not the fallback); EmployeeDetail passes `requesterId=e1` at `priority=High`, **proved by submitting**, which took Sarah Chen's own "IT Tickets" tab **3 → 4** with the ticket listed there (that tab filters by employee); Cancel from AssetDetail returned to `/assets/a5`. **A sweep for `isNewTicketModalOpen` and every requisition-Modal title now returns only the comments recording where each Modal used to be.** **Console-error scare run down for the second time today:** the accumulated buffer's `[vite] Failed to reload` entries proved stale against a cold load in a fresh tab (zero errors, `?assetId=a3` preselecting AST-0003) — `read_console_messages` returns a buffer, so a minute-old error reads like a live one. **`CHANGELOG.md` updated** — user-visible on three screens. Session close-out recorded as `CHECKPOINT-2026-09-08-006`. **Merged** (`71ba972`). | Frontend |
| **no PR** (`64dcdcd`…, direct to `main`) | 2026-09-08 | — (no branch) | **Raise F-54 — the product ships SLA commitments no business decision authorises.** **Deviation stated first**, since this file's rule is one row per merged PR: committed directly to `main`, no branch, no PR, keyed to the commit rather than a PR number invented for it. **Nothing was built** — one row added to `OPEN-FINDINGS.md`; zero product code, zero chain documents; `RAISE-FR-MAINT-001` stays at full `PASS`. **The finding:** `ticket-service.ts:14` defines `SLA_HOURS = { Critical: 2, High: 8, Medium: 24, Low: 48 }`, `ticketService.go:19-26` mirrors it (its own comment says *"exactly"*), every ticket gets `slaTargetHours` stamped from it, and the priority selector reads **"Critical (2h SLA)"** … **"Low (48h SLA)"** — a time commitment shown at the moment of filing. **The chain says the opposite in its own words:** PRD *"Still TBD: SLA per stage"*, **no §16 Resolved Question on SLA at all**, Prototype §15 P-009 **`Priority (conceptual — TBD)`**, AC §12 *"SLA per stage… remain **TBD** and are marked **NOT TESTABLE YET**"* — and **no Prototype/AC/Test Case names a numeric SLA anywhere**, verified by grep. **Origin traced:** inherited from the ESAPS reference (`src/data/requisitionData.ts:231,328`), so RAISE did not invent them, but inheriting an unconfirmed number is not confirmation and that app is REFERENCE ONLY per `CLAUDE.md`. **Why it earned a finding: it is F-03 inverted.** F-03 is five business numbers correctly *withheld* — refused across four requests under Q3a's *"Do not invent or use an illustrative number as if confirmed"* — while **SLA is four already invented and shipped in both tiers and displayed as a promise**. The rule was applied with great care to the numbers nobody uses and never to the numbers in production. **What the finding does NOT claim:** it does not invalidate the `PASS`. That rests on the confirmed 4-stage transitions (RQ33) and the AC **deliberately excluded** SLA as NOT TESTABLE YET, so **no executed test asserts an SLA value and no verdict rests on one** — hence it is filed as *unrequested scope carrying unauthorised business values*, under Unresolved rather than Blocking. **How it was found, including the part that reflects badly:** tracing the `priority` field that **PR #122** carried onto the new page hours earlier. **It was visible then and waved through as "pre-existing"**, and `CHECKPOINT-2026-09-08-004` records me noting Prototype's "conceptual — TBD" against a shipped Priority select and choosing not to fold it in. The note was right; concluding it therefore needed nothing was wrong. **Deliberately no code change:** removing the labels or changing the values would be inventing the business decision in the opposite direction. **Three exits, all business decisions:** confirm the values as-is; supply different ones; or decide MVP makes no SLA promise. Validation on `64dcdcd`: `tsc` 0, ESLint clean, **54 files / 286 tests**; register **39 `F-` rows × 5 cells**. `CHANGELOG.md` deliberately not updated — recording a finding changes no user-visible behaviour. Session close-out recorded as `CHECKPOINT-2026-09-08-007`. | Docs only (project-management) |
| **no PR** (`d2d7712`, `758c268`, direct to `main`) | 2026-09-08 | — (no branch) | **Record PRD §16 Resolved Question 53 and propagate it through the chain (F-54 → R-40).** **Deviation stated first**, since this file's rule is one row per merged PR: committed directly to `main`, no branch, no PR, keyed to the commit range. **Business chose option (a) of F-54's three exits — confirm the shipped values as they stand:** Critical **2h**, High **8h**, Medium **24h**, Low **48h**. **No code changed** — behaviour already in production now has authority behind it. **The distinction the whole task turned on, checked before writing anything:** the PRD's *"SLA per stage"* TBD is a **different question** — confirmed is one overall target per ticket keyed by priority (`slaTargetHours`), still open is how long each of the four workflow stages may take, with no budget supplied or implemented. **Conflating them would have silently closed a per-stage question nobody answered** — the shape of the **Q3/Q4** and **Q3a** errors already corrected twice in this project. *"SLA per stage"* survives as TBD in all six documents, along with the vendor model, cost model and delegated-approver rules. **Prototype's trigger line is fixed:** §15 P-009's `Priority (conceptual — TBD)` — the direct cause of F-54, waved through during **PR #122** — now shows the four SLA-labelled options, with the old wording **quoted in the correction note rather than silently overwritten**. **A classification new to this project, and deliberate:** `AC-MAINT-001-10` and `TC-MAINT-001-10` are **testable today — not BLOCKED, not NOT TESTABLE YET** — because the behaviour is already shipped in both tiers, unlike every other recent addition here; **neither is marked PASS**, because neither has been executed. **Matrix v2.9 handled that honestly:** `RAISE-FR-MAINT-001`'s full `PASS` **predates both**, having been granted while SLA was excluded as NOT TESTABLE YET — **no executed test has ever asserted an SLA value** — so the matrix **kept** the PASS on the executed 4-stage transitions and **opened Gap 23** for the unexecuted case rather than folding it in as coverage (*a specification being correct is not coverage*, the Gap 21 principle), then opened and closed **Gap 24** to record its independent verdict that F-54 is resolved. **The sync ran immediately rather than being deferred** — deferring is what created **F-53** this morning, and unlike **F-03** there was no pending input to bundle with. Versions: PRD 0.20→**0.21**, Design 0.18→**0.19**, Prototype 0.19→**0.20**, AC 0.18→**0.19**, Test Plan 0.19→**0.20**, Test Cases 0.28→**0.29**, Matrix 2.8→**2.9**, all through the `.claude/skills` subagents sequentially. **Verified per stage rather than taken on the subagents' reports:** six diffs filtered for hour figures contain **only 2h, 8h, 24h, 48h**; *"SLA per stage"* TBD everywhere; **`TC-DASH-01`'s row byte-unchanged** (2026-08-31 ×1, 2026-09-05 ×2, PASS ×4); **Gap 21 intact**; `RAISE-FR-EXEC-001` still **`PASS (partial)`**. Validation: `tsc` 0, **54 files / 286 tests** (unchanged, as expected); register **39 `F-` rows × 5 cells**. `CHANGELOG.md` deliberately not updated — confirming already-shipped values changes nothing a user would notice. Session close-out recorded as `CHECKPOINT-2026-09-08-008`. | Docs only (requirements + chain + project-management) |
| **no PR** (`d0770e4`, direct to `main`) | 2026-09-09 | — (no branch) | **Formally execute `TC-MAINT-001-10` — PASS on the frontend tier; Gap 23 closed, Gap 25 opened.** **Deviation stated first**, since this file's rule is one row per merged PR: committed directly to `main`, no branch, no PR, keyed to the commit. **No code touched** — this is execution reporting. **The execution:** run **2026-09-09** against the real running app (`raise-frontend`, port 5173) on `main` **`88f1017`**, **driving the actual UI rather than calling services directly**. The Priority selector was read from the live DOM: **exactly four** options, labelled verbatim `Critical (2h SLA)`, `High (8h SLA)`, `Medium (24h SLA)`, `Low (48h SLA)`. One request created per priority through the form itself, each ticket's `slaTargetHours` read from **its own Ticket Detail page**: Critical → `ITR-2026-007` **2h**, High → `ITR-2026-008` **8h**, Medium → `ITR-2026-009` **24h**, Low → `ITR-2026-010` **48h** — all four matching. **Each ticket's own subject was confirmed on its own detail page**, so each figure belongs to the ticket just created and not a neighbouring row. Zero console errors. **Result: PASS.** **The limitation, recorded rather than glossed, and the reason a second gap now exists:** `TICKET_API_ENABLED` (`featureFlags.ts:30`) is **off by default**, so the run went through the **mock ticket repository** and the value came from `ticket-service.ts:14` **only**; `go-template-main/service/ticketService.go:19-26`'s identical map was **not exercised**, and **no backend test covers `slaHours`** **[CORRECTED 2026-09-09]** The claim quoted just above was wrong, and so was the grep that "confirmed" it. The search used `SlaTargetHours`; the Go field is **`SLATargetHours`** (capital `SLA`), so the case-sensitive pattern missed the one place it appears. `ticketService_test.go:99` already asserted `SLATargetHours` **= 8** for High, incidentally, inside a broader snapshot test. The true position was **one of four priorities covered**, not none. Same failure mode as **F-49** — a real measurement whose conclusion outran what it showed. **All four priorities plus the unknown-priority case are now covered** by `TestCreateTicket_StampsSLATargetHoursForEveryPriority` and `TestCreateTicket_UnknownPriorityGetsZeroSLATarget`. What is still uncovered: **no test compares the two maps against each other**, and **no run has exercised the Go tier through the HTTP path**. (grep of `go-template-main/service/*_test.go` returns nothing). That tier is assured **only by inspection**, and by the Go file's own comment claiming it mirrors the frontend *"exactly"* — a claim nothing currently verifies. **Matrix v2.10 split that honestly rather than taking either easy verdict:** **Gap 23 CLOSED**, because its own stated closing condition was *"a formal execution of `TC-MAINT-001-10` against the real running app"* and that was **literally met**; the backend shortfall carved out as **new Gap 25** rather than stretched into Gap 23's closure or used to refuse a closure genuinely earned. Its reasoning was this project's own principle — *a specification being correct is not coverage* — applied by analogy: **exercising one of two cited tiers is not coverage of both.** **What the requirement now rests on, which is the part that actually moved:** before today `RAISE-FR-MAINT-001`'s full `PASS` rested solely on `TC-MAINT-001-01..09` and **predated** `TC-MAINT-001-10`, so **no executed test had ever asserted an SLA value**; that is no longer true, and the matrix row says so, with the caveat that the PASS is still **not** end-to-end both-tier assurance. **Dated 2026-09-09 deliberately** — the RQ53 confirmation and chain sync were 2026-09-08, and the project had already corrected one dating slip where a sync carried the decision's date instead of its own. Versions: Test Cases 0.29→**0.30**, Matrix 2.9→**2.10**, both through the `.claude/skills` subagents. **Verified after the subagents returned rather than taken on their reports:** the frontend-only limitation and absent backend test are in the case's own status; the execution row carries 2026-09-09 and no 2026-09-08; **Gap 21 intact**; `TC-DASH-01`/`TC-EXEC-001-01` superseded PASS records untouched; `RAISE-FR-EXEC-001` still **`PASS (partial)`**; *"SLA per stage"* still open. Validation: `tsc` 0, ESLint clean, **54 files / 286 tests** (unchanged, as expected). `CHANGELOG.md` deliberately not updated — executing a test changes nothing a user would notice. Session close-out recorded as `CHECKPOINT-2026-09-09-001`. | Docs only (test cases + matrix + project-management) |
| **PR #123** (`50fb36b`) | 2026-09-09 | `test/backend-sla-hours-coverage` | **Cover `slaHours` on the backend, and correct this project's own claim that nothing did. Gap 25 CLOSED, Gap 26 OPENED.** **Test-only — no production code changed.** **The error is stated before the result, because the PR exists for it:** Gap 25 said *"no backend test covers `slaHours`"* — **and the grep that "confirmed" it was wrong too.** The search used `SlaTargetHours`; the Go field is **`SLATargetHours`** (capital `SLA`), so the case-sensitive pattern missed the one place it appeared, and **`ticketService_test.go:99` already asserted `SLATargetHours = 8` for High**, incidentally, inside a broader snapshot test. **The true position was one of four priorities covered, not none** — the **same failure mode as F-49**: a real measurement whose conclusion outran what it showed. **The claim had reached six documents before being checked** (Test Cases ×6, Matrix ×8, `CURRENT-STATUS`, `DEVELOPMENT-LOG`, `NEXT-STEP`, `PROJECT-CHECKPOINTS`); every correction **quotes the wrong wording and names it wrong** rather than overwriting it. **The tests:** `TestCreateTicket_StampsSLATargetHoursForEveryPriority`, table-driven over all four priorities, asserting **2 / 8 / 24 / 48** and that the priority survives onto the ticket — **expected values hardcoded rather than read from `slaHours`, deliberately**, since a test that reads the map it checks would pass against *any* values including a silent edit away from **RQ53**; and `TestCreateTicket_UnknownPriorityGetsZeroSLATarget`, pinning a fifth priority at `0` as **observed behaviour, not an endorsed rule** — PRD §16 confirms four priorities and says nothing about a fifth, **so no fallback was invented**. **Mutation-tested:** `Critical: 2→4` fails 2 subtests, `Low: 48→24` fails 2, dropping `Priority: input.Priority` fails 5; restored → 0 failures. **What is still NOT covered, so the correction does not overshoot the other way:** **(a)** nothing exercises the two maps **against each other** — they are asserted by two separately-written sets of expectations that happen to agree, while `ticketService.go`'s comment claiming it *"mirrors the frontend map exactly"* stays **unverified by anything executable**; **(b)** no run has exercised the Go tier through the **real HTTP path** (`TICKET_API_ENABLED` off by default; these are unit tests calling the service directly). **Matrix v2.11 carries that remainder as new Gap 26** rather than stretching Gap 25's closure over it, and closed Gap 25 on its own stated condition. **A second correction, found by re-checking the matrix rather than trusting the subagent's report:** **Gap 25's heading still read `left OPEN`** while its own body recorded **`CLOSED 2026-09-09`** fifty-four lines below — a reader scanning gap headings would have stopped at the wrong answer. **Gap 23's heading had been updated correctly on closure in the same revision; Gap 25's was missed.** Fixed with the old wording named, not silently replaced; all six headings now lead with a status matching their body (21 OPEN, 22 opened+closed, 23 CLOSED, 24 opened+closed, **25 CLOSED**, 26 OPEN), and **Gap 26's `left OPEN` was deliberately left alone** — it is genuinely open. **`RAISE-FR-MAINT-001` stays full `PASS`** — already the ceiling, and the new evidence is a PASS not a contradiction — but what it **rests on** changed again: the 4-stage evidence, **plus** the 2026-09-09 frontend execution, **plus** these backend tests; still **not** cross-tier agreement and **not** an HTTP-path run. `RAISE-FR-EXEC-001` stays **`PASS (partial)`**; **Gap 21 intact**; Gap 23's closure and `TC-MAINT-001-10`'s PASS untouched and not re-litigated. Versions: Test Cases 0.30→**0.31**, Matrix 2.10→**2.11**, both through the `.claude/skills` subagents. **Validation on merged `main`, run rather than assumed:** backend `go build`/`vet`/`test` clean with the 5 new subtests passing; `gofmt` clean **over LF-stripped content** (the **R-34 method** — `core.autocrlf=true` makes the CRLF working tree flag files regardless); frontend `tsc` **0**, ESLint **0 warnings**, **54 files / 286 tests** (unchanged, as expected for a backend-only test addition); CI green on both jobs before and after the heading correction. `CHANGELOG.md` deliberately not updated — adding tests and correcting documents changes nothing a user would notice. Session close-out recorded as `CHECKPOINT-2026-09-09-002`. | `go-template-main/service/ticketService_test.go` (test-only) + test cases + matrix + project-management |
| **no PR** (docs close-out, direct to `main`) + **`PR #124` OPEN, NOT MERGED** | 2026-09-09 | — (docs) / `test/sla-map-contract` (`8f27358`) | **Gap 26 documentation close-out: (b) CLOSED on an HTTP-path execution; (a) evidence PASS but the gap STAYS OPEN because the test is on an unmerged PR; new Gap 27 opened.** **Deviation stated first**, since this file's rule is one row per merged PR: the documentation half was committed directly to `main`, keyed to the commit, while the code half sits on `PR #124` and is deliberately **not** merged. **The disagreement is the headline.** The instruction was to record **both** sub-parts as PASS and end with Gap 26 no longer OPEN; **the matrix, asked for an independent verdict, refused half of it** — *a gap opened against the state of `main` cannot be closed by code that does not exist on `main`.* Adopted rather than overridden: the evidence for (a) is recorded as the genuine PASS it is, and the gap stays OPEN **contingent on `PR #124` merging**. **Forcing it would have been the "edit the document so the result looks better" this project forbids.** **(b), PASS:** a real `docker compose` stack (`stl_asset_pj-backend-1` Go/Fiber:8080, `stl_asset_pj-db-1` postgres:16-alpine) driven directly over HTTP through the **real JWT auth middleware on the protected group** — `POST /api/auth/login` → `GET /api/employees`+`/api/assets` → `POST /api/tickets` ×4 → **separate** `GET /api/tickets/{code}` ×4 → direct Postgres query. All 201: Critical → `ITR-2026-001` → **2**, High → `ITR-2026-002` → **8**, Medium → `ITR-2026-003` → **24**, Low → `ITR-2026-004` → **48**, matching in the POST body, the independent GET **and** the database row. **Provenance checked before the run counted as evidence:** image built 2026-09-04, `ticketService.go` last changed 2026-08-23 (`460bafb`) — so the running code is `main`'s code. Schema detail: **no `sla_target_hours` column**, the value lives in `tickets.doc` **jsonb**. **Side effect recorded:** four real tickets now in the dev DB. No failure, so nothing fixed, no production code touched. **(a), evidence PASS:** `slaContract_test.go`'s `TestSLAHoursMatchesFrontendContract` parses `SLA_HOURS` from the TypeScript source and compares it with the Go map **both ways**, and **pins both maps at four entries** — without which dropping the same key from both files would read as agreement while the behaviour stopped matching **RQ53**. **Seven mutations, all seven FAIL** (frontend drift, backend drift, key removed, `SLA_HOURS` renamed, non-integer value, source file missing, same key dropped from both tiers); baseline PASS, production files **byte-identical to HEAD** after. **Stakeholder chose the contract test** over a shared fixture or a backend-owned endpoint — a coverage gap should not be paid for with production code — and the analysis surfaced that `HttpTicketRepository.create()` already omits `slaTargetHours` and returns the backend's response, so in API mode the backend **already** is the source of truth. **The mutation run exposed a hole in the new test:** every frontend-side mutation first passed because **`go test` served a cached result** — the `.ts` file is outside the module and the cache ignores it; plain `go test` said `ok (cached)` where `-count=1` said FAIL. CI now runs **`go test -count=1 ./...`**. **Gap 27 OPENED** for (b)'s honest remainder: (b) named a run with `TICKET_API_ENABLED=true`, and what ran was `curl`, so `ticket-service.ts`'s real-API branch has **still never been exercised by anything** — the third remainder carried forward rather than absorbed (Gap 23→25→26→27). `RAISE-FR-MAINT-001` stays full **`PASS`** (ceiling), now additionally resting on the HTTP-path confirmation, and still **not** on a merged cross-tier test or any frontend real-API run. Versions: Test Cases 0.31→**0.32**, Matrix 2.11→**2.12**, both through the `.claude/skills` subagents. **Verified after they returned rather than taken on their reports:** unmerged status stated (5×), `PR #124` referenced (17×), `-count=1` limitation recorded (10×); every gap heading leads with a status matching its body (**Gap 26 "STAYS OPEN OVERALL"**, **Gap 27 "left OPEN"**, Gap 21 OPEN, Gap 23/24/25 closed); `RAISE-FR-EXEC-001` still `PASS (partial)`; **F-03 still open**, `TC-WARRANTY-001-07` still **BLOCKED (partial)**; hour figures across both documents are only 2/8/24/48. `CHANGELOG.md` deliberately not updated — executions and document assessments change nothing a user would notice. Session close-out recorded as `CHECKPOINT-2026-09-09-003`. | Docs only (test cases + matrix + project-management); the test and CI flag remain on `PR #124` |
| **PR #124** (`9e125d6`) | 2026-09-09 | `test/sla-map-contract` | **Verify the two SLA maps against each other, and discharge the contingency that kept Gap 26 open. Gap 26(a) and Gap 26 overall: CLOSED.** **Test-only — the diff is two files and no production code:** the new `go-template-main/service/slaContract_test.go`, and one line of `.github/workflows/ci.yml` (`go test ./...` → **`go test -count=1 ./...`**) with its reason in a comment. **The test:** `TestSLAHoursMatchesFrontendContract` parses `SLA_HOURS` out of the TypeScript source and compares it against the Go `slaHours` map **in both directions**, and **pins both maps at exactly four entries** — without that pin, dropping the same key from both files would read as agreement while the shipped behaviour stopped matching **RQ53**. Values remain the confirmed **2 / 8 / 24 / 48**; **no new value introduced**. It is the first thing that verifies `ticketService.go`'s own comment claiming it *"mirrors the frontend map exactly"*. **Seven mutations, all seven FAIL** (frontend drift, backend drift, key removed, `SLA_HOURS` renamed, non-integer value, source file missing, same key dropped from both tiers at once); baseline PASS; production files **byte-identical to HEAD** afterwards. **Design rule: every failure mode fails loudly and never falls through to a pass because it could not find its input** — the direct lesson of the `SlaTargetHours` grep error corrected in PR #123. **The mutation run exposed a hole in the test itself, which is why the CI flag is in the same PR:** every frontend-side mutation first PASSED, because **`go test` served a cached result** — the `.ts` file is outside the Go module and the cache does not track it (plain `go test` said `ok (cached)` where `-count=1` said FAIL). **Approach chosen by the stakeholder** from three options (shared fixture / contract test / backend as source of truth): the contract test, since Gap 26(a) is a coverage gap and not a correctness bug — no production behaviour change, no shared fixture, no new endpoint; the analysis also surfaced that `HttpTicketRepository.create()` already omits `slaTargetHours` and returns the backend's response, so in API mode the backend **already** is the source of truth. **Why this PR needed its own close-out even though the docs were written yesterday:** `CHECKPOINT-2026-09-09-003` had recorded a **refusal** — the matrix would not close Gap 26(a) while the test lived only on a branch, because *a gap opened against the state of `main` cannot be closed by code that does not exist on `main`* — so the closure was written as **contingent on this merge**. **The merge discharges it by fact rather than by re-argument.** **Pre-merge checks run rather than assumed:** CI green both jobs at `8f27358`, `MERGEABLE`/`CLEAN`, diff filtered to confirm nothing but a `_test.go` and the workflow; CI had last run before `main` advanced by `a7aeb1e`, which was docs-only and cannot affect a Go test. **Post-merge validation on `main`:** the test file exists; `ci.yml:121` reads `go test -count=1 ./...`; `go build`/`go vet` clean; `go test -count=1 ./service/` reports **`--- PASS: TestSLAHoursMatchesFrontendContract`** plus all four priority subtests. **Matrix v2.13 independently assessed Gap 26(a) CLOSED and Gap 26 CLOSED overall, and corrected its own heading** (which read "STAYS OPEN OVERALL"), naming the old wording rather than overwriting it and citing the Gap 25 heading-lag it had already had to fix once; it verified the merge against the repository itself rather than the report. `RAISE-TEST-CASES.md` stays **v0.32** — it already recorded the test as passing-but-unmerged, and the merge changes the matrix's gap verdict, not the test case's result. **Verified after the subagent returned:** all seven gap headings lead with a status matching their body (21 OPEN, 22 opened+closed, 23 CLOSED, 24 opened+closed, 25 CLOSED, **26 CLOSED**, **27 left OPEN**); `RAISE-FR-EXEC-001` still `PASS (partial)`; **F-03 still open**; hour figures still only 2h/8h/24h/48h. `RAISE-FR-MAINT-001` stays full **`PASS`** (ceiling), now resting additionally on a **merged** cross-tier comparison where v2.12 had to state explicitly that it did not — still **not** on any frontend real-API execution (**Gap 27**, OPEN). `CHANGELOG.md` deliberately not updated — a test and a CI flag change nothing a user would notice. Session close-out recorded as `CHECKPOINT-2026-09-09-004`. | `go-template-main/service/slaContract_test.go` (test-only) + `.github/workflows/ci.yml` + matrix + project-management |
| **no PR** (`40c85be` + this close-out, direct to `main`) | 2026-09-09 | — (docs) | **Gap 27 executed and CLOSED — the frontend's own real-API branch driven through a browser for the first time; Open Finding F-55 opened and left open.** **Deviation stated first**, since this file's rule is one row per merged PR: committed directly to `main`, keyed to the commits. **No production code, no test code** — execution reporting plus a finding. **What Gap 27 asked and why two green runs had not answered it:** `ticket-service.ts`'s real-API branch, gated by `TICKET_API_ENABLED`, had **never been executed by anything** — the 2026-09-09 UI execution went through the **mock** repository and the `curl` HTTP-path run **bypassed the frontend entirely**. **Mechanism was the project's own, not a new harness:** `docker-compose.yml:63` already bakes `VITE_TICKET_API_ENABLED:-true`, so the composed frontend container serves a bundle with the branch selected; the container (`:3000`) was used rather than a dev server on 5173 because the backend's `CORS_ALLOW_ORIGINS` is **`http://localhost:3000` only**. **Provenance checked before the run counted:** existing image built 2026-09-03 while `CreateRequisition/index.tsx` last changed 2026-09-08, so the frontend was rebuilt from current source (image 2026-09-09T09:08); `ticket-service.ts`/`ticket-repository.ts`/`featureFlags.ts` predate both. **Proof the branch was selected rather than assumed:** on first load the browser issued `GET /api/tickets` → 200, **a request that does not exist at all in mock mode**. **Evidence, from the browser's own network log and not `curl`:** four `POST http://localhost:8080/api/tickets` → **201**, each preceded by an `OPTIONS` preflight (genuine cross-origin from `:3000`); the first response body carrying `"slaTargetHours":2` **as received by the frontend**; then four `GET /api/tickets/ITR-2026-005..008` → **200** issued by the app itself on each Ticket Detail page. Critical → `ITR-2026-005` → **2**, High → `ITR-2026-006` → **8**, Medium → `ITR-2026-007` → **24**, Low → `ITR-2026-008` → **48**, matching in the POST response, the frontend read-back **and** the Postgres row; each ticket's own subject confirmed on its own page. **A real defect was found, and is deliberately not folded into the PASS:** the first submit failed with `GET /api/employees/e1` → **404** and **no POST following**, because `CreateRequisition/index.tsx:60` defaults `requesterId` to `'e1'`, a `mockData.ts` fixture id absent from Postgres, so `ticket-service.ts:31` throws first — resolving in mock mode only because the mock repository is seeded from that same fixture file, which is why **286 tests and `tsc` are blind to it**. Filed as **F-55** (`40c85be`), **`OPEN — BLOCKED on a business decision, and deliberately not fixed`**: how the requester resolves in the default flow (logged-in user / explicit employee-or-delegate selection / another stakeholder rule), with the verified constraint that `AuthContext`'s `User` carries **no employee id** and the demo user's `id` is the literal `"admin"` — making it partly downstream of **F-08**. The execution reached the branch through the page's **own supported `?requesterId=` entry point** (its documented Employee Detail route), a supported route and not a fix; **the default no-param entry is unusable in real-API mode until the decision lands**, and both chain documents say so. **Matrix v2.14 assessed Gap 27 CLOSED**, noting that unlike Gap 26(b) — which closed on substance while its named mechanism went unused — **every clause of Gap 27's condition was literally met**, so **no successor gap was carved out**: the first time in five consecutive gaps (23→25→26→27) that none was needed. **Two corrections, both mine, both caught by checking:** I cited the compose flag as line **62** when it is line **63** (62 is `VITE_EMPLOYEE_API_ENABLED`) — the matrix subagent **caught it independently and refused to propagate it**, and Test Cases was corrected through the same subagent (0.33→**0.34**, recorded as a **citation correction only**, no verdict/status/figure/business value touched); and **Matrix v2.14 cites Test Cases v0.33 rather than v0.34** — stated rather than left to be found, and no re-assessment is owed since only a line number changed between them. Versions: Test Cases 0.32→0.33→**0.34**, Matrix 2.13→**2.14**, both through the `.claude/skills` subagents; each verified from the files before the next layer ran. **Verified rather than taken on the subagents' reports:** no new TC/AC id invented (1:1 convention held, `TC-MAINT-001-10` still the only case in this family), suite totals **unchanged** (`96 | 63 | 26 | 4 | 3`), F-55 referenced 16× in Test Cases and 26× in the Matrix with **no resolved claim anywhere**, all seven gap headings leading with a status matching their body (21 OPEN, 22 opened+closed, 23 CLOSED, 24 opened+closed, 25 CLOSED, 26 CLOSED, **27 CLOSED**), `RAISE-FR-EXEC-001` still `PASS (partial)`, **F-03 still open**, hour figures still only 2h/8h/24h/48h. `RAISE-FR-MAINT-001` stays full **`PASS`** (ceiling), now resting additionally on the frontend real-API execution. **Eight test tickets** (`ITR-2026-001`…`008`) now exist in the dev database. `PROJECT-TIMELINE.md` and `CHANGELOG.md` deliberately not updated — no phase moved and nothing a user would notice changed. Session close-out recorded as `CHECKPOINT-2026-09-09-005`. | Docs only (test cases + matrix + project-management; `OPEN-FINDINGS.md` committed separately at `40c85be`) |
| **PR #125** (`c3bac79`) | 2026-09-10 | `fix/f-56-assetdetail-employee-link` | **Stop the Asset Detail assignee link from guessing an employee id. Resolves F-56.** Found during the 2026-09-09 code review of the SLA work: `AssetDetail/index.tsx:313`'s assignee link fell back to the literal `'e1'` whenever `assignedEmployeeId` was missing, gated only on `assignedTo` being truthy. **Reachable today with real seeded data, not merely hypothetical:** fixture `a9` (Conference Room A) has `assignedTo` set but `assignedEmployeeId: null`, so clicking its assignee link navigated to Sarah Chen's real profile instead of erroring or hiding the link — the same hardcoded-fixture-id pattern as **F-55**, in a second location F-55's own investigation didn't cover. **Unlike F-55, needed no business decision** — there is no "who is the requester" question, only "do not guess an id," so `assignedTo`'s own display and business meaning are unchanged. **Fix:** the control now renders as a link only when `assignedEmployeeId` is present; otherwise the same label renders as inert text. Two new tests: `a9` (no `assignedEmployeeId`) shows the label with no link and no navigation on click; `a1` (real `assignedEmployeeId`) still links correctly, confirming the fix didn't overcorrect the working case. **F-56 opened and Resolved in the same session** — opened `2026-09-09` when found, fix committed same day, merge `2026-09-10` after CI review; not collapsed into one step because the register's own convention (reinforced this session against Gap 26(a)) is that a finding is not closed by code that isn't yet on `main`. Validation: `tsc` 0, ESLint 0, **54 files / 288 tests** (was 286). | Frontend only (`AssetDetail/index.tsx` + test) + `OPEN-FINDINGS.md` |
| **PR #126** (`265c152`) | 2026-09-10 | `fix/ticket-priority-validation-error-classification` | **Validate `Priority` before the SLA lookup, and classify `CreateTicket` errors correctly.** Two findings from the same code review. **Finding 2:** `ticketService.go`'s `CreateTicket` computed `SLATargetHours: slaHours[input.Priority]` with no validation anywhere in the chain — an empty, wrong-case, or unrecognized `Priority` silently produced `SLATargetHours: 0` via Go's map zero-value, no error, ticket persisted with **201**. Now validated against the four confirmed values (PRD §16 RQ53) before either the employee or asset lookup runs; anything else is rejected with a new `ErrInvalidPriority` sentinel. **Does not change or add to the confirmed set** — `2/8/24/48` untouched, only rejects what was never one of them. **Finding 3:** the controller mapped every `CreateTicket` error — employee/asset not found **and** a genuine repository/DB failure alike — to the same blanket `400`. Now discriminates exactly like `updateTicket` already does for the other three ticket endpoints: `ErrEmployeeNotFound`/`ErrAssetNotFound`/`ErrInvalidPriority` → 400, anything else → 500, with no raw error text in the 5xx body (F-19 convention) — the two existing not-found sentinels were already defined in `employeeService.go`/`assetService.go` but were being discarded and replaced with a fresh unsentineled error before this fix. **Tests:** `TestCreateTicket_UnknownPriorityGetsZeroSLATarget` (asserted the old silent-zero behaviour) superseded by `TestCreateTicket_RejectsUnknownPriority` (empty/whitespace/wrong-case/unrecognized, each asserting the sentinel **and** that nothing was persisted) and `TestCreateTicket_ValidPrioritiesStillSucceed`; new `controller/ticketController_test.go` drives the real handler through `fiber.App.Test()` against a fake `TicketService` — no live DB needed — covering each client-error case (400), a genuine repo error (500, body verified not to leak the raw driver text), and the happy path (201). **A gofmt defect in the new test file, caught before commit, not after:** the first draft (written 2026-09-09) failed the R-34 method (checked over LF-stripped content, not the CRLF working tree) — fixed by running `gofmt` on it directly before this commit. Validation: `go build`/`vet` clean, `gofmt` clean over LF content, `go test -count=1 ./...` clean across `controller`/`middleware`/`service`; `TestSLAHoursMatchesFrontendContract` and `TestCreateTicket_StampsSLATargetHoursForEveryPriority` re-verified passing on merged `main`. | Backend only (`ticketService.go`, `ticketService_test.go`, `ticketController.go`, new `ticketController_test.go`) |
| **PR #127** (`1da449f`) | 2026-09-10 | `docs/finding4-compose-env-cross-reference` | **Cross-reference `docker-compose.yml` and `.env.example`'s conflicting default claims for the same `VITE_*_API_ENABLED` flag names. Resolves Finding 4.** `docker-compose.yml`'s build-args comment said `true` is the "Default ... for this stack"; `frontend/.env.example`'s comment said `false` is "the default", for the same variable names. Both were correct for their own context (the composed Docker stack, which runs a real backend + Postgres, vs. plain local `npm run dev`/`npm test`, which does not) — **neither said so**, and read in isolation (how a new developer typically encounters one or the other) they present opposite unqualified claims about the same variable. Found during the 2026-09-09 code review; **never filed as a numbered `F-NN` finding in `OPEN-FINDINGS.md`** — tracked only in that review's own output, so this close-out records it directly rather than inventing a register row for it. **Fix: one cross-referencing comment added in each file**, naming the other context and pointing to the matching note in the other file. **Comment-only** — confirmed via `git diff -U0` that every added line in both files begins with `#`, no non-comment line moved. Validation: `docker compose config -q` clean, `python -c "yaml.safe_load(...)"` clean, `.env.example` still holds only placeholder values (`localhost`, `false`, `development`), `git diff --check` clean, line endings unchanged (CRLF, matching pre-existing state — neither file is pinned by `.gitattributes`, only `*.go` is). **Post-merge verification on `main` `1da449f`, not inferred from CI alone:** `docker compose config -q` re-run clean, both comments present, all runtime default values byte-identical to before merge (`VITE_ASSET_API_ENABLED=false` etc. in `.env.example`; `VITE_AUTH_API_ENABLED:-true` etc. in compose). **F-03, F-55, Gap 21, F-52 and Finding 5 all untouched by this PR.** `CHANGELOG.md` deliberately not updated — a clarifying comment changes nothing a user would notice. Session close-out recorded as `CHECKPOINT-2026-09-10-002`. | `docker-compose.yml` + `frontend/.env.example` (comments only) |
